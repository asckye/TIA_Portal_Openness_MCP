# v2.7.56

引擎 **2.7.56.0**（V20/V21 均重建）。工具 **450** 不变（`Connect` 新增 `projectName` / `allowStart`）。

## 2.7.55 部署后的真机结果（`项目1`，TIA V21，PLCSIM Advanced 8.0）

| 项 | 结果 |
|---|---|
| `Connect` | 虚拟机上同时开着 `AutomaticDipCoatingMachine`（pid 15100）与 `项目1`（pid 4840）；MCP 调用 60 s 超时；`camp.py call GetState` 显示引擎绑在 **新启动的空实例 pid 15748**、`project '-'`；`ListPortalProcessProjects` 列出三个进程；`AttachToOpenProject {projectName:"项目1"}` 换回 4840。本版修 |
| `RunPlcSimAdvancedTestScenario singleStep` | **PASSED** 8/8、4/4：`operatingModeApplied SingleStep_C`，`{cycles:5}` → `cyclesStepped 5`、`waitedMs 71`，`Cycles` 304→309（tolerance 0），`Start=false` + `{cycles:2}` → Running=false、Cycles 仍 309；`operatingModeRestored Default` |
| `SaveProject` | 通过，`GetProject` `IsModified false`、`LastModified 2026-09-21T05:48:20Z` |

## 修复

- **`Portal.ConnectPortal(projectName, allowStart, info)`**：`GetProcesses()` 非空 → 逐进程 `AttachWithTimeout`（20 s，累计 45 s 预算），记录 `ConnectLogic.Candidate {ProcessId, Attached, HasSession, ProjectNames, Failure}`；`projectName` 命中即停；`ConnectLogic.Choose`：命中工程 → 首个有工程 / 会话 → 首个可附加；`null` → `PortalException(InvalidState, ConnectLogic.Refusal(...))`（"No new instance was started"），除非 `allowStart`；`GetProcesses()` 为空才 `new TiaPortal(launchMode)`。安全拒绝（`EngineeringSecurityException`）照旧立即抛出；未选中的候选全部 `Dispose`。命中的 `projectName` 记为 `_expectedProjectName`（自愈只回绑它）；未命中时 `info.warning` 说明绑到了哪个进程。
- **`Connect` 工具**：`projectName`（可选）、`allowStart`（默认 false）；`Message` 区分"attached to PID n"与"Started a new TIA Portal instance"，`Meta` = `processCount` / `candidates`（每进程一句）/ `boundProcessId` / `startedNew` / `attachElapsedMs` / `warning` / `launchMode`。
- `IsProjectNull` 自愈与 `OpenProject` 里的 `ConnectPortal()` 走同一条路：有进程就绝不自启。

## 真机结果（2026-09-21 部署后，`项目1`，TIA V21）

| 项 | 结果 |
|---|---|
| `Bootstrap` | `serverVersion 2.7.56.0`，未连接 |
| `ListPortalProcessProjects`（前） | 2 个进程：4840 = `项目1`，15100 = 维护者的 `AutomaticDipCoatingMachine`（2.7.55 误启的 15748 已由维护者关掉） |
| `Connect {projectName:"项目1"}` | **通过**：attached to PID 4840，`processCount 2`、`candidates ["PID 4840: project 项目1"]`（命中即停，没有探测 15100）、`attachElapsedMs 70`、`boundProcessId 4840`、`startedNew false` |
| `ListPortalProcessProjects`（后） | 仍是 2 个进程——没有新实例 |
| `Connect {projectName:"NoSuchProject_2756"}` | 探测两个进程（385 ms）后绑到 4840，`warning` 说明该工程不在任一进程里、请用 `AttachToOpenProject` / `OpenProject`；`startedNew false` |
| `GetState` | `project 项目1`，`portalProcess.boundProcessId 4840`，`processAlive true` |

未跑（要关 `项目1`，未请求）：只剩维护者工程时 `Connect {projectName:"项目1"}` 应绑到维护者的进程并带 `warning`。`Connect {allowStart:true}` 只在明确需要隔离实例时用（随后必须 `Disconnect`）。小瑕疵：名字不存在时 `Message` 末尾是两个句号（warning 已带句号），下一版顺手修。

## 验证

离线 2218 项；形状检查 V20 2805 / V21 3097。真机台账：`Connect` 行记录 2.7.55 的自启缺陷与 2.7.56 修法。
