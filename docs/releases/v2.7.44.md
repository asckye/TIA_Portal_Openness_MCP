# v2.7.44

引擎 **2.7.44.0**（V20/V21 均重建）。工具 447 不变，默认 lite 清单 56 项不变。2.7.43 部署后的真机重跑（[v2.7.43 验证](v2.7.43.md#验证)）三处守卫都拦住了、TIA 未退出，但第一次看到了 CFC 空导出的真实结构，预检解析器要修。

## 修复

| 项 | 内容 | 真机依据 |
|---|---|---|
| CFC 预检解析器 | 图表名只取名字含 Chart、且不含 Folder / List、不以 s 结尾的元素（`CfcLogic.IsChartElement`）；离线测试改用真实的导出形状 | 空导出 `Data.xml`：`Document > DocumentInfo / FunctionChartsFolder(Name="Charts") > ObjectList / UsedAlarmClasses`（ZIP 条目 `Data/`、`Data.xml`，485 字节）——2.7.43 把 `FunctionChartsFolder` 的 `Name` 当成图表，清单为 `["Charts"]`，`ManageCfcChartProtection read MCP_NONE` 仍被拒（NotFound）但 `exportInstructionData` 的"无图表"分支不会触发 |

带真实图表的 PLC 上图表元素名尚未见过（参考工程无 CFC 图表）；`meta.preflight.xmlElements` 会把它暴露出来，若解析不到，`skipChartPreflight=true` 仍可放行。

## 验证

| 检查 | V20 | V21 |
|---|---|---|
| 离线逻辑测试 | 2144 通过（新增 1 项：图表元素规则；空导出 / 文件夹 / 列表容器按真实形状） | 同左 |
| 引擎 API 形状检查 | 2785 | 3073 |
| 真实工程 | — | 2.7.43 引擎：三处守卫通过（见 v2.7.43）；待部署后重跑 `ManageCfcChartProtection read MCP_NONE` 应回 InvalidState（0 张图表） |
