# v2.7.33

引擎 **2.7.33.0**（V20/V21 均重建）。工具 389 → **396**，默认 lite 清单 56 项不变。本版做两件事：修 2.7.32 真机暴露的五处问题，以及"官方 Openness API 全量对齐"计划 [路线图 §2.0](../development/roadmap.md#20-官方-api-全量对齐计划) **阶段 3 子批次 ③-④：Base 收尾**——`Siemens.Engineering.Base.dll` 里最后 52 个未封装功能类型（门户 / 会话诊断、事务、对象标识、受保护工程与 UMAC 在线凭据、传输路由与 R/H 冗余系统、硬件工具、Web 应用 / 遥控数据点 / 动态证书管理、类型化的传输提示 / 结果 / 交叉引用 / 比较 / 目录 / 多用户 / 版本控制 / 设置行）。**Base 的功能类型缺口归零，阶段 3 收口。**

## 2.7.32 真机问题的修复

| 问题 | 修法 |
|---|---|
| 工程级 `SyslogServerComposition.Create(name)` 让 TIA Portal V21 崩溃（两次复现） | `ManageSyslogServers scope=project create` 只留预览：`dryRun=false` 在调用前被 `NotSupportedException` 拒绝，文案说明原因与替代路径（TIA 界面里建，本工具 `read` / `update` / `assignModule` / `delete`）；描述里标 PREVIEW ONLY |
| `ManagePlcCertificate template` 误走"按 `certificateId` 查证书"的门 | `template` 加进免查证书的动作列表 |
| `RequireUmacDevice` 报错指向 CPU 项，而 `UmacDevice` 在设备上 | 文案改为 "on the reference project it lives on the Device (devicePathJson only, itemPathJson=[])" |
| `CheckLibraryUpdates` 的 `messageParts` 经标量读渲染，1539 条 = 2.2 MB | `KeyValuePair<string,string>` 直接渲染为 `parts {Key: Value}`（DeviceName / LibraryTypeName / LibraryVersionNumber / UpToDate / PathToInstance / InstanceName / CurrentVersionNumber），非键值部件才进 `messageParts` |
| 两个 TIA 进程同开时，`IsProjectNull()` 自愈经 `ConnectPortal()` 重挑进程，引擎在两次调用之间改绑到了另一个工程 | 记住显式绑定的工程名（`AttachToOpenProject` / `OpenProject` / `CreateProject` 设，`Connect` / `Disconnect` / `CloseProject` 清）：自愈只回绑该名字（3 s 内找不到就报 "Project is null: the explicitly bound project 'X' is not open…"，绝不改绑别的工程）；每次写操作（`AcquireHmiEditAccess`）与事务开始前核对 `_project.Name` 未变，变了就拒绝并要求重新附加；`GetState` 发现不一致时记警告 |

## 新增工具（域 Portal / Project / Hardware / PLC-Online）

| 工具 | 动作 | 官方入口 |
|---|---|---|
| `ReadPortalInfo` | 所有运行中的 TIA Portal 进程快照（Id / Mode / Path / ProjectPath / AcquisitionTime、`AttachedSessions`：Id / Version / IsActive / AttachTime / UtilizationTime / AccessLevel / TrustAuthority / ProcessPath / ProcessId、`InstalledSoftware`：产品名 / 版本 / 选件）、已绑定进程、工程的 `TextCategories`（V21）与 `HwUtilities`、`ObjectIdentifierProvider` 可用性、显式绑定的工程名；无工程也可用 | "Diagnostic interfaces on TIA Portal"：`TiaPortalProcess` / `TiaPortalSession` / `TiaPortalProduct`、`TiaPortal.GetCurrentProcess`、`ProjectBase.TextCategories / HwUtilities` |
| `ReadTransferRoutes` | 下载提供者的 `ConnectionConfiguration` 路由树（`ConfigurationMode` → `ConfigurationPcInterface` Name / Number / Addresses / `ConfigurationSubnet`（Addresses / `ConfigurationGateway`）/ `ConfigurationTargetInterface`）+ `RHDownloadProvider` / `RHOnlineProvider`（Primary / BackupState）/ `OnlineProvider` 可用性；不套用任何路由 | `Siemens.Engineering.Connection.*`、`Download.RHDownloadProvider`、`Online.RHOnlineProvider` |
| `ManageHardwareUtilities` | `list`、`findModuleTypes` / `findContainerTypes` / `normalizeTypeIdentifier`（`ModuleInformationProvider`）、`exportOpcUa`（`OpcUaExportProvider.Export(PLC 项, .xml)`）、`exportCardReaderPsc`（`CardReaderPscProvider.Export(设备, .psc[, 密码])`，卡片镜像，拒绝覆盖） | "Accessing normalized type identifiers" / "Export of data in OPC UA XML format" / "Creating and exporting psc file"：`ProjectBase.HwUtilities.Find(...)` |
| `ManageDeviceServiceObjects` | `family=webApplications`（`DefaultWebPagesFeature.WebApplicationConfigurations`：read / setDefault；V21）、`telecontrolDataPoints`（`TelecontrolManagement.TelecontrolDataPoints`：read / update / delete，DNP3 / IEC / WDC 类型化字段；`ExportDataPoints` / `ImportDataPoints` 两版都有）、`certificateServices`（`CertificateManagementConfiguration`：Usage / CertificateExpirationEventActivated / RemainingCertificateLifetime 与 `CertificateSupportedServices` 的 read / update / setServiceGroupName / createService(id, group, type) / deleteService） | "Managing dynamic certificate settings"、`HW.Features.DefaultWebPagesFeature` / `TelecontrolManagement` |
| `ReadObjectIdentifier` | `ObjectIdentifierProvider.GetIdentifier` 精确对象（设备 / 设备项 / PLC 块 / 类型 / 变量表）的跨会话稳定标识，或 `Find(identifier)` 反查并描述（类 / 名 / 归属路径 / `ISystemObject.IsSystemObject`） | "Identifying cross session object for Hardware & Software" |
| `ShowObjectInEditor` | `IShowable.ShowInEditor`：在 TIA 编辑器里打开设备（V21）/ PLC 块 / 类型 / 变量表；只动 UI | `Siemens.Engineering.IShowable` |
| `RunToolsInTransaction` | 1..20 个工具调用放进同一个 `ExclusiveAccess` + `Transaction(project, text)`：全部成功才 `CommitOnDispose`，否则销毁时整体回滚；内层 `dryRun` 强制 false，`CallTool` / 嵌套事务拒绝；回报 `CanCommit` / `CommitRequested` / 每个调用的结果；内层工具复用环境独占访问（`AcquireHmiEditAccess` 不再二次申请） | "Transaction handling"：`ExclusiveAccess.Transaction`、`Transaction.CanCommit / CommitRequested / CommitOnDispose`、`ExclusiveAccess.IsCancellationRequested` |

扩展：`OpenProject` 新增 `umacUserName` / `umacPassword` / `umacUserType`（`Projects.OpenWithUpgrade(file, UmacDelegate)`，`UmacCredentials.Name / Type / SetPassword`，打开受保护工程不再依赖界面登录）；`GoOnline` 新增 `userName` / `userType`（`OnlineAuthenticationConfiguration` → `OnlineCredentials.Name / Type / SetPassword`，`GetSupportedAuthenticationTypes` 与 `IsSecureCommunication` 回报到 meta；UMAC 保护的 PLC）与 `rhTarget=primary|backup`（`RHOnlineProvider.GoOnlineToPrimary / GoOnlineToBackup`，V21 可带路由树里的 `ConfigurationAddress`）；`DownloadToPlc` 新增 `rhTarget`（`RHDownloadProvider.DownloadToPrimary / DownloadToBackup`，类型化重载）；下载提示应答走官方基类（`DownloadConfiguration.Message`、`DownloadCheckConfiguration.Checked`、`DownloadPasswordConfiguration.SetPassword` + `IsSecureCommunication`、`SelectiveDeleteDownload.CurrentSelection`、`UploadConfiguration` / `UploadPasswordConfiguration`），43 个具体提示类仍经反射兜底；`DownloadResultMessage` / `UploadResultMessage` 类型化收集（State / Message / ErrorCount / WarningCount / 嵌套）；硬件动态属性两个以上时走 `SetAttributes(pairs, AttributeDelegate)`：`AttributeNameUnsupported` / `AttributeReadOnly` / `AttributeTypeUnsupported` / `AttributeValueUnsupported` 逐个记入 `refusedAttributes`（Name / Message）并 `Ignore` 继续，其余属性照常写入读回；`GetCrossReferences` 在官方 `CrossReferenceResult` 上走类型化遍历（`SourceObject` / `ReferenceObject` 的 TypeName / Address / Device / UnderlyingObject 类名、`Children` 递归），`CrossReferenceEntry` 多 8 个字段；`CompareProjects` 软件比较树类型化（`CompareResult.RootElement` → `CompareResultElement`，附 `rootElement` 摘要）；`SearchHardwareCatalog` 的 `CatalogEntry` 行类型化；`ManageMultiuserSession` 的 `LockStateProvider` / `Markings` / `ProjectServerGroup`、版本控制的 `WorkspaceSystemGroup` / `WorkspaceUserGroup`、`ReadProjectSettings` 的 `TiaPortalSettingsFolder` / `TiaPortalSetting` 改为类型化访问；`CompilerResultMessage` 顶层行（Path / State / Description / 计数 / 时间）记入引擎日志；步骤工具的错误 meta 附 `EngineeringException.MessageData` / `DetailMessageData`（Text / DetailText）。

未暴露 / 已核实：`Siemens.Engineering.Compiler.CompileProvider` 与 `Siemens.Engineering.Private.ProcessHelper` 在 PublicAPI 里是 internal（有文档、不可引用），审计脚本从分母里排除；`HW.CustomDataTypes.TableData` / `StructuredData` 只作为 GSD 自定义属性的 `GetAttribute` 值出现（登记为动态覆盖，仅形状检查）；`ProjectServerGroup.AddProjectToServer` / `GetServerProjects`（服务器端工程管理）与 `TelecontrolDataPointComposition.Create<T>` 未做工具。V20 差异：`TextCategory`、`WebApplicationConfiguration` / `DefaultWebPagesFeature`、`TelecontrolDataPoint*`、`OnlinePasswordConfiguration.IsSecureCommunication`、`RHOnlineProvider.GoOnlineTo*(ConfigurationAddress)`、`CertificateSupportedService.ApplicationUri / Guid`（V20 Id 是 `UInt16` 只读、枚举少 `None` / `OpcUaClient`）、`Device : IShowable` 都是 V21 才有，V20 上对应动作报 NotSupported。

## 验证

| 检查 | V20 | V21 |
|---|---|---|
| 离线逻辑测试 | 1667 通过（新增 47 项：硬件工具请求、设备服务对象请求与范围、对象选择、事务调用清单与 dryRun 强制、UMAC / 在线凭据、R/H 目标、工程级 syslog 拒绝文案） | 同 |
| 引擎 API 形状检查（对本机 PublicAPI 逐成员核对） | 1585（原 1403，新增 182 项） | 1708（原 1500，新增 208 项） |
| 真实工程 | — | **2026-09-19 重跑**（见下表）：五处修复、`ReadPortalInfo`、`ReadTransferRoutes`、`ManageHardwareUtilities`（含 OPC UA XML 真实导出）、`ManageDeviceServiceObjects` 三族读取、`ReadObjectIdentifier` 设备 / 块往返、`ShowObjectInEditor`、`RunToolsInTransaction` 提交与回滚、类型化交叉引用与目录行全部通过；**缺陷**：事务内层调用未带 `dryRun` 时按预览执行却回报 `committed=true`（显式 `dryRun:false` 可绕过，2.7.34 修） |

### 真机结果（V21 `AutomaticDipCoatingMachine`，2026-09-19）

| 工具 | 结果 |
|---|---|
| 修复 ① 工程级 syslog `create` | `dryRun=false` 在调用前被 `NotSupportedException`（"preview-only…crashed TIA Portal V21 twice"）拒绝，TIA 进程存活（`ListPortalProcessProjects` 仍为 1）；`dryRun=true` 预览正常 |
| 修复 ② 证书 `template` | 不带 `certificateId` 直接读到 Tls 模板（CN `S1-K1/Tls-3`、`Sha256RSA`、SAN `IP 192.168.0.1`） |
| 修复 ③ `UmacDevice` 文案 | 对 CPU 项 `+S1-K1` 的报错含 "On the reference project it lives on the Device (devicePathJson only, itemPathJson=[])" |
| 修复 ④ `CheckLibraryUpdates` | `ReportOutOfDateOnly` 104 条（HMI + PLC），每条带 `parts {DeviceName / LibraryTypeName / LibraryVersionNumber / UpToDate / PathToInstance / InstanceName / CurrentVersionNumber}`，`messageParts` 不再出现；payload 40 KB（2.7.32 同类树 726 KB） |
| 修复 ⑤ 改绑守卫 | 只开一个 TIA 实例时 `GetState.project` / `ReadPortalInfo.expectedProject` 稳定为 `AutomaticDipCoatingMachine`；双实例改绑场景本次未复现（未再开第二个工程），"bound project changed" 拒绝路径未真机触发 |
| `ReadPortalInfo` | 进程 8804（`WithUserInterface`，`Siemens.Automation.Portal.exe`，工程路径 `…\AutomaticDipCoatingMachine.ap21`）；`attachedSessions` 1 条 = 本引擎（`accessLevel` "Modify, Published"、`trustAuthority` None、`processPath` 指向虚拟机上的 `TiaMcpServer.exe`、`utilizationTime`）；`installedSoftware` 5 项（TIA Portal / Startdrive Advanced / STEP 7 Professional / WinCC Basic-Comfort-Advanced / WinCC Unified，均 V21，带选件）；`textCategories` 18 条；`hardwareUtilities` 3 个（`OPCUAExportProvider` / `ModuleInformationProvider` / `CardReaderPscProvider`）；`objectIdentifierProviderAvailable=true` |
| `ReadTransferRoutes` | `+S1-K1`：模式 `PN/IE`，2 个 PG/PC 接口（Intel 82574L、PLCSIM Virtual Ethernet Adapter，接口自身无地址），子网 `PN/IE_1` 地址 `192.168.0.1`、无网关，目标接口 `1 X1`；`rhDownloadProviderAvailable` / `rhOnlineProviderAvailable` = false（非 R/H），`onlineState=Offline` |
| `ManageHardwareUtilities` | `list` 3 个；`normalizeTypeIdentifier` `OrderNumber:6ES7 510-1SK03-0AB0/V4.1` → `OrderNumber:6ES7510-1SK03-0AB0/V4.1`；`findModuleTypes` → 自身；`findContainerTypes` → `System:Device.ET200SP`、`System:Rack.ET200SP`；`exportOpcUa`（`+S1-K1` → `C:\Users\SIEMENS\Desktop\mcp2733-opcua.xml`）真实写出 20,072,458 字节；`exportCardReaderPsc` 预览 |
| `ManageDeviceServiceObjects` | `webApplications`：2 条（"系统 Web 页面" `System_Inbuilt` 非默认、"硬件配置中的 Web 服务器入口页面" `None` 默认）；`certificateServices`：`usage=TIAPortal`、过期事件 false、剩余寿命 0、0 个服务；`telecontrolDataPoints`：CPU 无 `TelecontrolManagement`（预期，报 "does not expose TelecontrolManagement"） |
| `ReadObjectIdentifier` | 设备 `ET 200SP station_1` → `mf8qUCbYAkituuc//yPo5g==` → `Find` 回到 `DeviceImpl` 同名；块 `00_Safety/OpMode01/DBs/OPMODE01_FDB` → `0ph2WumUDEGM6EtZ3B7AxQ==` → `Find` 回到 `GlobalDB`（标识是 Base64，跨调用稳定） |
| `ShowObjectInEditor` | 块预览通过；设备 `dryRun=false` 实做返回成功（`IShowable.ShowInEditor` 被调用，TIA 界面侧未核对） |
| `RunToolsInTransaction` | 预览通过。实做 A（`ManagePasswordPolicy update` 同值 + `ManageUmcUsers createOffline mcptmptx2733`，内层显式 `dryRun:false`）：`canCommit=true`、`commitRequested=true`、`committed=true`，事务后用户存在。实做 B（同名 `createOffline` 两次）：第二次失败 → `committed=false`、`stoppedAt`，**第一次创建被回滚**（用户列表里只有 A 的用户）。清理：删除 `mcptmptx2733`，`verifiedAbsent`。**缺陷**：内层调用不带 `dryRun` 时，`ForceRealExecution` 只翻转已有键，工具默认 `dryRun=true` 生效——两条调用都按预览跑，事务仍回报 `committed=true`（首次实做就是这样，用户未创建）。2.7.34：无论是否存在都写入 `dryRun=false` |
| `GetCrossReferences`（类型化） | FB `OPMODE01_IN_FFB` 15 条：`SourceTypeName` "LAD-Function block"、`SourceAddress` %FB12020、`SourceObjectClass` FB、`ReferenceObjectClass` FC / InstanceDB / GlobalDB、`ReferenceTypeName` 如 "Instance DB of OPMODE01_IN_FFB [FB12020]"；DB `OPMODE01_FDB` 0 条（其一位置文本提示需在 TIA 设置里重建交叉引用信息） |
| `SearchHardwareCatalog`（类型化 `CatalogEntry`） | `6ES7 510-1SK03` → 3 条（V3.0 / V3.1 / V4.0，`CatalogPath` `Root\控制器\SIMATIC ET200 CPU\…`、`TypeIdentifierNormalized`） |

未验证：`DownloadToPlc` / `GoOnline` 的 `rhTarget` 与 `GoOnline` 的 `userName`（无 R/H、无在线 CPU）、`OpenProject` 的 UMAC 凭据（无第二个受保护工程文件）、`CompareProjects rootElement`（只有一个 PLC 软件）、`exportCardReaderPsc` 实做、`ManageDeviceServiceObjects` 写动作、双实例改绑拒绝。虚拟机桌面留下 `mcp2733-opcua.xml`（20 MB）可删。

审计（V21，本版源码；分母去掉 internal 类型后 1,215）：类型 = 有专用引用 **396**（2.7.32 为 314）/ 仅类型名 64 / 动态覆盖 327 / 完全未触及 **428**（原 490）；未封装功能类型 **178 / 735**（原 230 / 896），其中 **Base 0 / 0**（原 52 / 161）。阶段 3 收口；下一阶段 4：Step7（`SW.Units` 软件单元、`PlcDocument*`、`PlcChecksumProvider` / `PlcSimulationSettingsProvider`、`SW.ExternalSources`、`SW.Blocks` 零头、TO 映射，44 类型 / 140 成员）。
