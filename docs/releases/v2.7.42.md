# v2.7.42

引擎 **2.7.42.0**（V20/V21 均重建）。工具 437 → **447**，默认 lite 清单 56 项不变。本版是"官方 Openness API 全量对齐"计划 [路线图 §2.0](../development/roadmap.md) 阶段 6（选件包）的最后一批 ⑥-③：**SafetyValidation + Test Suite + Teamcenter + CFC**（V21 `Siemens.Engineering.SafetyValidation.dll` / `TestSuite.dll` / `TeamcenterGateway.dll` / `CFC.dll`；V20 的后三者在整体 `Siemens.Engineering.dll` 里，SafetyValidation 是 V21 新增）。2.7.39 审计剩下的 25 个功能类型 / 122 个成员全部改为专用引用，**阶段 6 收口，全部程序集的功能类型缺口为 0**。SafetyValidation 与 Teamcenter 本机、虚拟机都没有许可 / 环境，只有形状检查；Test Suite / CFC 虚拟机装有选件但工程里没有内容，真机只能验空表与 NotFound / NotSupported 路径。

## 新增工具（域 Safety，Safety Validation Assistant，V21）

| 工具 | 动作 | 官方入口（Openness 手册 "Functions for Safety Validation Assistant"） |
|---|---|---|
| `ReadSafetyActivationTests` | 根或 `groupPathJson` 所指用户组的激活测试（名 / 作者 / 评估设备名 / `OverallState` / 安全功能数，可带安全功能与条件）、该层的子组、根层 `DeviceQuery.EvaluationDevices()`；给 `name` 时读单个测试含 `AvailableDevices()` 与 `TestValidity.CheckValidity()` | "Accessing Safety Validation Assistant"、"Querying the Activation Tests"、"Querying the Evaluation Devices"、"Querying Available Devices"、"Overall state"、"Validity of the Activation Tests" |
| `ManageSafetyActivationTest` | read / create（`Create(name, DeviceItem)`）/ createFromTest（`CreateFrom(ActivationTest)`）/ createFromMasterCopy（`CreateFrom(MasterCopy)`）/ rename / setAuthor / changeEvaluationDevice / checkValidity / generateReport（`ActivationTestPrintout.Generate` → .xlsx）/ export（`Export(FileInfo, ExportOptions[, DocumentInfoOptions])`）/ import（`Import(file, ActivationTestImportOptions)`）/ delete（`confirmDelete`） | "Creating new Activation Tests"、"Duplicating"、"Copying from a library"、"Modifying"、"Generating reports"、"Exporting / Importing Activation Tests" |
| `ManageSafetyActivationTestGroup` | read / create / createFromMasterCopy / rename / delete（非空组拒绝） | "Creating new Activation Tests"（用户组）、"Copying Activation Tests from a library" |
| `ManageSafetyFunction` | read / create（`Create()` + `TestName` / `Description`）/ createFrom / update / resetTestResult / checkValidity / setTrace（`TraceConfiguration.IsTraced` + 动态属性 `PretriggerTime` / `RecordingDuration` / `Signals`）/ checkTraceValidity / export / import（`Import(file, ImportOptions)`）/ delete | "Querying / Creating / Duplicating / Modifying Safety Functions"、"Resetting Test Results"、"State / Validity of the Safety Functions"、"Getting / Modifying / Validity of the Trace Configuration"、"Exporting / Importing Safety Functions" |
| `ManageSafetyFunctionCondition` | 按 `Conditions` 位置 `index`：read / create（`Create(DeviceItem, SignalUsage, signalName)` + `propertiesJson`）/ update（comment / deviceName / signalName / signalUsage / initialInput / executedInput / response：true/false 或 `FALSE` / `TRUE` / `NotRelevant`）/ checkValidity / delete | "Querying the Conditions"、"Creating new Conditions"、"Validity of the Conditions" |

`SafetyFunction.Name` 是生成的只读名，`TestName` 才是标题：工具先按 Name、再按唯一的 TestName 选。V20 五个工具都回 NotSupportedOnVersion（形状检查钉住该命名空间在 V20 缺席）。

## 类型化改造 + 新增（域 Project，Test Suite）

| 工具 | 变化 | 官方入口（Test Suite 手册 "Openness Support"） |
|---|---|---|
| `ReadTestSuiteCases` | 签名兼容，新增 `kind` testSet（`ApplicationTestSets`）；行带类与作用域（`TestCase.GetScope()`、`SystemTestCase` 的 OPC UA 地址 / 接口类型 / 目录） | "Accessing Style guide / Application Test / System Test system folder"、"Accessing Test Case Scope"、"Accessing System Test Case Scope"、"Accessing Test set" |
| `ExchangeTestSuiteCase` | 签名兼容：export 走 `SaveToFile(FileInfo)`（原生 bool 回报），import 走三类组合的 `LoadFromFile(file, ImportOptions, RSLoadOptions｜TCLoadOptions)`（样式指南可 `\|` 组合）；新增 `importTestSets`（`ApplicationTestSystemGroup.LoadFromFile(file, ImportOptions, TSLoadOptions)`，回测试集与测试用例）与 `kind` testSet 的 delete | "Import and Export of Rule Sets"、"Import/Export of test cases"、"Import Test sets"、"Delete Rule Set / Test Case / Test set / System Test Case" |
| `RunTestSuiteCase` | 签名兼容，新增 `namesJson` / `runAll` / `kind`：`RuleSetExecutor` / `TestCaseExecutor` / `SystemTestCaseExecutor` 的单个 / 列表 / 整组 `Run` 重载（测试集用 `Run(ApplicationTestSet)` / `Run(IEnumerable<ApplicationTestSet>)`）；`TestResults` 与递归 `TestResultsMessage`（路径 / 状态 / 描述 / UTC 时间 / 计数）类型化读取，深 12 / 2000 条封顶 | "Execution of Rule Sets / Test Cases / System Test Cases"、"Execute Test sets"、"Exceptions" |
| `ManageTestSuiteCase`（新） | read / rename / setScope（样式指南 `RuleSet.SetScope(UpdateOptions, IEnumerable<IEngineeringObject>)`，对象由 `scopeJson` 的 project / deviceGroup / plc / blocks / tags / types / units 条目解析；应用测试 `TestCase.SetScope(PlcSoftware[, instanceName, ExecutionMode])`；系统测试 `SystemTestCase.SetScope(address, ServerInterfaces[, DirectoryInfo])`）/ copyScope（`RuleSet.CopyScope`）/ createFromMasterCopy（三类 `CreateFrom(MasterCopy)`）/ showInEditor | "Modify the Scope of a Rule Set / Test Case / System Test Case"、"Copy Master copies to projects"、"Show Rule Set / Test Case / Test set" |

## 新增工具（域 VersionControl，Teamcenter Gateway）

| 工具 | 动作 | 官方入口（Openness 手册 "Functions for accessing Teamcenter Gateway"） |
|---|---|---|
| `ManageTeamcenterConnection` | read / connect（`Connect(user, SecureString, group, role, hostURL, instance)`）/ connectSso（`ConnectSSO(hostURL, instance, loginURL, applicationID)`）/ disconnect；引擎按会话保存一份 `TcGatewayConnectionInfo`，只回 group / role / 令牌 SHA-256 前缀 | "Connecting to the Teamcenter Gateway"、"Connecting to the ConnectSSO"、"Disconnecting" |
| `ManageTeamcenterDataset` | checkout / checkin / cancelCheckout（`TcGatewayLockProvider`，`DatasetType`）、search（`TcGatewaySearchAndDownloadProvider.Search` → `SearchResult`）、download（`Download` → 启动文件路径，不自动打开） | "Checkout / Checkin / Cancel checkout dataset"、"Searching"、"Downloading Project/Global library" |
| `ManageTeamcenterWorkflow` | 目标 project / globalLibrary：readCustomAttributes（`GetTeamcenterCustomAttributes` → `TeamcenterProperty` 行）、save / saveWithProxyObject / saveToItem / saveToItemWithProxyObject / saveAsNewItem(WithProxyObject)（`ItemDetailsDelegate`）/ saveAsNewRevision(WithProxyObject)（`RevisionDetailsDelegate`），`customAttributesJson` 经 `SetValue(value, ErrorCallback)`；一律 `confirmSave=true`，回 `ItemInfo` | "Saving Project/Global library in Teamcenter"、"… with proxy object"、"… with new item"、"Accessing custom attributes"、"Saving … as new item / new revision"、"save to item with proxy object" |

## 类型化改造 + 新增（域 PLC-Software，CFC）

| 工具 | 变化 | 官方入口（"CFC Charts (Export/Import)"） |
|---|---|---|
| `ExchangeCfcCharts` | 签名兼容（`modelVersion` / `filter` 有了默认值）：export = `CompleteExport`，新增 selectiveExport（`SelectiveExport(path, chartNamesJson, …)`）、exportInstructionData（`ChartProviderS7.ExportInstructionData`）；import 仍要求确认的 Offline 状态 | "Exporting CFC charts"、"Exporting only selected CFC charts"、"Importing CFC charts" |
| `ManageCfcChartProtection`（新） | read（`GetChartProtection` → 哈希）/ add（`AddChartProtection(chart, newHashedPassword)`）/ change（`ChangeChartProtection(chart, SecureString, newHashedPassword)`）/ remove（`RemoveChartProtection(chart, SecureString)`） | "Setting / Reading / Changing / Removing a password for CFC charts" |

## 验证

| 检查 | V20 | V21 |
|---|---|---|
| 离线逻辑测试 | 2136 通过（新增 80 项：激活测试组路径 / 激活测试 / 组 / 安全功能 / 条件请求与条件值；Test Suite 类别 / 种类 / 加载选项 / 交换 / 执行 / 管理请求与作用域条目；Teamcenter 连接 / 数据集 / 工作流请求与条目 / 修订详情 / 自定义属性；CFC 交换与密码请求） | 同左 |
| 引擎 API 形状检查（对本机 PublicAPI 逐成员核对） | 2781（原 2591，新增 190 项；SafetyValidation 只钉命名空间缺席） | 3068（原 2811，新增 257 项） |
| 真实工程 | — | **2.7.42 真机重跑（2026-09-20，虚拟机 2.7.42.0，V21 `AutomaticDipCoatingMachine`；临时对象用完即删，工程未保存）**：Test Suite——`ReadTestSuiteCases` 三类 + `kind:"testSet"` 都到达且为空（`RuleSetComposition` / `TestCaseComposition` / `SystemTestCaseComposition` / `ApplicationTestSetComposition`）；`RunTestSuiteCase styleGuide runAll dryRun=false` 真的跑了 `RuleSetExecutor.Run(StyleGuideSystemGroup)`，回 Success + 2 条本地化消息（"规则集的执行已完成（错误：0；警告：0）"）；system 空组 `Run` 回原生 "No test case(s) in the selected project"；**application 空组 `Run` 在 TIA 内部抛 NullReferenceException**（可恢复，TIA 未退出）；NotFound / `confirmExternalExecution` 门按预期。CFC——`+S1-K1` 拿得到 `ChartProviderS7`，`CompleteExport` 写出 484 字节的空 ZIP；**`GetChartProtection("MCP_NONE")` 与 `ExportInstructionData` 各让 TIA Portal V21 退出一次**（`NonRecoverableException`，进程消失）。Safety Validation Assistant——**虚拟机有该选件**：`DeviceQuery.EvaluationDevices()` 列出 `+S1-K1`，建组 / 建激活测试（自带 "SF 1"）、`AvailableDevices()`（4 台 G120C + F-CPU）、测试级 / 功能级 / 跟踪级 `CheckValidity`（"SF 1 has errors" / "Mandatory Signal Usage type(s) missing." / "Error: Sensor - Controlmode"…）、安全功能 update / create / createFrom（生成名 "SF 2" / "SF 3"，TestName 歧义按设计拒绝）/ `setTrace` / `resetTestResult` / export（3131 字节）/ import（→ "SF 2"）/ delete、三种条件 `Create` + 按位置 update / delete（`InitialInput` 等实为 `ConditionValue` 枚举，bool → TRUE/FALSE 正确）、激活测试 rename / setAuthor / createFromTest（生成名 "Safety Activation Test_1"，作者不复制）/ export（1393 字节）/ import Rename（→ "MCP_TMP_AT_R_1"）与 None（原生 "already contains an object with the same Name"）/ `generateReport`（9821 字节 .xlsx）/ delete、非空组删除拒绝、组删除全部通过；`changeEvaluationDevice` 给驱动被 TIA 拒绝（"'+S1-7Q1' not a valid evaluation device"）；条件 update 写 `ExecutedInput=NotRelevant`（InputCondition）被 TIA 拒绝但 `Comment` / `SignalName` 已写入，**紧接着的条件级 `CheckValidity` 让 TIA Portal V21 退出**。Teamcenter——`TeamcenterConnectionProvider` 由该 TIA 提供（`providerAvailable=true`），无服务器；数据集 / 工作流工具无连接时按设计回 InvalidState。三次 TIA 退出引擎都活着（2.7.41 legacy 策略首次真机验证：`GetState` 回 `processAlive=false`，重启 TIA 后 `AttachToOpenProject` 即恢复）。 三处退出的守卫见 [v2.7.43](v2.7.43.md)。 |

审计（V21，本版源码；分母 1,215）：类型 = 有专用引用 **668**（2.7.39 为 629）/ 仅类型名 106 / 动态覆盖 325 / 完全未触及 **116**（原 158）；领域成员已引用 **2,219**（原 2,066）；未封装功能类型 **0 / 0**（原 25 / 122）——阶段 6 收口。

## 兼容性

- 三个 Test Suite 工具与 `ExchangeCfcCharts` 的既有参数原样保留（`ExchangeCfcCharts` 的 `modelVersion` / `filter` 从必填改为有默认值，`exportInstructionData` 不需要它们）；`ExchangeTestSuiteCase` 的默认 `importOptions=None` 在 export / delete 上被容忍。
- 新工具都不在默认 lite 清单里，经 `FindTools` / `CallTool` 或完整档使用。
- `Check-DeadToolReferences.py` 的 ALLOWED 表补了本批描述里点名的 13 个原生成员 / 枚举名。
