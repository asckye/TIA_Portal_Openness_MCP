# v2.9.2

小修（PATCH）。2.9.1 真机（`项目1`，Override 重生成后未编译）：`DeletePlcBlock {..., crossReferences:true}` 被护栏挡住时只说“⚠️ 取不到这个块的交叉引用”，看不出是“PLC 有未编译块、先 CompileSoftware”。

## 改动

- `Portal.DeletePlcBlock` / `DeletePlcType`：改用带 `out reason` 的 `GetCrossReferences` 重载；`crossReferences=true` 时结果多一个 `crossReferenceUnavailableReason`（与 `DeletePlcTagTable` 一致），警告写成“取不到这个块的交叉引用（refused: N block(s) ... not compiled ...）”。
- 其余同 2.9.1（工具 453、lite 59、离线 2461）。

## 待真机

- `项目1`：Override 重生成后不编译，`DeletePlcBlock {softwarePath:"MCP_STD", blockPath:"MCP_SimLogic", dryRun:true, crossReferences:true}` 的 `crossReferenceUnavailableReason` 应以 `refused:` 开头并点名块；编译后再调应有交叉引用。
