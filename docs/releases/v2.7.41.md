# v2.7.41

引擎 **2.7.41.0**（V20/V21 均重建）。工具 **437** 不变，默认 lite 56 项不变。2.7.40 真机重跑（见 [v2.7.40](v2.7.40.md#验证)）发现：TIA Portal V21 在 G120C 上退出后，**MCP 引擎进程自己也跟着崩溃**（退出码 -532462766 / 0xE0434352，控制台只剩 "由于 Exception.ToString() 失败，因此无法打印异常字符串"），虚拟机上的服务虽把它拉起来，但连接、绑定与临时对象状态全部丢失。本版让引擎在 TIA 退出后活下来，并再挡一处 Startdrive 崩溃触发点。

## 变更

| 项 | 内容 |
|---|---|
| `App.config`（新） | `<legacyUnhandledExceptionPolicy enabled="1"/>`：Openness 在 TIA 退出后于后台线程抛出的异常（其 `ToString()` 自身会失败）不再终止 .NET Framework 进程；SDK 生成的程序集绑定重定向照常合并进同一个 `<runtime>`，`runtime/v20`、`runtime/v21` 的 `TiaMcpServer.exe.config` 随之带上这一行 |
| `Program.cs` | `AppDomain.UnhandledException` / `TaskScheduler.UnobservedTaskException` 记进诊断日志（`DescribeSafely`：先记类型名，`Message` 取不到就写 "Message unavailable"，绝不因为记录本身再抛） |
| `GetState` | 绑定的 TIA 进程不在时直接回 `isConnected=false`，`meta.hmiReadHealth.portalProcess.processAlive=false` 说明原因，不再抛 "disposed" |
| `ManageDriveTelegrams` | 驱动对象没有任何报文（未连到 IO 系统）时 check / insert / erase 一律以 InvalidState 拒绝：新建 G120C 上 `CanInsertAdditionalTelegram(2, 4)` 抛 "Invalid operation"，接着的 `CanInsertTelegram(700, SupplementaryTelegram)` 让 TIA 退出 |
| PLCSIM Advanced 桥（`PlcSimAdvancedChannel`） | 维护者报告 2.7.38 在 PLCSIM 连续读写 15–30 次后引擎崩溃 5 次（与并发无关）。此前每次调用都 `CreateInterface` → `UpdateTagList` → 读写 → `Dispose`；现在每个实例名只保留一个 `IInstance` 接口（`Acquire`，探测失效才重建）、变量表只装载一次（`EnsureTagList`；列表时与报 "not found" 时刷新，`ReadWithRefresh` / `WriteWithRefresh` 重试一次）、所有 API 调用在同一把锁上串行；`unregister` / `powerOff` / `memoryReset` 与任何异常后 `Forget` 释放接口；结果里 `interface` 字段回 `cachedInterface` / `openedAt` / `uses` / `tagListLoaded`。本机无 PLCSIM Advanced，未做真实验证 |

已知的 TIA Portal V21（G120C）退出触发点至此五处：读 `r2139` 的值；已有主报文时 `CanInsertMainTelegram`；新建驱动上读未接线 `p840[0]` 的值；未联网驱动上 `CanInsertTelegram`；（`CanInsertAdditionalTelegram` 只抛异常不退出）。交接页 §5 有完整清单。

## 验证

| 检查 | V20 | V21 |
|---|---|---|
| 离线逻辑测试 | 2056 通过 | 同左 |
| 引擎 API 形状检查 | 2591 | 2811 |
| 真实工程 | — | **已验证（2026-09-20，虚拟机 2.7.41.0，临时设备 `MCP_TMP_G120C`（`OrderNumber:6SL3210-1KE11-8AB1/4.7.13`）与 `MCP_TMP_S120`（CU320-2 PN，`OrderNumber:6SL3040-1MA01-0A**/V5.1/S120`），测完 `deleteDevice`，工程原有设备未碰，全程 TIA 未退出）**：守卫——无报文的 G120C 上 `ManageDriveTelegrams check` 以 InvalidState 拒绝，S120（主报文 999）上 `check MainTelegram 1` 回 `mainTelegramPresent=true` 并跳过 `Can*`；G120C——`ManageDriveFunctions read`（`DriveObjectFunctions` / `FunctionInUse` 为 null，Commissioning / SafetyCommissioning / HardwareProjection 提供）、`readMotorConfiguration` 在未设电机类型时被 TIA 拒绝 "No motor type is defined. Please set an appropriate motor type first"，`setMotorType {InductionMotor, dataSet 0}` 实做后回 required entries（`p304` 400 Vrms、`p305`、`p307`、`p310` 50 Hz、`p311` …），`projectMotorConfiguration {entries {p305:2.2, 307:0.75, p311:1395}}` 按名 / 按号写入并 `ProjectMotorConfiguration` 回 true（float 读回 2.20000005），`setEncoder` / `readEncoderConfiguration` 被 TIA 拒绝 "The selected device does not support the encoder projection"；`ManageDriveSecurity` / `ManageTechnologyExtensions read` / `ManageDriveSafetyAcceptanceTest` / `ReadDccCharts` / `ManageDcbLibraries` / `ReadOnlineDriveParameters` / `ManageOnlineDriveFunctions` 在 G120C 上都按设计回 NotSupported（`Security` null、无工艺扩展容器、无验收测试提供者、无 DCC 容器、离线无在线容器），`ManageDriveHardwareModule` 在驱动项与 `PM G120C` 上都不提供 `DriveItemHardwareModule`；门户——`ManageTechnologyExtensions readPackages` 列出已装 .tec 包（REACTPWR、VIBX、TRCDATA、POLYGON、DCDCCONV、OALINK、SETPGEN、HEM、SERVC…）；S120——`DriveObjectContainer` 在机架项 `驱动控制`（`DeviceItems[0]`）上而不在 CU 项上，`driveObjectNumber` 1 可读，主报文 999（1/1 字，未联网无地址），`DriveObjectFunctions` 提供（当前类型 "-" / 0，可选 "SINAMICS S" / 1；激活 `Activate` / `isActive=true`），`Security` 提供但 `UmacConfiguration` / `DriveDataEncryption` 都为 null，`TechnologyExtensionContainer` 提供（TRCDATA / OALINK），`activate TRCDATA/1101400` 实做后 `isActivated=true`、参数 0 → 250，按名 `deactivate TRCDATA` 回 false 读回；`DriveItemHardwareModule` 在机架项上提供（`System:Rack.S120`、`AcxSinamics.Rack.CU`，位置 0），`changeType` 预览通过；`ReadDriveParameters r2 / p3 / r18` 含枚举表（`r2` = 70 初始化）；`changeDriveObjectType` / `changeActivationState` 预览通过；DCC 容器在 CU 驱动对象上为 null，验收测试提供者不在机架项上。未验：DCC 全族（需带 DCC 的驱动轴对象）、在线工具（驱动不在线）、S120 上的电机 / 编码器投影（需插功率 / 电机模块）、TIA 退出后引擎存活（本轮没有崩溃，未触发）。 PLCSIM Advanced 桥仍请维护者在自己的 PLCSIM 环境连续读写 50 次以上核对（结果里 `interface.uses` 应递增而不重建） |
