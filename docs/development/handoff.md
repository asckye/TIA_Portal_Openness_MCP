# 接续工作交接（2026-09-21 更新，2.7.56 已发布）

[文档目录](../README.md) · **[换机器交接单](handoff-checklist.md)** · [交接历史](handoff-history.md) · [路线图 §2.0](roadmap.md#20-官方-api-全量对齐计划) · [发布流程](release-workflow.md) · [覆盖清单](../reference/openness-coverage.md)

换机器继续"官方 Openness API 全量对齐"计划时先读这一页。它记录**当前停在哪**、**下一步做什么**、**每个阶段的固定动作**、**发布闸门**和**只在真机上学到的 API 事实**——这些都不在代码里，也不在提交历史里。逐版本条目与阶段收口清单在[交接历史](handoff-history.md)。

## 1. 现状（2026-09-21）

| 项 | 值 |
|---|---|
| 最新发布 | **v2.7.58**（`Release.ps1` 第二次真跑，全程无人工干预：三条工作流全绿，ZIP `TIA_MCP_Delivery_v2.7.58_20260921.zip` + `.sha256` 已上传；见 `docs/releases/v2.7.58.md`）：全部工具的调用纪律——schema `enum` / `default` / `examples` / 词汇表 `description`、失败自动 `meta.preflight`、示例覆盖 453 个、`GetRecipe` 12 条、参数描述批次 1 + 回升门禁；工具 453、lite 59、离线 2453 |
| 虚拟机 | 跑的是 **2.7.57**（2.7.58 待部署：停引擎后 `Update-Engine.ps1` 在线更新即可，虚拟机有外网）。2.7.57 真机通过（示例在描述里、`PreflightToolCall` 报告、`CheckForUpdate` 经 API 取到 v2.7.57）。**TIA 现在只开着维护者的工程（pid 11104），`项目1` 未打开**——写类真机测试前要请维护者打开 `项目1`；`项目1` 已保存（2026-09-21 05:48 UTC，含 `MCP_STD` + 程序、两台 CPU 的保护设置、监控表两行） |
| 规模 | 工具 453、默认 lite 59；离线套件 2453；引擎 API 形状检查 V20 2805 / V21 3097（`Build-Release.ps1` 第 73 行硬编码这两个数） |
| 真机 | 台账 `docs/reference/real-machine-ledger.md`：✅ 330 / 手工 5 / 早期 24 / ⛔ 31 / ⚠ 60 / 🔁 0。**在线族收口**（标准 CPU `MCP_STD` + PLCSIM Advanced Softbus：下载 / 上线 / 比较 / 标签读写 / default 与 singleStep 场景全部通过）；F-CPU 不能经 Openness 下载（TIA 规则）；站上载对 PLCSIM 实例是 TIA 侧不支持 |
| API 对齐 | 阶段 1–6 全部收口，所有程序集功能类型缺口 0 / 0（2.7.42 审计）；DCC 与在线驱动工具真机待有对象 |
| 待办 | ~~2.7.58 调用纪律~~ 已发布。仓库按 GitHub 社区标准整理（2026-09-21：PR 模板、SUPPORT、CODEOWNERS、issue 模板 config、dependabot（仅 actions）、CHANGELOG 加 Unreleased 与 compare 链接、CONTRIBUTING 加开发段、删三份过时归档）。下一步：部署 2.7.58 跑 `docs/releases/v2.7.58.md` "待真机" 1–4；参数描述批次 2（199 个工具还有 1392 个无自身描述，其中 390 个词汇表也不覆盖——按 `tools-list.json` 的 `undocumentedTools` 顺序补）；之后 2.8.0 引擎拆分。原计划：**2.7.58：全部工具的调用纪律**（维护者 2026-09-21 明确：目标是让 AI 对所有工具一次调对、不再试错耗 token 和时间，不是教它写程序）——见 §2 第 2 条第 2 项；部署 2.7.57 后按 `docs/releases/v2.7.57.md` "待真机" 验证；之后 2.8.0 引擎拆分 |

逐版本记录（2.7.41–2.7.56 每版做了什么、部署后真机结果）、2.7.42 覆盖审计与阶段收口清单都在 [交接历史](handoff-history.md)；各版详情在 `docs/releases/`，变更摘要在 `CHANGELOG.md`。**新发布后**：本表改现状，`handoff-history.md` §1 顶部加一条，不再往本页堆每版条目。

## 2. 下一步（按顺序）

1. ~~部署 2.7.56，验证 `Connect` 附加而不自启~~ **已通过**（2026-09-21，`docs/releases/v2.7.56.md` "真机结果"）：两个 TIA 进程时 `Connect {projectName:"项目1"}` 70 ms 附加到 4840、命中即停、`startedNew false`、进程数不增；名字不存在时绑 4840 并带 `warning`。未跑的只剩"只开维护者工程时按名 Connect 应带 warning 绑过去"（要关 `项目1`）。
2. **维护者 2026-09-21 定下的后续顺序**（热更新 / GitHub Actions 发布 / 全项目整理 / 规范 AI 调用，按建议的顺序做）：
   1. ~~一键发布脚本 + 文档整理~~ 已做（2026-09-21 提交）：`scripts/build/Release.ps1`（§4 第一条）、本页 §1 压缩 + `handoff-history.md`、`docs/README.md` / `scripts/README.md` / `release-workflow.md` / `handoff-checklist.md` 对齐。托管 runner 编不了引擎（Openness NuGet 只有 targets、PublicAPI 不可分发），所以"用 Actions 完成发布"= 本机一键脚本 + 现有 tag 触发的发布工作流；自托管 runner 留到 2.8.0 一起决定。
   2. ~~**2.7.57 更新器 + 调用规范**~~ 已发布（2.7.57，见 `docs/releases/v2.7.57.md`）。**2.7.58 继续：全部工具的调用纪律**（维护者 2026-09-21："需要规范 AI 对所有工具的调用，不能老是试错"）——引擎侧自动、对 452 个工具都生效：① 参数的文档备选值与默认值写进每个工具的 `inputSchema`（`enum` / `default`），客户端发出前就校验；② 任何工具失败时引擎自动把预检报告（缺参 / 参数名 / 该用的值 / 前提 / 示例 / 下一步）附进 `Meta.preflight`，不用再调 `PreflightToolCall`；③ 示例覆盖 452 个：80 条人工 + 其余按签名与描述里的 `e.g.` 自动生成骨架，构建时校验；④ 构建门：每个参数都要有描述（现在 2211 个参数里 1488 个没有，213 个工具），分批补齐；⑤ `GetRecipe(topic)`：真机验证过的多步序列（下载到 PLCSIM、监控表、CPU 保护、SCL 建块、S7DCL 导入、HMI 画面…）给出逐步精确调用。不做"没绑工程就前置拒绝"——很多工具会自动连接 / 绑定，前置拒绝是退步。**2.7.58 实做**：①②③⑤ 全部落地（③ 是词汇表 + 手写批次 1，不是一次补完 1488 个；④ 的示例覆盖用按签名派生）；备选值解析在真机 schema 上核对后收紧到"参数自己那句话里独立成段的列表"。（2.7.57 原计划：交付包里加 `scripts/operations/Update-Engine.ps1`（或配置器里的按钮）——查 GitHub Releases 最新 tag 与本机 `manifest/delivery.json` 版本比较；发现 `TiaMcpServer.exe` 在运行就列出 pid 并拒绝，让维护者先关；下载 ZIP + `.sha256`、校验、把当前 `runtime/` 备份到 `runtime.bak/`、解压覆盖、`-Rollback` 换回备份；虚拟机没有外网时收 `-ZipPath` 离线更新。引擎侧只加只读的 `CheckForUpdate`（回报最新版本与本机版本，不改文件）。同时加 `PreflightToolCall(name, argumentsJson)`（只做参数门控 + 依赖检查，不碰 TIA，返回"会做什么 / 缺什么 / 该先调什么"），并给常用工具在描述里加一条调用示例，让 AI 先预检再调用、被纠正后按预检结果重规划而不是反复试错；纠正后的做法写进 `SKILL.md` / `GetAuthoringGuide` / 自然语言配方，让它持久。）实做与计划的差别：更新器不带配置器按钮（脚本即可）；示例不是手改 80 条属性字符串而是一张 `ToolExamples` 表在注册时附进描述并由构建校验；`PreflightToolCall` 的会话前提经 partial method 从引擎读，离线套件里为未知。
   3. **2.8.0 引擎拆分**：`Siemens/Portal/Portal.Software.cs`（7,423 行）按族拆成 `Portal.Software.<Family>.cs`，`Program.ReportBuilders.cs` / `Program.CliProbes.cs` 的 CLI 代码移到 `Cli/`，`McpServer.PlcSoftware.cs`（4,017 行）拆注册；同时决定是否改用自托管 runner（仓库公开，必须限制为 owner + `workflow_dispatch`）与不再把 exe 提交进仓库（以 Release 资产为准）。
   4. **其他语言的规范**：目前只有 SCL 与 LAD（S7DCL）经真机验证并有指南（`GetAuthoringGuide` topic `scl` / `lad`）；FBD / STL / GRAPH 要先在 `项目1` 上真机验证 S7DCL 导入路径，再写指南，不先写文档。
3. **真机剩余项（有条件时）**：~~工艺对象族~~ 2.7.48 起可建（5.0 版本），只剩导出导入待编译通过后跑；~~在线族~~ 走 PLCSIM Advanced（§1 ②）；~~工程级工具~~ 已跑。`ManageCommunicationConnection create HmiConnection` 的"Create 返回但连接数不增"待查（可能要 HMI 侧的连接对象）。~~DCC 全族~~ 已在 `项目1` 的临时 S120 V5.2 驱动轴上通过（2.7.45 真机结果）；剩 `DccChartInterfaceComposition.Create`（该固件不支持）与 `ManageDcbLibraries import`（无 DCB 扩展库 zip）；在线两个工具要驱动在线；S120 电机 / 编码器投影：电机 / 编码器已能插入，但 `GetCurrentMotorConfiguration` 只对"电机数据输入型"电机（目录电机被拒），还没找到能插的数据输入型电机标识。引擎存活已在 2.7.42 真机三次验证（TIA 退出后 `tools/list` 正常、`GetState.portalProcess.processAlive=false`，重启 TIA 后 `AttachToOpenProject` 即恢复）。PLCSIM Advanced 桥由维护者在自己的环境连续读写 50 次以上核对。
   **阶段 6 之后的工作**（不再有"下一批类型"）：回头把"仅类型名"（106）与"完全未触及"（116）逐个核对是否真有可封装成员（大多是壳子）；真机验证清单（第 4 条）里能补的项；`ReadTestSuiteCases` 等在有内容的工程上的行为；用户反馈驱动的修复。
4. **悬案**：`AddDevice {orderNumber:"6AV2124-0GC01-0AX0", version:"14.0.1.0"}` 在真机失败，而它的变体列表含目录原样的 `OrderNumber:6AV2 124-0GC01-0AX0/14.0.1.0`（`AddHardwareCatalogDeviceWithProbe` 用同一标识、同样的 `CreateWithItem(id, name, name)` 插成功）——2.7.44 只报最后一个变体的错误，2.7.45 起逐变体列出，下次真机重跑一次看原因；`AddDevice` 对 `V5.3/S120` 抛异常（目录最高 V5.2）。下次改源码顺手修一处陈旧文案：`ExportAlarmInstanceTexts` 的描述还写着 "ImportInstanceTexts (not yet exposed)"，而 `ImportPlcAlarmInstanceTexts` 早已存在（`Check-DeadToolReferences.py` 的 ALLOWED 里也还留着 `ImportInstanceTexts` 这条例外）。
5. **仍未真机验证的项**（无对象或无许可，留待有条件时）：所有经典 WinCC 实做路径（没有经典 HMI 工程；HMI-Classic 域 18 个工具里 8 个是离线构建 / 校验器，其余 10 个只验过 Unified 拒绝路径）、Startdrive 的在线工具、S120 电机 / 编码器投影、验收测试实做与 DCC 全族（需带 DCC 的驱动轴对象 / 在线驱动 / 插模块；其余 Startdrive 工具已在临时 G120C / S120 上验过）、`createFromType` / `createFromMasterCopy`（工程库没有规则表类型与规则母本）、`LayoutData` 导入、`UpgradeSivarcDefinitions` / `GenerateSiVArc` 实做（HMI 未连接 PLC）、Test Suite 定义本体的导入 / 导出 / 删除 / 作用域 / 复制 / 母本 / 编辑器（三组为空，只验过空表、NotFound 与空组执行）、CFC 有图表时的 `selectiveExport` / `import` / `exportInstructionData` / 密码动作与图表元素名（参考工程无图表，只验过空导出与预检拒绝）、Safety Validation 的 `createFromMasterCopy` 与条件级 `checkValidity`（后者在半更新对象上让 TIA 退出过，未再试）、Teamcenter 全部原生调用（无服务器）、DCC 全族与 Startdrive 在线工具（见第 2 条）、R/H、`GoOnline userName`、`OpenProject` UMAC 凭据、`CompareProjects rootElement`、`exportCardReaderPsc` 实做、双实例改绑拒绝、安全单元本体、单元 / 文档 / 外部源母本、`.nvt` 文档、真实 XLSX 导出导入、ProDiag 实做、带地址驱动的硬件连接 / 主值耦合 / 解释器映射 / Ident；虚拟机桌面的 `mcp2733-opcua.xml`（20 MB）、`LDiag_typeFault.s7dcl`、`MCP_TMP_FC_2735.scl`、`mcp2735-alarmclasses.dat`、`mcp2738-layout.yml` 可删，未保存工程里的临时监控表 `MCP_TMP_2736_WT` 随不保存即消失。

## 3. 每个阶段的固定动作

| 步骤 | 位置 / 命令 |
|---|---|
| 纯逻辑（无 Siemens 依赖）：参数门控、JSON 拆分、路径规则 | `tools/tiaportal-mcp/src/TiaMcpServer/Siemens/<Family>Logic.cs`（Unified 的放 `Siemens/Hmi/`）；把它加进 `tests/TiaMcpServer.Tests/TiaMcpServer.Tests.csproj` 的 `<Compile Include=…>` |
| 门户实现 | `Siemens/Portal/Portal.<Family>.cs`，用 `RunHmiStepTool(name, meta => {...}, requiresProject)`、`ExactPlcForEngineering`、`ResolvePlcService<T>`、`AcquireHmiEditAccess()`、`EngineeringScalarProperties`、`EngineeringGroupOperations`、`EngineeringObjectAddress`（属性路径 JSON `[{property,name}]`）、`NativeFileOutput.Plan/Verify`、`PortalException(PortalErrorCode.*)`；V20/V21 差异用 `#if TIA_V20` |
| 工具注册 | `ModelContextProtocol/Tools/McpServer.<Family>.cs`：`[McpServerTool(Name=...), Description("[L2][Domain][OP] ...")]`；描述里点名的其他工具必须真实存在（`scripts/checks/Check-DeadToolReferences.py`，例外名加进其 `ALLOWED`） |
| 离线测试 | `tests/TiaMcpServer.Tests/<Family>Tests.cs`，在 `Program.cs` 注册；`dotnet run -c Release` 于该目录。**不能用 Windows 盘符字面量**（CI 在 ubuntu 上跑），要 `Path.Combine(Path.GetTempPath(), …)` |
| 引擎 API 形状检查（逐成员核对 PublicAPI） | `tests/TiaMcpServer.HttpTests/<Family>ShapeChecks.cs`，在其 `Program.cs` 的 `engineering-api-only` 分支注册；跑 `tests/TiaMcpServer.HttpTests/bin/Release/net48/HttpTests.exe <引擎exe> engineering-api-only <PublicAPI目录>`，两版都跑，把 `COMPLETE: N` 写回 `scripts/build/Build-Release.ps1` 第 73 行 `if($major -eq 21){…}else{…}` |
| 发布后的坑（2.7.42） | `Build-Release.ps1` 末尾的 `Prepare-Delivery.ps1` 已经跑过 `Build-Configurator.ps1 -Test` 并把记录哈希写进 `manifest/delivery.json`；**之后不要再单独跑 `Build-Configurator.ps1 -Test`**（它重建 exe 并改写 `configurator-build.json`，`Package-Release.py` 随即报 "Configurator build record changed after delivery preparation"）。已经跑了就再跑一次 `Prepare-Delivery.ps1 -Release X.Y.Z -ReleaseDate YYYYMMDD` 并把 `TiaMcpConfigurator.exe` + 三个 manifest 作为 follow-up 提交。`Build-Release.ps1` 从 Git Bash 调用时路径要用正斜杠（反斜杠会被吃掉） |
| 覆盖审计回写 | `.\scripts\diagnostics\Audit-OpennessCoverage.ps1 -PublicApiDirectory <V21\net48> -Version V21 -OutputDirectory .\bin-build\audits\<名> -SummaryMarkdown .\bin-build\audits\<名>\V21-summary.md`；把统计表与"动态覆盖的类型"段整段替换进 `docs/reference/openness-coverage.md`，更新标题/缺口结构段的数字。"未封装功能类型"= `V21-types.csv` 里 `status=UNTOUCHED` 且类型名不以 `Composition/Association/EventArgs/Exception/Result/Info` 结尾者，按程序集求和 |
| 词法审计的坑 | 审计只看"类型名字面量 + `.成员(`"是否同时出现：链式调用 `umac.UmcUsers.Create(...)` 不算引用 `UmcUserComposition`，要写成类型化局部变量 `UmcUserComposition users = umac.UmcUsers;`；`Siemens.Engineering.Private.*` 与 `Compiler.CompileProvider` 是 internal，2.7.33 起审计脚本从分母排除；命名空间 `TiaMcpServer.Siemens` 会遮住根 `Siemens`，在 Portal 文件里写全名要用 `global::Siemens.Engineering...` |
| 动态覆盖登记 | 反射/泛型到达、类型名不出现在源码里的类型，登记进 `scripts/diagnostics/openness-dynamic-coverage.json`（`pattern` / `tool` / `mechanism` / `verified`）；`verified` 必须如实写真机证据或 "shape checks only" |
| 文档 | `CHANGELOG.md`、`docs/releases/vX.Y.Z.md`、`docs/development/roadmap.md`（§2.0 表 + §5 已完成段，编号顺延）、`docs/reference/capabilities.md`、`docs/reference/openness-coverage.md`（`docs/README.md` 当前发布链接由 `Release.ps1` 改）；发布后 `handoff.md` §1 改现状表、`handoff-history.md` §1 顶部加条目；工具数手改 `README.md` / `README.zh-CN.md` / `tools/tiaportal-mcp/skill/SKILL.md`（`tool-matrix.md` 与 `manifest/*` 由构建生成） |
| 版本号 | `scripts/build/Release.ps1 -Version X.Y.Z` 自动改 8 处：两个 `.csproj`（`AssemblyVersion` / `FileVersion` / `InformationalVersion`）、`tools/mcp-configurator/Configurator.cs`（两行）、`.claude-plugin/plugin.json`、`docs/README.md` 当前发布链接、`capabilities.md` 首段、`roadmap.md` 标题；手改时按同一清单 |

## 4. 构建与发布闸门（新机器要先满足）

- **一键发布**（2.7.56 之后的常规路径）：先手写 `CHANGELOG.md` 顶部 `## [X.Y.Z] - 日期` 条目与 `docs/releases/vX.Y.Z.md`（以及 `capabilities.md` 新段、`roadmap.md` §5 条目等文案），再 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build/Release.ps1 -Version X.Y.Z -Summary "<(1/3) 提交的一句话说明>"`——它依次做：前置检查（master 且不落后 origin、PublicAPI 目录、Python、没有残留的 `TiaMcpServer.exe`、CHANGELOG 与发布说明存在）→ 版本号 8 处 → `Build-Release.ps1` → `Check-Repository.py` + `Check-DeadToolReferences.py` + `Validate-Bundle.ps1 -Strict` → 三段提交 → `Package-Release.py` 干跑 → 逐 sha 推送 → 等 `validate-bundle` / `offline-checks` → annotated tag → 等 `Publish complete release` 并核对发布页的 ZIP；任一步失败即停，日志 `release.log`。选项 `-DryRun`（只到本地闸门，不提交）、`-SkipBuild`、`-NoPush` / `-NoTag` / `-NoWait`、`-KillStrayEngine`、`-ReleaseDate`。下面各条是它逐步做的事，也是它失败时的手工路径。
- 需要：Windows、.NET SDK 8 或 10（desktop-ivrlcht 用 10.0.401，net48 目标包正常）、Python 3.10+、Git，以及**放在仓库根的 PublicAPI 副本**（Siemens 授权组件，不可分发，`.gitignore` 已忽略这两个目录名）：`TIA_V20_PublicAPI\V20`（2000.4.401.2）与 `TIA_V21_PublicAPI\V21\net48`（2100.0.121.1，含 `Siemens.Engineering.Safety.dll`）。不需要安装 TIA Portal。
- `dotnet build … -p:SiemensEngineeringDirectory=D:\…` 要在 PowerShell 里跑：Git Bash 会改写反斜杠路径，Openness NuGet 的 targets 找不到目录就回落到 "Package" 解析，结果是 456 个 CS0246。
- 统一构建：
  ```powershell
  powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build/Build-Release.ps1 -V20ReferenceRoot <repo>\TIA_V20_PublicAPI\V20 -V21ReferenceRoot <repo>\TIA_V21_PublicAPI\V21\net48 -Python <python.exe 完整路径>
  ```
  约 4–15 分钟；它重建两版引擎、跑离线套件与形状检查、重建 `TiaMcpConfigurator.exe`、重生成 `manifest/*.json` 与 `docs/reference/tool-matrix.md`。若后台运行并用 `*>` 记日志，日志是 UTF-16——用 `tr -d '\0'` 再 grep。**构建期间不要再改 `src` / `tests` 里的任何文件**：源码哈希在最后一步（`delivery.log` 的 Validate-Bundle）才算，中途改一行就 "Source changed after validation"，整轮白跑（2.7.30 第一轮就是这样）。
- 手工单版构建（调试用）：V21 `dotnet build tools/tiaportal-mcp/src/TiaMcpServer/TiaMcpServer.V21.csproj -c Release -p:SiemensEngineeringDirectory=<V21\net48>`；V20 需另给 `-p:SiemensEngineeringDirectory=<V20> -p:BaseIntermediateOutputPath=<src>\obj-v20\ -p:MSBuildProjectExtensionsPath=<src>\obj-v20\`（绝对路径、以 `\` 结尾；在 PowerShell 里跑，Git Bash 会把尾部反斜杠吃掉）。
- **哈希闸门**：`manifest/release-build.json` 记录 `tools/tiaportal-mcp/src` 与 `tests`（含离线测试 .cs）的源码哈希；任何改动后不重跑 Build-Release，`validate-bundle` CI 与 `Package-Release.py` 都会报 "Source changed after validation"。不要手改哈希。只改文档/登记表不触发。
- 提交模式（`Release.ps1` 第 5–7 步）：`Release X.Y.Z (1/3)` 源码 + 版本 + CHANGELOG + 发布说明 + 文档；`(2/3)` `runtime/v20/TiaMcpServer.exe`；`(3/3)` `runtime/v21/TiaMcpServer.exe` + `TiaMcpConfigurator.exe` + `manifest/*` + `tool-matrix.md`。然后 `python scripts/build/Package-Release.py --git "<git.exe>"` 本地干跑，`git push origin master`，再推注解 tag `vX.Y.Z` → "Publish complete release" 工作流上传 ZIP。
- 不要 `git add -A`：仓库根可能有本地大压缩包。提交、PR、Release 正文**不加任何 AI 署名行**（维护者明确要求）。
- 本地等价于 CI 的检查：`validate` 工作流 = `python scripts/checks/Check-Repository.py` + `./scripts/checks/Validate-Bundle.ps1 -Strict`（PowerShell）+ `Build-Configurator.ps1 -Test`；`offline-checks` = `dotnet run --project tools/tiaportal-mcp/tests/TiaMcpServer.Tests/TiaMcpServer.Tests.csproj -c Release` + `python scripts/checks/Check-DeadToolReferences.py`。推 master 前四个都过；推 tag 前确认两条工作流已绿。
- CI 状态无 `gh` 时：REST API `https://api.github.com/repos/asckye/TIA_Portal_Openness_MCP/actions/runs?head_sha=<40 位 sha>` 未登录很快被 403 限流；`Release.ps1` 改抓工作流页 `https://github.com/asckye/TIA_Portal_Openness_MCP/actions/workflows/<validate|offline-checks|release>.yml` 里的 `aria-label="completed successfully: Run N of … Release X (3/3)…"`；发布产物：`.../releases/tag/vX.Y.Z`（ZIP + .sha256 两个资产）。
- 遇到过一次 `.git/refs/heads/master` 被写成 41 个空白字符（`git status` 报 "No commits yet"、push 报 "cannot be resolved to branch"）：提交对象与 reflog 都完好，`rm .git/refs/heads/master` 后 `git update-ref refs/heads/master <reflog 里最后的 sha>` 即恢复，`git fsck` 干净。再遇到照此处理，不要重新 clone。
- `Build-Release.ps1` 的 tools-list 闸门要求每个 `[McpServerTool]` 描述的 `[L2][<域>]` 标签已在 `ModelContextProtocol/ToolTaxonomy.cs` 注册（2.7.38 用 `[PLC]` 被拒，改 `[PLC-Software]`；2.7.39 的在线驱动工具用 `[Hardware][ONLINE]` / `[Hardware][ONLINE-WRITE]`，写保护钩子按操作标签拦截；现有域名看 `grep -o '\[L2\]\[[A-Za-z-]*\]' ModelContextProtocol/Tools/*.cs | sort | uniq -c`）；`Check-DeadToolReferences.py` 会把描述里点名的 API 成员名（`CreateOptions`、`GetLayoutFields`…）当成未注册工具名，加进它的 `ALLOWED` 并写明理由。
- 引用多个 Siemens 命名空间时同名类型会撞（`Siemens.Engineering.SiVArc.CreateOptions` vs `Library.Types.CreateOptions`；`MC.Drives.Enums.EncoderType` vs `HW.EncoderType`；V20 的 `MC.Drives.Enums.ConnectOption` vs `Motion.ConnectOption`）：用 `using X = ...;` 别名，不要去掉 `using`。在 `namespace TiaMcpServer.Siemens` 里写 `Siemens.Engineering.X` 会被解析成 `TiaMcpServer.Siemens.Engineering`——要 `global::Siemens.Engineering.X`。
- 工具名不分大小写必须唯一（`CallTool` 的 `AllToolMethods` 用 `OrdinalIgnoreCase`）：`Check-DeadToolReferences.py` 自 2.7.39 起拦截，本地和 `offline-checks` CI 都跑。
- 官方文档站：`POST https://docs.tia.siemens.cloud/api/khub/clustered-search` 体 `{"query":"<类型名>","contentLocale":"en-US","paging":{"page":1,"perPage":10}}` 回 `results[].entries[].topic{mapId,contentId,title,mapTitle}`；`GET /api/khub/maps/<mapId>/toc` 回目录树（`title` / `contentId` / `children`）；`GET /api/khub/maps/<mapId>/topics/<contentId>/content` 回 HTML。GET 的 clustered-search 是 404。脚本模板在会话草稿里没留，照上面三个 URL 重写即可。
- 快速对比 V20 / V21 某命名空间成员（不用装 TIA）：PowerShell `[Reflection.Assembly]::LoadFrom(<dll>)` 后 `GetExportedTypes()` 按 `Namespace` 过滤、`GetMembers(Public|Instance|Static|DeclaredOnly)` 列属性 / 方法签名，两版各存一份文本再 `diff`（2.7.38 用这个发现 V20 的 SiVArc 标记接口与 V21 的 `IEngineeringObject` 差异）。
- 给 AI 助手：含反斜杠的补丁脚本（Windows 路径、`\"` 转义）不要经 Bash heredoc 传，先用 Write 写到临时目录再执行；文档补丁一律先 `assert s.count(old)==1` 再替换。

## 5. 真机验证约定

- 虚拟机 HTTP 端口 `8765`（Bearer 鉴权，用户 `SIEMENS`，TIA V21），**地址随宿主机网络变**：一台机器上是 `192.168.0.172`，desktop-ivrlcht 上是 `10.10.10.56`——以本机 `~/.claude.json` 里 `tia-portal-vm` 的 `url` 为准。服务器端路径（`C:\Users\SIEMENS\...`）在虚拟机上，宿主机不可访问。
- 宿主机若设了 `HTTP_PROXY`（desktop-ivrlcht 是 `127.0.0.1:7897`），MCP 客户端会把局域网请求送进代理，表现为 "Version negotiation probe timed out after 5000ms"（curl 经代理同样 5 秒 502，`--noproxy '*'` 直连立即 401/200）。把虚拟机地址加进 `NO_PROXY`，或临时用直连脚本经 `CallTool` 调用。
- 默认 lite 只暴露 56 个工具，Unified 等全量工具经 `CallTool(name, argumentsJson)` 调用；大响应（>20,000 字符）会被服务器切成 `GetExport(exportId, offset)` 分页——验证时给小的 `limit`（2–5）而不是翻页。
- 直连探针（不经 MCP 客户端，绕过代理）：`python scripts/diagnostics/Probe-McpServer.py tools [关键字]` / `call <lite 工具> '{...}'` / `bridge <任意工具> @args.json`，连接信息默认取 `~/.claude.json` 的 `tia-portal-vm`（或 `--url` / `--token`、`TIA_MCP_URL` / `TIA_MCP_TOKEN`）。`bridge` 会把 `CallTool` 包装的内层 JSON 解开打印。每次调用是独立 MCP 会话，引擎的 Portal 状态跨会话保留。
- 换引擎 exe 后必须重启虚拟机上的服务进程，否则 `server:` 行仍报旧版本。工具返回 "HMI operation blocked after a connection failure"（`GetState` 里 `snapshotReadsBlocked=true`）时，再调一次 `AttachToOpenProject` 即解封；这是 HMI 快照的保守保护，已释放代理在 2.7.31 起由各工具就地处理。
- 虚拟机安装的选件（2.7.38 `ReadPortalInfo`）：TIA Portal V21 + Test Suite Advanced；SINAMICS Startdrive Advanced V21（SINAMICS DCC、G110M/G120/G120C/G120D/G120P/G115D、G130/G150/S120/S150/G200/G220/S200/S210/S220/Innomotics MVD）；STEP 7 Professional（CFC、Safety、Tech WebApplication）；WinCC Basic/Comfort/Advanced 与 WinCC Unified 各带 **SIMATIC Visualization Architect**（SiVArc，有许可）。2.7.33 记的"5 个产品"没展开 `options`。
- 工程 `AutomaticDipCoatingMachine`（V21），PLC `+S1-K1`（1510SP F V4.1），HMI `HMI_RT_1`（Unified，设备名 `++0.000OP1-KE06`，`HmiApplication.Type` = MTP1000 Unified Comfort）。**2026-09-20 起维护者要求：硬件 / 驱动类测试一律在新建的临时设备上做（`AddDevice` → 测 → `ManageHardwareObject deleteDevice`），不在工程原有设备上做；现有设备只读。**
- **让 TIA Portal V21 整个退出的 Openness 调用（2.7.39–2.7.42 真机；进程消失，`GetState` / 所有工具只见 `EngineeringObjectDisposedException` "TIA Portal has either been disposed or stopped running"，`AttachToOpenProject` 失败，未保存工程的临时对象随之消失，要在虚拟机里重启 TIA 并重开工程）**：① `ReadParameters.Find("r2139")` 后读 `Value`（现有 G120C）；② 已有主报文时 `TelegramComposition.CanInsertMainTelegram(1)`（前一步 `CanInsertTelegram(1, MainTelegram)` 回 false）；③ 新建 G120C 上读未接线 BICO 汇点 `p840[0]` 的 `Value`（同一参数在已接线的现有驱动上可读）；④ 新建未联网 G120C（`Telegrams` 为空）上 `CanInsertTelegram(700, SupplementaryTelegram)`（前一步 `CanInsertAdditionalTelegram(2, 4)` 只抛 "Invalid operation"）；⑤ 没有 CFC 图表文件夹的 `+S1-K1`（1510SP F，CFC 已装）上 `ChartProviderS7.GetChartProtection("MCP_NONE")`；⑥ 同一 PLC 上 `ChartProviderS7.ExportInstructionData(path)`（`CompleteExport` 却正常写出空 ZIP）；⑦ Safety Validation 条件在一次被 TIA 半拒绝的 update（`ExecutedInput=NotRelevant` 被拒但 `Comment` / `SignalName` 已写）之后调 `TestValidity.CheckValidity()`。⑤–⑦ 都是 `NonRecoverableException` 后进程消失；2.7.43 起 ⑤⑥ 经 `CompleteExport` 预检拒绝，⑦ 经引擎侧 NotRelevant 校验避免半更新。**⑩**（2.7.47 真机，`项目1`）`ScreenComposition.Import` 导入 Width/Height 与面板不一致的经典画面（构建器默认 640×480，TP700 Comfort 800×480）——`NonRecoverableException "The screen size does not match the device"`，进程消失；同一画面 800×480 正常（2.7.48 起 `ImportHmiScreen` 先比对面板已有画面或硬件目录描述里的分辨率，不一致/查不到即拒绝）。**⑨ 的精确边界**：`TO_PositioningAxis 5.0` / `TO_SpeedAxis 5.0` / `PID_Compact 2.3` 在同一 CPU 上都能建，只有 6.0 退出。**⑧**（2.7.45 真机，`项目1`）`Devices.CreateWithItem("OrderNumber:6AV2 128-3GB06-0AXx/20.0.0.0", …)`——TIA V21 里建上一代设备版本的 WinCC Unified Comfort 面板，进程直接消失；同一面板 `/21.0.0.0` 正常（2.7.46 起 `AddDevice` / 目录探针拒绝主版本不等于 Portal 主版本的 Unified 面板）。**⑨**（同日）`TechnologicalInstanceDBComposition.Create("MCP_Axis", "TO_PositioningAxis", 6.0)` 在 CPU 1515F-2 PN V2.9 上——官方版本表说 ≥V5.0/FW≥2.8 合法，17 秒后 `NonRecoverableException`，进程消失（2.7.46 只在描述里标明并建议 `ImportTechnologyObject`，没有可靠的前置判定）。保存过的 `项目1` 让重启代价只剩重开工程。2.7.40 起 `GetState.hmiReadHealth.portalProcess.processAlive` 直接说进程在不在；2.7.41 起引擎自己不再随 TIA 崩溃（在此之前 TIA 退出后引擎也以 0xE0434352 退出，虚拟机上的服务再把它拉起，绑定与临时对象状态全丢）。**永不保存工程**；临时对象（画面、变量、组）用完即删；改过的设置（报警类颜色、记录大小、GlobalSettings）复原；先 `dryRun` 再实做。
- 工程事实（硬件）：设备 `ET 200SP station_1` 在组 `+S1` 下（`devicePathJson=["ET 200SP station_1"]` 单名即可），CPU 项路径 `["+S1-K1"]`（**不经**机架 `机架_0`），PROFINET 接口 `["+S1-K1","PROFINET 接口_1"]`，其端口 `["+S1-K1","PROFINET 接口_1","端口_3"]` 与 `["BA 2xRJ45","Port_1"]`；IO 模块 `["+S1-K3","+S1-K3"]`（K3 DI16、K4 DQ16、K5 AI2、K6 AI4 RTD）、`F-DI 8x24VDC HF_1`；OPC UA 子模块 `["+S1-K1","OPC UA_1"]`；4 台 SINAMICS G120C（`SINAMICS G_5` → `["+S1-7Q1","PROFINET 接口"]` 等）是 IO 设备；子网 `PN/IE_1`，IO 系统 "PROFINET IO-System" #100；无拓扑互连、无传输区、无 MRP 环。
- 工程事实：`HmiSoftware.Connections` 为空（集成连接 `HMI_Connection_1` 只在硬件网络里）；无工厂视图；报警审计类为空；语言 en-US / zh-CN / de-DE；变量表 `Default tag table` 可放临时变量（`EnsureUnifiedHmiTag`，参数名是 `hmiSoftwarePath`）。

## 6. 只在真机上学到的 Openness 事实

（从 API 上核实的：`WebserverUserPermissions` 值为独立单比特但没有 `[Flags]`，组合值 `ToString()` 是数字，引擎按位解码；`SimpleWebserverUserComposition` 无 `Create`、`SimpleWebserverUser` 无 `Delete`；`Subnet.Nodes` / `Subnet.IoSystems` 是关联；`ISyncDomainParticipant` 由 `NetworkInterface` 与 `IoSystem` 实现；`TransferAreaType.F_CD` 仅 V21；`Address.AssignProcessImageToOrganizationBlock` 仅 V20。官方文档站的正文可经 `https://docs.tia.siemens.cloud/api/khub/maps/<mapId>/topics/<contentId>/content` 直接取 HTML，`clustered-search` API 按标题搜页面——比逐页导航快得多。）

- `Create<T>` / `Create` 返回的代理与 `Find()` 不同，`ReferenceEquals` 恒假——创建后按名称查回 + 计数核对。
- `HmiSlider` / `HmiToggleSwitch` / `HmiCircleSegment` / `HmiEllipseSegment` 隐藏基类 `EventHandlers`，`Type.GetProperty` 抛歧义——取最派生声明（`UnifiedUiModelLogic.FindProperty`）；`HmiLabel` 没有 `EventHandlers`；`HmiButton` 事件是 `Tapped` 不是 `Click`。
- `Object` 类型属性写 JSON 数字 `0` 后读回是字符串 `"0"`——跨类型比较（`EngineeringScalarProperties.SameValue`）。
- `HmiThresholdComposition.Create()` 在 PublicAPI 里，但 TIA V21 对普通变量拒绝 "New thresholds are not supported"；替代值只对外部变量可设，内部变量报"已禁用的字段不支持设置"；`Font.Weight` 只接受 `Bold` / `Normal`。
- `HmiTagComposition.Export(dir, name)` 回报的 `FileInfo` 没有 `.hmi.yml` 扩展名（文件本身写出）；脚本模块导出正确回报 `Name.hmi.yml` + `Name.hmi.js`。`ShiftModel` 这张 15 变量的表导出只有 `ShiftModel.hmi.yml`（4,400 B）一个文件，没有 `NameData.yml` 等副文件。
- 硬件（2.7.30 真机）：变更前取到的 HW 组合代理是陈旧的——`MrpDomainComposition.Create` 后在旧代理上按名查不到，`Delete` 后旧代理枚举抛 `EngineeringObjectDisposedException`；从 owner 重新导航即正常。空的临时同步域会被 TIA 释放。SINAMICS 接口暴露两个 IoConnector（文档说一个），未连接的那个所有 PN 属性不支持。1510SP F V4.1 上 `WebserverUserManagement` / `SimpleWebserverUserManagement` 为 null（Web 用户由 UMAC 接管），`OpcUaUserManagement` 在 `OPC UA_1` 子模块。`MrpInstancesOwner` 只在组态了 MRP 环的接口项上出现。ET 200SP DI 模块通道的 `ChannelActivated` / `InputDelay` 只读；AI 通道 `Smoothing` 写 1 被拒、写 0 同值可通。S7-1500 地址不支持 `Context` / `IsochronousMode` / `InterruptObNumber`。
- `HmiTextList.Export` / `HmiSystemTextList.Export` 各写两个文件：`<名>.hmi.yml` + `<名>.TextLibrary.hmi.yml`，后者带整个文本库（`PowerState` 的两个文件 554 KB + 599 KB）。
- `HmiTag.Validate()` 在 `ShiftModel` 的变量上返回 3 条 `HmiValidationResult`（`Connection` / `DataType` / `PlcTag`），错误文本按 TIA 界面语言本地化（zh-CN），`Warnings` 为空；"HMI_Connection_1 不存在"与 `HmiSoftware.Connections` 为空一致。
- `HmiScreenGroupComposition.Create(name)` 后 `deleteEmpty` 对空组生效，按名查回 "Exact named object not found"。
- 变量对象路径：`[{TagTableGroups,组},{Groups,子组},{TagTables,表},{Tags,变量}]`；`Default tag table` 在该工程为空，临时变量要用它但真实变量在 `/_SICAR_HMI-tags/...`。
- V20 画面对象目录 37 个、V21 43 个；`HmiScreenItemBaseComposition` 在 `UI.Base` 命名空间。
- 下载提示 `SafetyProgram` 应答 `ConsistentDownload`；`SafetyAdministration.ProgramSignatures` 与 `SafetyBaseIdProvider` 仅 V21。
- Base 收尾（2.7.33 真机）：`ObjectIdentifierProvider.GetIdentifier` 返回 Base64 短串（设备 `mf8qUCbYAkituuc//yPo5g==`），`Find` 反查同名对象；`TiaPortalProcess.AttachedSessions` 里能看到本引擎自己（`accessLevel` "Modify, Published"、`trustAuthority` None）；`InstalledSoftware` 5 个产品（TIA Portal / Startdrive Advanced / STEP 7 Professional / WinCC Basic-Comfort-Advanced / WinCC Unified）；工程 18 个 `TextCategories`；`HwUtilities` 三个标识符 `OPCUAExportProvider` / `ModuleInformationProvider` / `CardReaderPscProvider`；`ModuleInformationProvider.FindContainerTypes(1510SP)` → `System:Device.ET200SP` / `System:Rack.ET200SP`；`OpcUaExportProvider.Export` 对 1510SP F 写出 20 MB XML；路由树：PG/PC 接口本身 `Addresses` 为空，地址挂在子网 `PN/IE_1` 上，目标接口 `1 X1` 的 `Addresses` 也为空；`DefaultWebPagesFeature` 2 个 Web 应用（系统页 `System_Inbuilt` + 硬件配置入口页 `None` 默认）；`CertificateManagementConfiguration` 默认 `TIAPortal` / 0 服务；`Transaction` 真机行为：全部成功 + `CommitOnDispose` 持久，任一失败不调用 `CommitOnDispose` 即整体回滚（第一条已执行的创建被撤销）；`CrossReferenceResult` 的 `SourceObject.TypeName` 是本地化描述（"LAD-Function block"），`Address` 是 `%FB12020` 形式。
- Step7 ④-②（2.7.35 真机）：`PlcExternalSourceComposition.CreateFromFile(name, path)` + `GenerateBlocksFromSource(PlcBlockUserGroup, None)` 从 208 B 的 SCL 文件生成 FC 到用户组，回报 `IList<IEngineeringObject>`（`FC`）；根外部源组名本地化“外部源文件”；`PlcTableCommentEntryComposition.Create()` 后同一组合代理的 `Count` 陈旧（重读为准），而 TIA 拒绝 `PlcTableCommentEntry.Delete`（“not valid because of the current status”）；旧工具 `SetWatchTableModifyValue` 在 V21 建不了带地址的条目（`Create()` 无地址重载）；`PlcAlarmTextListProvider.ExportToXlsx` 在无用户文本列表时抛 `TextListNotFoundException`，过滤重载对不存在的列表抛 `EngineeringTargetInvocationException`（文案和官方页面一致）；`WatchTableAccessRuleComposition.Create` 在 CPU Web 服务器未启用时被拒（“Operation cannot be performed when the web server is disabled.”）；`AlarmClassDataProvider` 在 PLC 上直接可得、消息本地化；默认变量表 `Standard` 有 79 个 `Pip` 类型系统常量；`GetPlcTagTables` 回的表路径带组名（`IO/F_Input`），`ReadPlcTagTableConstants` 要用同样的路径。
- Startdrive（2.7.41 真机，临时 S120 CU320-2 PN V5.1 与临时 G120C）：S120 的 `DriveObjectContainer` 在机架项 `驱动控制`（`DeviceItems[0]`，官方 "S devices" 例）上而不在 CU 设备项上，`DriveObjectNumber` = 1 可读；未联网时主报文 999 为 1/1 字且无地址；`DriveObjectFunctions` 提供：`DriveObjectTypeHandler.CurrentDriveObjectType` 名 "-" 号 0，`PossibleDriveObjectTypes` = ["SINAMICS S" / 1]，`DriveObjectActivation` Activate / IsActive；`DriveObject.Security` 非 null 但 `UmacConfiguration` / `DriveDataEncryption` 都为 null；`TechnologyExtensionContainer` 列出 TRCDATA / OALINK（`IsActivated=false`，`Parameters` 空），`Activate()` 后 `Parameters` 250 个，`Deactivate()` 读回 false；`DriveItemHardwareModule` 在机架项上（`ComponentName` 驱动控制、`TypeName` AcxSinamics.Rack.CU、`TypeIdentifier` System:Rack.S120、位置 0），G120C 的驱动项 / `PM G120C` 都不提供；`TechnologyExtensionInstallationProvider` 在 `TiaPortal` 上列出已装 .tec 包（REACTPWR / VIBX / TRCDATA / POLYGON / DCDCCONV / OALINK / SETPGEN / HEM / SERVC …）；CU 驱动对象不提供 `DriveControlChartContainer`（DCC 要驱动轴对象），机架项不提供 `SafetyAcceptanceTestProvider`；G120C：`HardwareProjection.GetCurrentMotorConfiguration(0)` 在未设电机类型时抛 "No motor type is defined. Please set an appropriate motor type first"，`SetMotorType(InductionMotor, 0)` 回 true 后 required entries 为 p304 / p305 / p307 / p310 / p311 …（float 写 2.2 读回 2.20000005），`ProjectMotorConfiguration` 回 true；`SetEncoder` / `GetCurrentEncoderConfiguration` 在 G120C 上抛 "The selected device does not support the encoder projection"。
- Safety Validation Assistant（2.7.42 真机，虚拟机有选件，F-CPU `+S1-K1`）：`DeviceQuery.EvaluationDevices()` 只列 F-CPU，`ActivationTest.AvailableDevices()` 列 4 台 G120C + F-CPU（条件设备可选驱动，评估设备不行——`ChangeEvaluationDevice(驱动)` 抛 "not a valid evaluation device"）；新建激活测试自带一个 "SF 1"，`SafetyFunctionComposition.Create()` / `CreateFrom` 生成 "SF 2" / "SF 3"（`Name` 只读，`TestName` 可空可重复）；`CreateFrom(ActivationTest)` 生成 "Safety Activation Test_1" 且不复制 `Author`；`Import(file, Rename)` 生成 "MCP_TMP_AT_R_1"，`None` 抛 "composition already contains an object with the same values for the identifier(s) 'Name'"；`Condition.InitialInput / ExecutedInput / Response` 实际是 `ConditionValue` 枚举（新建为 NotRelevant），TIA 按信号用途拒绝 NotRelevant（"'NotRelevant' is not supported when the signal usage is 'InputCondition'"）；`TraceConfiguration` 动态属性 `IsTraced` / `OrganizationBlock` / `PretriggerTime` / `PretriggerUnit` / `RecordingDuration` / `RecordingDurationUnit` / `Signals` / `TriggerTag`，默认 1000 / 2000；`CheckValidity` 消息本地化英文（"SF 1 has errors"、"Mandatory Signal Usage type(s) missing."、"Error in trace configuration."、"Error: Sensor - Controlmode" / "Error: OrganizationBlock" / "Error: TriggerTag"）；`ActivationTestPrintout.Generate` 写 9.8 KB .xlsx；导出 XML 1.4 KB（测试）/ 3.1 KB（安全功能）。
- Test Suite（2.7.42 真机，Test Suite Advanced，三组为空）：`RuleSetExecutor.Run(StyleGuideSystemGroup)` 对空组回 Success，消息路径 "样式指南"、描述 "规则集的执行已完成（错误：0；警告：0）"（按 TIA 界面语言 zh-CN）；`TestCaseExecutor.Run(ApplicationTestSystemGroup)` 空组在 TIA 内抛 NullReferenceException（`EngineeringTargetInvocationException`）；`SystemTestCaseExecutor.Run(SystemTestSystemGroup)` 空组抛 "No test case(s) in the selected project to start the execution."。
- CFC（2.7.42 / 2.7.43 真机，STEP 7 CFC 已装，`+S1-K1` 无图表文件夹）：`PlcSoftware.GetService<ChartProviderS7>()` 非 null；`CompleteExport(path, "V2.0", 0, true)` 写出 484–485 字节 ZIP，条目 `Data/` + `Data.xml`，XML 结构 `Document > DocumentInfo / FunctionChartsFolder(Name="Charts") > ObjectList / UsedAlarmClasses`（文件夹元素带 `Name`，图表元素名尚未见过——参考工程没有 CFC 图表）；名字类 / 指令数据调用让 TIA 退出（§5 ⑤⑥）。
- Teamcenter（2.7.42 真机）：`TiaPortal.GetService<TeamcenterConnectionProvider>()` 非 null（网关组件随 TIA 安装），无服务器可连。
- 2.7.55 部署后学到的（`项目1`，PLCSIM Advanced 8.0，TIA V21）：按同步点等待后 `SingleStep_C` 每次 `RunToNextSyncPoint` 恰好一个 OB1 周期（5 周期 71 ms，含每周期的 Freeze 轮询），`Default` 模式恢复后实例继续 Run；引擎的 `Connect` 在 `TiaPortal.GetProcesses()` 返回多个进程时会 `new TiaPortal(...)` 自启一个空实例而不是附加（引擎重启 + 维护者另开工程时触发），`ListPortalProcessProjects` 能逐进程 attach 并列出工程，`AttachToOpenProject` 按工程名换绑进程。
- 2.7.54 部署后学到的（`项目1`，PLCSIM Advanced 8.0，TIA V21）：`IInstance.RunToNextSyncPoint()` 是异步的——只解除 `Freeze`、立即返回，实例跑到下一个同步点才再次 `Freeze`（手册 "SingleStep operating modes"），连发 N 次只推进 1 周期，必须每次等 `OperatingState == Freeze`；`SingleStep_C` 只在周期控制点冻结（每周期一个同步点），`SingleStep_CP` 在读过程映像前再冻结一次（两个）；切换单步模式后实例先跑到第一个同步点才 `Freeze`；`Default` 模式下写 / 读之间 300 ms 足够程序跑若干周期。`StationUploadProvider.StationUpload` 对 PLCSIM Advanced 实例一律 "The selected object cannot be uploaded from the device."（路由 / 地址 / 提示都过了才拒），工程级 `ConnectionConfiguration` 在 Softbus 下的三个模式是 MPI / PROFIBUS / PN/IE，且扫描把实例列为 Softbus 默认地址 192.168.0.1 而不是已配置的 192.168.0.3（下载 / 上线用 .3 没问题）。
- 2.7.53 部署后学到的（`项目1`，PLCSIM Advanced 8.0，TIA V21）：**Openness 不能下载故障安全数据**——F-CPU 的 `DownloadProvider.Download` 一律 "Loading or overloading fail-safe data in Openness is not permitted."（硬件编译干净也一样），在线族要用标准 CPU；TIA V21 新建的标准 CPU 1515-2 PN V2.9 默认也是 `NoAccess` + `WithoutPassword`，所以每台新 CPU 下载前都要 `ManagePlcProtection`；首次下载到 PLCSIM Advanced 实例（IP 还是默认 192.168.0.1）用 created 地址走 5 参 `Download`，TIA 会抛 `PlcMasterSecretPassword` 下载提示（文案 "The PLC has no password for confidential PLC configuration data. Validate the password again."，`IsSecureCommunication=True`），`DownloadToPlc` 的 `masterSecretPassword` 参数应答它；下载后实例的 `CPUType` 变成工程的型号（CPU1515）、`ControllerName` = 工程设备名、IP = 工程 IP，之后走子网路由；PLCSIM 标签表只有程序符号（空程序 0 标签，DB 成员按 `DB.Member` 列出，无 `%` 地址）；`EOperatingMode` 8.0 = `Default / SingleStep_C / SingleStep_CT / TimespanSynchronized_C / SingleStep_P / TimespanSynchronized_P / SingleStep_CP / SingleStep_CPT / TimespanSynchronized_CP / SingleStep_Bus`；工程级 `StationUploadProvider.Configuration` 在 Softbus 下按连接模式把 "PLCSIM (#1)" 列三遍（PN/IE / PROFIBUS / MPI）；`FingerprintDataProvider` 在 1515-2 PN V2.9 上也为 null；SCL 外部源里 `Counter` 是保留字（"设定值 'Counter' 无效"），SCL `ORGANIZATION_BLOCK "Main"` 生成会覆盖默认的 LAD OB1；`ManagePlcExternalSources` 真跑要先 `GoOffline`。
- 2.7.52 部署后学到的（`项目1`，PLCSIM Advanced 8.0，TIA V21）：TLS 信任提示确实经 `ConnectionConfiguration.OnlineLegitimation` 抛 `TlsVerificationConfiguration`（`VerificationInfo "certificate not matching"`，初值 `NonVerified`），答 `Trusted` 后 TIA 在会话内记住（后续 `GoOnline` / `Download` 不再问）；对一台从未下载过的 PLCSIM Advanced 实例（CPU1500_Unspecified）`OnlineProvider.GoOnline(address)` 抛 `EngineeringTargetInvocationException`（外层文案只有 "Error when calling method 'GoOnline'"，正文在内层异常里——工具应把 `InnerException` 链摊出来），而 `OnlineProvider.State` 已是 `Incompatible`；`DownloadToPlc` 前 TIA 先做硬件编译，F-CPU 1515F-2 PN V2.9 在 TIA V21 新建时访问级别默认 **`NoAccess`**（不是 FullAccess），且 "Protect confidential PLC configuration data" 勾选但无密码（`MasterSecretConfiguration.WithoutPassword`）——硬件编译以 3 个错拒绝：完全访问密码未设、机密组态数据密码未配置、通信证书建不了；两项都不是设备项属性（`GetAttribute("PlcProtectionAccessLevel")` 被拒 "not supported"），只能经 `PlcAccessLevelProvider` / `PlcMasterSecretConfigurator` 服务改；`Device.GetService<ICompilable>().Compile()` 就是 UI 的硬件编译，`CompilerResult.Messages` 树里 `Path` 依次是 `MCP_PLC` / `Hardware configuration` / `导轨_0` / `MCP_PLC` / `CPU 显示_1` / 子网 `MCP_PN`。V20 的 `PlcMasterSecretConfigurator` 没有 `ProtectAllPlcConfiguration*` / `UnprotectAllPlcConfiguration`，`MasterSecretConfiguration` 也没有 `WithPasswordAllDataProtection`。反射工具的 `DeviceItem objectPath` 就是 CPU 名（`MCP_PLC`），`ReadHardwareFeatures` 的 `itemPathJson` 是名字数组 `["导轨_0","MCP_PLC"]`。
- 2.7.51 部署后学到的（`项目1`，PLCSIM Advanced 8.0，TIA V21）：**FW ≥ 2.9 的 S7-1500（PLCSIM Advanced 的 CPU1500_Unspecified 也算）首次 `GoOnline` / `Download` 会经 `ConnectionConfiguration.OnlineLegitimation` 抛 `TlsVerificationConfiguration`，不答就是 "Connection to device cannot be established. The device is not trusted. Please check the certificate."（下载侧文案只有 "连接到模块 … 失败"）**；`ScanAccessibleDevices` 不受影响（DCP 扫描无 TLS）。Softbus 下 PLCSIM 接口的 PC 侧仍 `[no IP]`，路由选到子网地址 `192.168.0.1 (subnet MCP_PN)`，TIA 接受这条路由（失败在 TLS 而不是路由）。`OnlineProvider.GoOnline` 只有无参与 `(ConfigurationAddress)` 两个重载，TLS / 密码 / UMAC 提示全部走 `OnlineLegitimation` 事件；`FingerprintDataProvider.GetFingerprintData` 则直接收 `OnlineConfigurationDelegate`。`Download.Configurations.DownloadCertificate` 只有 `Parent`（用途未知，未接）。监控表 SimaticML 往返去掉 `ModifyIntention` 后 `Import(Override)` 通过，新行不写 `DisplayFormat` 时 TIA 按地址定（`%M0.0` → Bool）；`ModifyIntention` 不随 `ModifyValue` 变（读回仍 false）——它是行的"修改"勾选框，Openness 只读，所以经 API 只能预置值和触发器，勾选要在 TIA 里点。`SimulationRuntimeManager.NetworkMode` 真机 `{get;set}`，无实例运行时写 `Softbus` 立即生效，同一实例 `unregister` 后再 `register` 读回 `Softbus`；Softbus 下 `powerOn` 后实例 `controllerIP` 直接是 `192.168.0.1`（TCPIP 下未下载是 `0.0.0.0`），TIA 的 `ConnectionConfiguration` 路由树随之只剩 PC 接口 "PLCSIM"（两块物理网卡不再列出）——PG 侧不需要任何 IP。
- 2.7.50 部署后学到的（`项目1`，PLCSIM Advanced 8.0，TIA V21）：监控表 SimaticML 导入时 `PlcWatchTableEntry` 的 `ModifyIntention` 是只读属性——行里带它（哪怕 `false`）`Import(Override)` 就报 "Cannot update … 'set_ModifyIntention' is not supported … read-only"，TIA 按 `ModifyValue` 自己推导；失败的 Override 导入不动原表。PLCSIM Advanced 8.0 API：`CommunicationInterface` 在 `CInstanceNet` 与 `IInstance` 上都只有 getter，没有 `SetCommunicationInterface`（`memberFilter` 证实），实例级只剩 `Get/Set/CheckNetInterfaceMapping`；接口选择是全局 `SimulationRuntimeManager.NetworkMode`（`ENetworkMode`：`TCPIPMultipleAdapter` / `TCPIPSingleAdapter` / `Softbus`，手册 "Interfaces for IBaseRuntimeManager"，有实例在跑时 `InstanceAlreadyRunning`，Npcap 未启动时 `PCAPDriverNotRunning`）；虚拟机重启后已注册实例全部消失（`RegisteredInstanceInfo` 为空），`register` 默认 TCPIP、`powerOn` 后 `controllerIP 0.0.0.0`。`ConfigurationAddressComposition.Create` 只收 IP：传扫描到的 MAC `02-C0-A8-00-C8-00` 报 "does not specify a valid address for 'Siemens PLCSIM Virtual Ethernet Adapter'"（官方 "Uploading PLC device" 页只用 `Create("192.68.0.1")`）——没下载过的 PLCSIM 实例没有 IP，站上载先要下载。本机构建：`dotnet build` 的 `-p:BaseIntermediateOutputPath` / `-p:MSBuildProjectExtensionsPath` 必须是绝对路径（相对 `obj/` 时 NuGet 的 `*.nuget.g.targets` 不被导入，Openness 包的 targets 不生效，几百个 CS0234）。
- 2.7.49 部署后学到的（`项目1`，PLCSIM Advanced 8.0，TIA V21）：`PlcTableCommentEntryComposition.Create()` 建出的对象类型就是 `PlcTableCommentEntry`（注释行），对它 `SetAttribute("Address" | "Name" | "ModifyValue" | "ModifyTrigger")` 全部 "'X' is not supported by type PlcTableCommentEntry"——类型化 API 没有任何入口建带变量的监控表行，只能 SimaticML 导入（`PlcWatchTableComposition.Import(file, ImportOptions.Override)` 覆盖同名表）；`PlcWatchTableComposition.Create(name)` 在根组建表时若同名表已在别的组里，TIA 自动改名 `Name_1`、`Name_2` …（不报错）。PLCSIM Advanced 8.0 .NET API：实例对象的具体类型与其实现的全部接口都没有带 setter 的 `CommunicationInterface` / `OperatingMode` 属性（`PropertyInfo.CanWrite=false`），手册里的 `SetCommunicationInterface()` 应是方法（2.7.50 起试）；`UnregisterInstance` 后同名再 `RegisterInstance` 沿用持久化的设置。`ConnectionConfiguration.ApplyConfiguration(ConfigurationAddress)` 对子网地址返回 true，随后 `Download(IConfiguration=address)` / `GoOnline(address)` 都真正发起连接——PG 网卡无 IP 时 TIA 报 "连接到模块 … 失败" / "The connection partner is not responding"（不是路由错误）。工程级 `StationUploadProvider.Configuration` 的 PC 接口 `TargetInterfaces` 与 `Subnets` 都为空。刚 `ImportTechnologyObject` 的 TO 未编译前 `Export` 被拒 "Inconsistent blocks and PLC data types (UDT) cannot be exported"。
- 2.7.48 部署后学到的（`项目1`，PLCSIM Advanced 8.0，TIA V21）：`ConnectionConfiguration` 路由树里 CPU 的配置 IP（192.168.0.1）在 `ConfigurationPcInterface.Subnets["MCP_PN"].Addresses` 下，两块网卡的 `TargetInterfaces` `1 X1` / `1 X2` 都没有地址（PG 网卡看不到 CPU 时目标接口为空）；`ConfigurationAddress` 与 `ConfigurationTargetInterface` 都实现 `IConfiguration`，`ConfigurationAddressComposition.Create(string)`（V20 / V21）是给目标接口 / 子网建地址的官方入口（§6 上面 "没有公开构造函数" 一条指的是 `new`）；`ConnectionConfiguration.ApplyConfiguration` 有 `ConfigurationAddress` 与 `ConfigurationTargetInterface` 两个重载；`OnlineProvider.GoOnline(ConfigurationAddress)` 仅 V21；`DownloadProvider.Download(IConfiguration, ConfigurationAddress, pre, post, options)` 是"下载到指定 IP"的 5 参重载（V20 / V21）。虚拟网卡 "Siemens PLCSIM Virtual Ethernet Adapter" 没配 IP 时路由描述 `[no IP]`，`ScanAccessibleDevices` 仍能按 DCP 找到实例（MAC `02-C0-A8-00-F1-00`，`deviceSeries "S7-1500 (PLCSIM)"`，无 IP），但下载 / 上线需要 PG 侧有同网段 IP。PLCSIM Advanced 运行时的实例类把 `CommunicationInterface` / `OperatingMode` 公开为只读属性、setter 在 `IInstance` 接口上（`PropertyInfo.SetValue` 在具体类型上抛 "未找到属性设置方法"）；`UnregisterInstance` 之后实例仍存在于 PLCSIM 的持久化目录，再 `RegisterInstance` 同名会沿用原设置（TCPIP）；`PowerOff` 后 `OperatingState` 为空字符串。`FingerprintDataProvider`（`Siemens.Engineering.FingerprintData`）与 `ParameterUploadProvider` 在 1515F-2 PN V2.9 的 PlcSoftware / CPU 项 / 导轨 / Device 上 `GetService` 都为 null。`PlcWatchTable.Entries` / `PlcForceTable.Entries` 的类型是 `PlcTableCommentEntryComposition`，只有无参 `Create()`（返回 `PlcTableCommentEntry`，`PlcWatchTableEntry` / `PlcForceTableEntry` 是其子类），条目的类型化属性全部只读——值经 `SetAttribute` / `GetAttribute`（`ModifyTrigger` 枚举 `PlcWatchAndForceTablePreDefinedTrigger`：Permanent / PermanentAtStart / OnceOnlyAtStart / PermanentAtEnd / OnceOnlyAtEnd / PermanentAtStop / OnceOnlyAtStop / Undef）。`TO_SpeedAxis 5.0` 没有驱动报文时 TO 不能编译一致，`Export` 抛 "Inconsistent blocks … cannot be exported"；PID_Compact 2.3 导出 / 导入（含进用户文件夹）正常；`TechnologicalInstanceDBGroup.Groups` 里的 TO 不在根 `TechnologicalObjects` 里。
- 2.7.47 重跑学到的（`项目1`）：192.168.0.1 的 "CPU 1510SP F" 是维护者当时开着的 PLCSIM Advanced 实例（TCPIP 模式在虚拟网卡上），不是真实 CPU；新注册的实例默认 `communicationInterface TCPIP`、`controllerIP 0.0.0.0`，`ReadTransferRoutes` 列出 "Siemens PLCSIM Virtual Ethernet Adapter"；TP700 Comfort 没有任何显示分辨率属性（253 属性里没有），分辨率只在硬件目录描述里；`SaveAsProject` 后 TIA 切到副本；`CreateProject` 会替换本会话打开的工程；`TechnologicalParameter.Value` 对 PID_Compact 2.3 不可写；`CamDataSupport` 只有 TO_Cam 提供；`WriteClassicHmiMinimalPackageFiles` 的输出路径在 `screenXmlPath` / `tagTableXmlPath`。
- 2.7.46 全量重测学到的（`项目1`）：S7-1500 的模块插在导轨项 `MCP_PLC/导轨_0`（`CanPlugNew` 在 CPU 项与 Device 上对 DI16 全 false），插入后模块是 Device 级设备项、导轨只在 `Items` 里列它；DI16 的 I/O 地址挂在子项 `MCP_DI/MCP_DI` 上；Comfort 面板头项的 `DeviceItems` 为空，`IE_CP_1` 等是 Device 级设备项且头项只在 `Items` 里列它们，以太网口路径 `MCP_TP700.IE_CP_1/PROFINET Interface_1`；1515F-2 PN V2.9 的 253 个属性里没有 PUT/GET；报警类导入导出格式 `.DAT`、系统诊断设置 `.dat`；空 PLC 上 `PlcAlarmTextListProvider.ExportToXlsx` 抛 `TextListNotFoundException`；知识保护 `Protect` 对 `Mcp12345` 抛 `EngineeringPasswordPolicyViolationException`；`ServerInterfaceGroup.AccessControl` 在 FW 2.9 不可用；TP700 Comfort V17 的 `HmiTarget` 不提供 `GraphicsProvider`，其 Button 不接受 `set_Visible`；WinCC Unified 按钮事件名是 `Tapped/Down/Up/…`（没有 Click），`textLists` 没有 `Create(string)`，`PlantView.Comment` 不可写；`UserGlobalLibrary.SaveAs(dir)` 后库名变成目录名；PLCSIM Advanced 在虚拟机可用（`register/powerOn` 即得 Stop 态实例）；虚拟机网段 192.168.0.1 上是维护者的真实 CPU 1510SP F（RUN）——S7 只读探测可用，**不要**对它 GoOnline / 下载 / 写值。
- DCC（2.7.45 真机，`项目1` 临时 S120 V5.2 驱动轴 DO 2）：`DriveControlChartContainer` 只在带 `DCC/5201000` 工艺扩展的驱动轴上（V5.1 固件的轴没有）；`DriveControlChartComposition.Create(name)` 建根图 / 子图（`Root/Sub` 路径）；`DccBlockComposition.Create("ADD")` 自动命名 `add_1`，`Create("mul_1","MUL")`；引脚 `Value` 是块定义的 CLR 类型（ADD 的 X1 是 `System.Single`）；`Publish(bool)` 给出参数号 21500，`DccParameter.ArrayIndex` 抛 not supported；`DccChartInterfaceComposition.Create(DccPin)` 根图 / 子图都 "This operation is not supported"；`GetRunSequence` 列块与子图；导出文件必须 `.dcc`；`Import(file, RenameOnConflict)` 生成 `MCP_Chart_1`；DCB 库 `TPDCBLIB` 5.20.10.0 有 142 个块类型。
- S120 驱动组件（2.7.45 真机）：`Device.CanPlugNew("OrderNumber:6SLxx2x-1xxxx-xxxx", name, 65535)` 为 true（`6SL3xxx-xxxxx-xxxx` 与具体 MLFB `6SL3120-1TE21-0AA4` 为 false，带 `/V5.1` 后缀也 false），设备项上全 false；电机 `OrderNumber:1PH2092-4WG4x-xxxx` / 编码器 `OrderNumber:XExxxxx-xxxxx-xxxx//DRIVE-CLIQ.202` 插在机架项 `驱动轴_1`（= 电机模块的 `Container`）上，`1FK7xxx` / `1PH8xxx` / `1LE1xxx` 通配为 false；`DriveItemHardwareModule.PositionNumber` 运行时只读、`ChangeType` 对手册原例回 "Invalid type identifier"；`GetCurrentMotorConfiguration` 只接受电机数据输入型电机。
- 硬件目录（2.7.45 真机）：`SearchHardwareCatalog` 查不到 `6SL3120`（电机模块只有通配条目 `6SLxx2x-1xxxx-xxxx` / `6SLxx2x-2xxxx-xxxx`），S120 CU 的 `AddDevice` 版本最高 `V5.2/S120`。
- Startdrive（2.7.39 真机，G120C）：`DriveObjectContainer` 只在驱动单元项（`+S1-7Q1` / 新建设备的同名项）上，`PM G120C` 与嵌套项不提供；`DriveObjectNumber` 抛 "Drive object number could not be retrieved"（按序号选）；现有驱动 1 个驱动对象、自由主报文 999（10/10 字，地址 256/160 位，控制器 `["ET 200SP station_1","+S1-K1"]`，`HwIdentifiers` 271，`ModuleAccessPoint` 270，PKW null），新建未联网的驱动无报文、`ModuleAccessPoint.HwIdentifiers` 为空；`DriveFunctionInterface` 提供 `Commissioning` / `SafetyCommissioning` / `HardwareProjection`，`DriveObjectFunctions` / `FunctionInUse` 为 null（无驱动对象类型 / 激活视图）；`DriveObject.Security` 为 null，`TechnologyExtensionContainer` / `DriveControlChartContainer` 不提供，`DriveItemHardwareModule` 在驱动单元项上不提供；`ReadParameters.Find("r47")` 回 200 并带 16 项枚举表，`Find("r722")` 的 `Bits` 有 32 个（`r722.0` …），`Find("r722.0")` / `Find("r19")` 回 null（点名只在 `Bits` 里，G120C 没有 r19），`Find(947, 6)` → `r947[6]`、`Find(96, -1)` → `p96`，枚举按原生顺序从 `r2` 开始；`DriveParameter.Value` 写 11 / 10 立即读回，BICO 汇点 `p1070[0]` 的 `Value` 回 `DriveParameter`（`r2050[1]`），赋源参数对象成功；参数短文本按 TIA 界面语言本地化（zh-CN）。
- SiVArc（2.7.39 真机）：`ManageSivarcTableRule` 的 `ScreenRule.Condition` / `LoopCount` / `Comment` / `Enabled` 写读一致，组内规则建删正常；无 HMI 连接的工程里 `ScreenRule.GetAttribute(<任何设备名>)` 都回 "is not supported by type ScreenRule"（设备列不存在），`SetAttributes` 同样被拒；`ProgramBlock` 赋 null 被拒 "may not be null"；`Sivarc.Generate` 对软件名和设备名都回 "PLC device '…' not found"（HMI 未连接该 PLC）。
- Startdrive / DCC（2.7.39，PublicAPI + 手册核实）：`DriveObjectContainer` / `ModuleAccessPoint` / V21 `DriveItemHardwareModule` / `SafetyAcceptanceTestProvider` 是 `DeviceItemFeature`（`DeviceItem.GetService`），`SafetyAcceptanceTestReport` 是 `DeviceFeature`（`Device.GetService`），`TechnologyExtensionInstallationProvider` 挂在 `TiaPortal` 上；`DriveObject` / `OnlineDriveObject` 是服务提供者（`DriveFunctionInterface` / `OnlineDriveFunctionInterface`、`TechnologyExtensionContainer`、DCC `DriveControlChartContainer`）；`DriveObjectComposition` V20 只有 `IEnumerable<T>` + `Count` + 索引器、V21 是 `IList<T>`；`DriveParameter` 与 `ReadDriveParameter` 成员相同但没有公共基类，`Value` 是 `Object`（BICO 汇点回 `DriveParameter`，写入时赋源参数对象），`EnumValueList` 是 `IDictionary<int,string>`，`Bits` 按名升序而非按位号索引；官方参数名文法 `p|r数字[索引].位`，无数组的参数 `Find(number, -1)`；`Telegram.PKW` 本身是 `Telegram`，改大小后旧 `Telegram` 代理失效；V20 的基接口 `AxisEncoderHardwareConnectionInterface` / `TorqueHardwareConnectionInterface` 直接有 `Connect(Telegram[, MC.Drives.Enums.ConnectOption])`，V21 移到 `AxisEncoderHardwareConnectionSDRInterface` / `TorqueHardwareConnectionSDRInterface`（经 `AxisHardwareConnectionSDRProvider` / `EncoderHardwareConnectionSDRProvider`，后者的 `SensorInterface` 隐藏基类同名属性，反射要 `DeclaredOnly`）并改用 `Motion.ConnectOption`；`HardwareProjection` 在 G120 上要插 Power Module 否则 null / 异常，`SetMotorType` / `SetEncoder` 仅 G120，S120 只离线；`Commissioning.SetMotorCode(int, int)`，`SetSimoGearMlfb` 回 void；`ConfigurationEntry.Number` 是 `UInt32`（`Find(uint)` / `Find(string)`），`Value` 可写；`Security` / `UmacConfiguration` / `DriveDataEncryption` 无状态属性只有 `Activate` / `Deactivate`；DCC：`SetAsPredecessor` 分别声明在 `DccBlock` 与 `DriveControlChart` 上（不在 `Statement`），`Statement.MoveInRuntimeSequence(uint)` / `Delete()`；`DccChartInterfaceComposition` 只有 `Create(DccPin)`（按名查找靠枚举），`DccPin.Publish` 的号 / 索引是 `UInt32`，`DccParameter.Number` / `ArrayIndex` 是可写 `Int32`；`DccConnection` 有 `Sink` 与 `Source`；`DccBlock` 没有块类型属性（类型只在 `Create` 时给）；39 个 `DccException` 子类两版一致，`DccLicenseUnavailableException` 表示缺许可；`DcbLibraryImporter` 仅 V21，挂在 `ProjectLibrary`（服务提供者）上；Startdrive 的 `Upload.Configurations.OverrideTelegramMismatch` / `OverwriteOfflineConfiguration` 仅 V21，派生自 `UploadConfiguration`（不是 `UploadCheckConfiguration`）。
- SiVArc（2.7.38 真机）：`project.GetService<Sivarc>()` 非 null，六族锚点都到达，参考工程没有任何规则表（也没有 `Default screen rule table`）；`ScreenRuleFolderComposition.Create` / `ScreenRuleTableComposition.Create` / `ScreenRuleComposition.Create` / `ScreenRuleGroupComposition.Create` 直接成功（无许可异常）；`ScreenRule` 标量 `Condition` / `Comment` / `LoopCount`（字符串）可写并读回；删除规则 / 组后旧组合代理枚举抛 `EngineeringObjectDisposedException`（对象确已删；同 2.7.30 硬件组合）；`SivarcDataProvider` 在 FB 上可用，`TagMemberSetting.CommonParameters.Name` 回的是 `UseCommonConfiguration` 的字符串值（"False" / "True"），`BlockParameters` 默认为空，`AcquisitionCycle` 接受 "1 s"、`AcquisitionMode` 接受 `CyclicContinuous`；`ExpressionResolver.Resolve`：`Block.Name` → 块号（`FB22`），`Block.SymbolicName` → 块名，`Block.DB.SymbolicName` → 实例 DB 名，`HmiApplication.Type` → "MTP1000 Unified Comfort"，`Block.Comment` 抛 "Source objects not defined or error in expression"；`LayoutData.Export` 在 Unified `HmiScreen` 上写出 71 B 的 YML；`Sivarc.Generate` 的 `plcs` 要 PLC 设备名（`+S1-K1` 报 "PLC device '+S1-K1' not found"），HMI 设备名 `++0.000OP1-KE06` 被接受。
- SiVArc（2.7.38，PublicAPI + SiVArc 手册核实）：`Sivarc` 是工程服务（`project.GetService<Sivarc>()`，实现 `IUpdateProjectScope`，规则表实例更新走 `ProjectLibrary.UpdateProject(types, [sivarc])`），`SivarcDataProvider` 是代码块服务，`SivarcDefinitionsUpgrader` 是 `PlcSoftware` 服务，`LayoutData` 是画面服务（经典 `Screen` 与 Unified `HmiScreen` 都是 `IEngineeringServiceProvider`）；六个规则族没有公共基类，`Rules` 锚点没有 `Name`；`ScreenRuleTable.IsDefault` 只读，实例化的表经 `GetService<LibraryTypeInstanceInfo>().LibraryTypeVersion` 看连接版本；规则组 `ConditionOperator.And` 与不适用规则上的写入会被 TIA 以可恢复异常拒绝；PLC / HMI 设备列是以运行系统名命名的动态属性，变量 / 文本列表规则不支持；V20 用 `ISivarcProgramBlockSource` / `ISivarcDataBlockSource` / `ISivarcLibraryItem` / `ISivarcLibraryMasterCopy` 声明引用属性，V21 改为 `IEngineeringObject`；`TagMemberSetting` / `TagMember` / `AcquisitionMode` / `LayoutData` 仅 V21；`GenerationOptions` 是 `[Flags]`，`None` 表示用工程设置；SiVArc 的官方页面在 SiVArc 手册的文档地图 `TOFaq6RHmuKvo0~qDLnlOg`（`clustered-search` 结果的 `mapId`），不在 Openness 地图里，取内容要用该 mapId。
- 经典 WinCC（2.7.37 真机）：Unified 工程 `AutomaticDipCoatingMachine` 的工程库有 1,038 个类型——`CodeBlockLibraryType` 425、`PlcTypeLibraryType` 454、`HmiUdtLibraryType` 4（经典 HMI 命名空间的子类在 Unified 工程里也会出现）、Unified `ScriptModuleType` 8、其余 147 个 Unified 面板 / 图形的运行时类就是基类 `LibraryType`（Unified 没有自己的面板 / 画面库类型子类）；V21 PublicAPI 共 19 个 `LibraryType` 子类（Step7 3、经典 HMI 8、Unified 1、SiVArc 7、DCC 1）。四个经典 HMI 工具对 `HmiUnified.HmiSoftware` 与 `SW.PlcSoftware` 都在参数门之后、任何写操作之前以 NotSupported 拒绝。
- 经典 WinCC（2.7.37，PublicAPI 核实）：`HmiTarget` 的六个文件夹根都是系统文件夹子类，用户文件夹 `Name` 只读（没有原生改名）；`ScreenSlidein` 只有 `SlideinType` + `Export`（无 Name / Delete，`ScreenSlideinComposition.Find(SlideinType)`）；`ScreenOverview` / `ScreenGlobalElements` 只有 `Export`，导入是 `HmiTarget.ImportScreenOverview / ImportScreenGlobalElements`；`GraphicsProvider`、`ConstValue`、`NullableDateTime` 仅 V21，后两者在 `Siemens.Engineering.WinCC.Extension.dll`（引擎经 NuGet 包的 `*.dll` 通配引用到），`NullableDateTime` 的年月日时分秒是可写 `Nullable<Int32>`、`DateTimeValues` 是粒度枚举；这些值类型不出现在任何 XML 文档的成员签名里，只作为 `Object` 属性的运行时值出现，所以走 `EngineeringScalarProperties.ValueRenderer` 钩子（离线套件里为空）。
- Step7 ④-③（2.7.36 真机）：工艺对象的 `typeIdentifier` 是 `TO_SpeedAxis` / `TO_PositioningAxis`… 这种形式（`System:TO.…` 被 TIA 拒绝 "does not exist or is not a valid technology object"），1510SP F V4.1 + TIA V21 的 TO 版本是 9.0（7.0 / 8.0 都不存在）；`TechnologicalInstanceDBComposition.Create` 后 `Find` 立即可见（没有陈旧代理问题）；新建速度轴 42 个参数、`isConsistent=false`、`number 1`；速度轴 `AxisHardwareConnectionProvider.SensorInterface` 为空、其余 Motion / Ident 服务 `notProvidedByObject`；`Connect(DeviceItem)` 对没有 I/O 地址的 G120C 报 "Target not available for input=-1 and output=-1"；根工艺对象组名本地化“工艺对象”，运行时类是 `TechnologicalInstanceDBGroup`。
- Step7 ④-③（2.7.36，PublicAPI 核实）：`PlcSoftware.TechnologicalObjectGroup` 的声明类型是 `TechnologicalInstanceDBGroup`（不是 `TechnologicalInstanceDBSystemGroup`）；`TechnologicalParameter.Value` 是可写 `Object`（`ManageTechnologyObject setParameter` 按现值类型转换）；`TOMapping` / `DBMemberMapping` / `InterpreterMappings` / `SuperimposingAxes` / `IdentTechnologicalObjectProvider` 仅 V21，V20 反而多 `Connect(Telegram[, ConnectOption])`；`TorqueHardwareConnectionInterface` 没有 `Connect(Channel)` / `Connect(PlcTag)`；`AxisEncoderHardwareConnectionInterface` 七个 `Connect` 重载（Channel / DeviceItem / PlcTag / string DB 成员 / 两模块 [+ ConnectOption] / 两个位地址 + ConnectOption）。
- Step7 ④-②（2.7.35，PublicAPI 核实）：`WatchTableAccessRule` / `ForceTableAccessRule`（命名空间 `Siemens.Engineering.HW`）在 V21 位于 `Siemens.Engineering.Step7.dll`，形状检查要按类型所在程序集解析；`PlcExternalSourceUserGroup.Name` V20 只读、V21 可写；`NamespaceAccessRestriction` 仅 V21；`PlcWatchTable.Entries` / `PlcForceTable.Entries` 都是 `PlcTableCommentEntryComposition`（`Create()` 只建注释行，`PlcWatchTableEntry` / `PlcForceTableEntry` 是其子类）；`PlcAlarmTextListProvider` 同时是 PLC 与单元的服务，`ExportToXlsx` 过滤重载要 `IEnumerable<string>` + `IEnumerable<Language>`；`AlarmClassDataProvider` 官方在 `ProjectBase` 上（引擎先试 PLC 再回退工程）；`PlcExternalSource.GenerateBlocksFromSource` 有四个重载（无参 void、`GenerateBlockOption`、`PlcBlockUserGroup` + 选项、`PlcTypeUserGroup` + 选项，后三个回 `IList<IEngineeringObject>`）。
- Step7 ④-①（2.7.34 真机）：`PlcUnitRelationComposition.Create` 后在同一组合代理上 `Find(relatedObject)` 回 null，`PlcUnitRelation.Delete` 后旧代理 `Find` 抛 `EngineeringObjectDisposedException`（和 2.7.30 的硬件组合一样：变更后要从 owner 重新导航）；`PlcUnitSystemGroup.Name` 按界面语言本地化（"软件单元"），单元里的系统组也是（"程序块" / "PLC 数据类型"）；`PlcType.ExportAsDocuments` 只写一个 `.s7dcl`（168 B 的 UDT）；UDT 文档导入时名字在整个 PLC 内唯一，单元不隔离（"already exists in the plc"）；`ImportFromDocuments Override` 成功后该 UDT 变为不一致、`GetFingerprints` 随即被拒绝，直到编译；参考工程 `PlcChecksumProvider.Software` 为 null（未编译）而 `TextLists` 有值；通道对象在嵌套的同名设备项上（`["+S1-K3","+S1-K3"]`），硬件组件层 0 通道；`PlcTagProvider.GetLinkedTags` 对 DI / F-DI / DQ / AI 都回 1 个变量并带 `Parent` 变量表；V21 `Project.GetService<PlcSimulationSettingsProvider>()` / `<VirtualPlcSettingsProvider>()` 在参考工程都非 null。
- Step7 ④-①（2.7.34，PublicAPI 核实）：V21 的 `Project` **没有** `IsSimulationDuringBlockCompilationEnabled` / `IsVirtualPlcDuringBlockCompilationEnabled`（官方 "Updating project properties" 仍按属性举例），只剩 `Project.GetService<PlcSimulationSettingsProvider>()` / `<VirtualPlcSettingsProvider>()`；`Channel` / `Address` 在 V20 不实现 `IEngineeringServiceProvider`（`PlcTagProvider` / `ProcessImageProvider` 是 V21 才有的通道 / 地址服务，V21 同时删掉了 `Address.AssignProcessImageToOrganizationBlock`）；`PlcUnitSystemGroup.Name` 与 `PlcDocumentComposition.CreateFrom(MasterCopy / PlcDocumentLibraryTypeVersion)` 仅 V21，而 `PlcTypeComposition.CreateFrom` 与 `PlcUnitComposition.CreateFrom` 两版都有；`PlcDocument` 在 PublicAPI 只有 `Name` + `ExportAsDocuments`；`PlcSafetyUnit` 没有 `Delete`、`PlcSafetyUnitComposition` 没有 `Create`；`PlcBlockWriteProtectionProvider : ProtectionProviderBase`（`GetInvalidPasswordCharacters` 可用）；`DocumentExportResult.ExportedDocuments` 是 `IEnumerable<FileInfo>`；`FingerprintProvider.GetFingerprints()` 返回 `IList<Fingerprint>`（`Id : FingerprintId` 12 值）。
- Base 收尾（2.7.33，PublicAPI 核实）：`ConfigurationAddress` 没有公开构造函数，只能从路由树（`TargetInterfaces[].Addresses`）里取；`RHOnlineProvider.GoOnlineToPrimary/Backup(ConfigurationAddress)` 与 `Device : IShowable`、`ProjectBase.TextCategories`、`DefaultWebPagesFeature` / `WebApplicationConfiguration`、`TelecontrolDataPoint*`、`OnlinePasswordConfiguration.IsSecureCommunication`、`CertificateSupportedService.ApplicationUri / Guid` 都是 V21 才有（V20 的 `CertificateSupportedService.Id` 是只读 `UInt16`，`CertificateSupportedServiceName` 只有 `OpcUaServer` / `Webserver`）；`CertificateSupportedServiceComposition.Create(id, serviceGroupName, serviceType)` 三参；`IShowable` 在 Step7 里由 `PlcBlock` 全家、`PlcType` / `PlcStruct`、`PlcTagTable`、`PlcWatchTable` / `PlcForceTable`、`TechnologicalInstanceDB` 实现；`AttributeConfiguration` 只在 `SetAttributes(pairs, AttributeDelegate)` 的回调里出现（子类 `AttributeNameUnsupported` / `AttributeReadOnly` / `AttributeTypeUnsupported` / `AttributeValueUnsupported`，`CurrentSelection` Abort / Ignore）；`TableData` / `StructuredData` 没有公开的拥有者属性，只作为 `GetAttribute` 的值出现；`ExceptionMessageData` 挂在每个 `EngineeringException.MessageData / DetailMessageData` 上；`UmacDelegate` 是 `Projects.Open/OpenWithUpgrade/Retrieve(file, UmacDelegate[, ProjectOpenMode])` 的凭据回调，`TiaPortal.Authentication` 事件是另一条路。
- 安全 / UMC（2.7.32 真机）：`SyslogServerProvider` 在工程上可用（0 台服务器），但 `Servers.Create(name)` 让 TIA Portal V21 崩溃；CPU `+S1-K1`（1510SP F V4.1）的 `SysLogConfigurationManager` 可读（`EnableSystemLogging=false`、`TransportProtocol=None`、三个动态属性可读）；`UmcServerConfigurator.CheckConsistency()` 不带凭据时 TIA 抛 `EngineeringTargetInvocationException` "凭证无效"，`UmcServer` 没有属性信息；`CreateOfflineUmcUserGroup()` 的原生名是 `Group_1`；offline 用户 / 组的 `DomainId` 为空串、创建即 `IsActive=true`；系统角色 `Engineering administrator` 与自定义角色都能挂到 UMC 用户 / 组；`UmacDevice` 服务在设备 `ET 200SP station_1` 上（30 个系统设备功能权，`comment` 全空），不在 CPU 项；证书模板默认值：Tls CN `S1-K1/Tls-2` + SAN `IP 192.168.0.1`，WebServer CN `S1-K1/Webserver-2` + SAN IP/Dns，有效期到 2037；三套密码策略在本工程都可用（PLC `PasswordPolicyEnabled=true`）。
- 安全 / UMC（2.7.32，PublicAPI 核实）：`Security` 与 `Umac` 的功能成员 V20/V21 完全一致（V21 只多 `.Any()`）；`SyslogServerProvider` 是工程服务（`project.GetService`），`SyslogServer.Port` 是 `Int64`，`AssignedModules` 是 `DeviceItemAssociation`（有 `Add` / `Remove` / `Contains`）；`SysLogTransportProtocolType` 在 `Siemens.Engineering.HW` 而非 `.HW.Features`，`SysLogServerConfigurationComposition.Create(address, UInt16 port)`；`UmcUserGroupComposition.CreateOfflineUmcUserGroup()` 不带名字（之后 `SetName`），`UmcUser` 派生自 `User`（`Name` / `Roles`），`UmcUserGroup` 不是（自带 `Name` / `Roles` / `Description`）；`UmcServer` 没有公开标量，`Authentication` 是 `EventHandler<UmcAuthenticationEventArgs>`，凭据写进 `e.UmcCredentials.Name` / `SetPassword`；`CustomDeviceFunctionRight.Comment` 只读（只能在 `Create(name, group, comment)` 时给）；`CertificateTemplate` 五个标量全可写，`SubjectAlternativeName.Type / Value` 只读（删了重建）；`Certificate.Id` 是 `UInt32`；`PasswordPolicyConfigurator` 的数值属性都是 `Int16`。
- 库（2.7.31 真机）：`ProjectLibrary` 没有 `Name`（只有 `MasterCopyFolder` / `Parent` / `TypeFolder`）；`SystemGlobalLibrary.TypeFolder` 为 null（系统库只有母版，`Buttons-and-Switches` 的母版树在 `FunctionButtons/Rectangular` 等文件夹）；对系统库调 `UpdateCheck` 抛 `NonRecoverableException`（会被连接保护当作断连，需要 `AttachToOpenProject`，且打开的全局库句柄随之丢失）；`Close` / `Save` / `SaveAs` / `Archive` / `CleanUpLibrary` 只在 `UserGlobalLibrary` 上，`Open(GlobalLibraryInfo)` 打开的系统库无法经 API 关闭；`GetGlobalLibraryInfos` 在虚拟机上列出 4 个系统库（Buttons-and-Switches / Long Functions / Monitoring-and-control-objects / Documentation templates），没有用户全局库。工程库顶层 26 个类型文件夹（PLC / LGF / LBC / _SICAR_Types_PLC / Open Library V19 / LUC / LSicar / LAF / LAxisCtrl / LSafe / _LibrarySafety / LPD / … / LSyslog），`LGF/00 Bit logic operations` 下 7 个 `CodeBlockLibraryType`（`LGF_BitSet` 3.0.1 等，`originalLibrary=LGF_V5_4_0_TiaLib_V21`）可作比较/读取样本。`DetailedCompareResult` 的 `Original Library` 行状态是 `NotCompared`。
