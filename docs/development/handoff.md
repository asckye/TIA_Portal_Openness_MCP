# 接续工作交接（2.7.37 之后，2026-09-19）

[文档目录](../README.md) · [路线图 §2.0](roadmap.md#20-官方-api-全量对齐计划) · [发布流程](release-workflow.md) · [覆盖清单](../reference/openness-coverage.md)

换机器继续"官方 Openness API 全量对齐"计划时先读这一页。它记录**当前停在哪**、**下一步做什么**、**每个阶段的固定动作**、**发布闸门**和**只在真机上学到的 API 事实**——这些都不在代码里，也不在提交历史里。

## 1. 现状（2.7.37 已发布并真机验证；阶段 5 收口，核心程序集缺口全部归零）

- 最新 tag `v2.7.37`（2026-09-19，本机构建并发布，`Publish complete release` 已上传 ZIP，同日真机验证——Unified 拒绝、`typeKind`、钩子无回归；上一个 `v2.7.36` 同日，已真机验证）。
- 工具 413 个、默认 lite 56；离线套件 1881 项；引擎 API 形状检查 V20 1957 / V21 2140（`Build-Release.ps1` 第 73 行硬编码这两个数）。
- 审计（V21）：类型 1,215（分母去掉 internal 的 `Compiler.CompileProvider` 与 `Private.ProcessHelper`，审计脚本 2.7.33 起排除）= 专用引用 505 / 仅类型名 61 / 动态覆盖 327 / 完全未触及 322；未封装功能类型 107 / 515（**Base 0 / 0、Step7 0 / 0、经典 WinCC 0 / 0、`WinCC.Extension` 0 / 0**，选件包 107 / 515）。**阶段 5 收口，核心程序集全部归零。**
- 阶段 5 经典 WinCC（2.7.37，真机已验其 Unified 拒绝路径与 `typeKind`；经典实做路径无工程可验）：`ReadClassicHmiScreenTree`、`ManageClassicHmiScreenObject`、`ManageClassicHmiFolder`、`ManageClassicHmiGraphic` + `ReadClassicHmiScripts` / `ManageClassicHmiScript` / `ReadLibraryType typeKind` / `EngineeringScalarProperties.ValueRenderer`（`ConstValue` / `NullableDateTime`）类型化。文件：`Siemens/ClassicHmiFoldersLogic.cs`、`Siemens/Portal/Portal.ClassicHmiFolders.cs`、`ModelContextProtocol/Tools/McpServer.ClassicHmiFolders.cs`、`tests/.../ClassicHmiFoldersTests.cs`、`tests/.../ClassicHmiFoldersShapeChecks.cs`。
- 阶段 4 ④-③ 工艺对象映射（2.7.36，**真机验证通过** 2026-09-19，临时 `TO_SpeedAxis` V9.0）：`ReadTechnologyObjectTree` + `ReadMotionAxisConfiguration` / `ManageMotionAxis` / `ManageTechnologyObject` / `ConfigureMotionHardwareConnection` 类型化改造（`Portal.TechnologyMapping.cs` 的类型化行、`TypedMotionView`、`ConnectTyped` / `DisconnectTyped`；`targetJson` 新增 `channelType` / `channelIoType` / `channelNumber`）；另修 2.7.35 真机的注释行计数缺陷。文件：`Siemens/Portal/Portal.TechnologyMapping.cs`、`ModelContextProtocol/Tools/McpServer.TechnologyMapping.cs`、`tests/.../TechnologyMappingTests.cs`、`tests/.../TechnologyMappingShapeChecks.cs`。
- 阶段 4 ④-② Step7 收尾（2.7.35，**真机验证通过** 2026-09-19；注释行计数缺陷已在 2.7.36 修）：`ManagePlcExternalSources`、`ReadPlcSystemGroups`、`ReadPlcTagTableConstants`、`ExchangePlcAlarmTextListsXlsx`、`ManagePlcTableEntries`、`ExportPlcProDiagInfo` + 类型化改造（`ManageWatchForceTableWebAccess` 访问规则、OPC UA `OpcUaCommunicationGroup` / `NamespaceAccessRestriction`、`ExportAlarmClasses` / `ImportAlarmClasses` 消息、`ManagePlcSupervision` 设置消息、`ReadLibraryType typeKind`、`ManagePlcUserGroup` 新增 `watchTables` / `externalSources`）；另修 2.7.34 真机的关系回读缺陷。文件：`Siemens/Step7LeftoversLogic.cs`、`Siemens/Portal/Portal.Step7Leftovers.cs`、`ModelContextProtocol/Tools/McpServer.Step7Leftovers.cs`、`tests/.../Step7LeftoversTests.cs`、`tests/.../Step7LeftoversShapeChecks.cs`。
- 阶段 4 ④-① Step7 软件单元与 `PlcSoftware` 小服务（2.7.34，**真机验证通过** 2026-09-19，结果见 `docs/releases/v2.7.34.md#真机结果`；关系回读缺陷已在 2.7.35 修）：`ReadPlcSoftwareUnits`、`ManagePlcDocuments`（命名值类型文档 + UDT 文档导入导出 / 母本 / 库类型实例化）、`ReadPlcChecksums`、`ReadPlcObjectFingerprints`、`ManagePlcBlockWriteProtection`（V21）、`ManageProjectCompilationSettings` + `ManagePlcSoftwareUnit`（`unitKind=safety` / `createFromMasterCopy` / `commentsJson`）、`ReadDeviceItemChannels includeLinkedTags`（V21 `PlcTagProvider`）、`UpdateDeviceAddress`（V21 `ProcessImageProvider`）、类型化 `DocumentImportResultForBlocks` 扩展；另修 2.7.33 真机的事务缺陷（`ForceRealExecution` 无键也写 `dryRun=false`）。文件：`Siemens/SoftwareUnitDeepLogic.cs`、`Siemens/Portal/Portal.SoftwareUnitDeep.cs`、`ModelContextProtocol/Tools/McpServer.SoftwareUnitDeep.cs`、`tests/.../SoftwareUnitDeepTests.cs`、`tests/.../SoftwareUnitDeepShapeChecks.cs`。
- 阶段 3 ③-④ Base 收尾（2.7.33）：`ReadPortalInfo`、`ReadTransferRoutes`、`ManageHardwareUtilities`、`ManageDeviceServiceObjects`、`ReadObjectIdentifier`、`ShowObjectInEditor`、`RunToolsInTransaction` + `OpenProject` UMAC 凭据、`GoOnline` 用户认证 / R/H、`DownloadToPlc` R/H、类型化的传输提示 / 结果 / 属性批量写 / 交叉引用 / 比较 / 目录 / 多用户 / 版本控制 / 设置；2.7.32 真机的五处问题已修（工程级 syslog `create` 只预览、证书 `template` 门、`UmacDevice` 文案、`CheckLibraryUpdates parts`、多实例改绑守卫）。**2.7.33 真机重跑已于 2026-09-19 完成**（`docs/releases/v2.7.33.md#真机结果`）：五处修复、门户诊断、传输路由、硬件工具、设备服务对象、对象标识往返、事务提交与回滚、类型化交叉引用 / 目录行全部通过；**一处缺陷**：`RunToolsInTransaction` 内层调用不带 `dryRun` 时按预览跑却回报 `committed=true`（源码已在本机修好、未发布，见 §2 第 1 步）。
- 阶段 3 ③-③ 用户管理与安全（2.7.32，真机验证通过）：`ManageSyslogServers`（工程级 `SyslogServerProvider` + CPU `SysLogConfigurationManager`）、`ManagePasswordPolicy`（UMAC / PLC / legacy PLC 三套）、`ManageUmcUsers`（offline 与服务器导入的 UMC 用户 / 组、`UmcServerConfigurator`）、`ManagePlcCertificate` 的 `template` / SAN / 密码导入、`ManageProjectUserManagement` 的匿名用户激活；2.7.31 真机的四处库缺陷已修（`LibraryRef` 标签、null `TypeFolder` 守卫、`UpdateCheck` 预拒绝、用户库限定文案）。**2.7.32 真机重跑已于 2026-09-18 完成**：库缺陷修复、`CompareLibraries`（首次真机到达，登记表已回写）、密码策略、CPU 级 syslog 读取、offline UMC 用户 / 组、证书模板 + SAN、匿名用户预览、设备功能权行全部通过；**两处问题**：工程级 `SyslogServerComposition.Create` 让 TIA Portal 崩溃（两次复现）、`ManagePlcCertificate template` 误要求 `certificateId`。记录见 `docs/releases/v2.7.32.md#真机结果`。
- 阶段 3 ③-② 库深层（2.7.31）：`ReadLibraryOverview` / `ReadLibraryType` / `ManageLibraryType` / `CheckLibraryUpdates` / `SynchronizeLibrary` / `CompareLibraryObjects` + `ManageGlobalLibrary`（infos / openInfo / archive）与 `ImportLibraryTypeDocuments`（版本导入）扩展。**2.7.31 真机重跑已于 2026-09-18 完成**：2.7.30 的三处修复（组合刷新、就地捕获已释放代理、通道 accessMode）全部验证通过；全局库头/母版树、`ReadLibraryType` 三种入口、`ManageLibraryType` 预览、`CompareLibraryObjects` 三种对象、`ManageGlobalLibrary infos/openInfo` 通过。**暴露 4 处要在 2.7.32 修的问题**：`ProjectLibrary` 没有 `Name`（工程库上的 `ReadLibraryOverview` / `CheckLibraryUpdates` / `SynchronizeLibrary` 与原有 `CompareLibraries` 全部在读头时失败）；`SystemGlobalLibrary.TypeFolder` 为 null 未守卫（NRE）；`UpdateCheck` 对系统库触发 TIA `NonRecoverableException` 并被连接保护当作断连；`close` 对系统库报 "not found" 而非说明 API 没有 `Close`。记录见 `docs/releases/v2.7.31.md#真机结果`。
- 阶段 3 ③-① 硬件网络深层（2.7.30）：13 个强类型工具（`ReadIoSystems` / `ManageIoSystem`、`ReadNetworkDomains` / `ManageNetworkDomain`、`ReadTransferAreas` / `ManageTransferArea`、`ReadDeviceItemChannels` / `UpdateDeviceItemChannel`、`ReadDeviceAddressing` / `UpdateDeviceAddress`、`ManageDeviceUserGroup`、`ManageDeviceUsers`、`ManagePortInterconnection`）。**2.7.30 真机重跑已于 2026-09-18 完成**：全部到达真实对象，读取与可复原写入通过；暴露两处引擎缺陷（域组合代理在 Create/Delete 后陈旧；`EngineeringObjectDisposedException` 误触发连接失败保护）与一处改进（通道属性名缺 AccessMode），记录见 `docs/releases/v2.7.30.md#真机结果`。
- 阶段 1 Safety（2.7.25）、阶段 2 WinCC Unified（2.7.26–2.7.29）已收口。**2.7.29 真机重跑已于 2026-09-18 完成**（变量导出核对通过；文本/系统文本列表读取与原生导出、`Validate`、画面组建删通过），登记表 `scripts/diagnostics/openness-dynamic-coverage.json` 已回写；仍未真机验证的只剩 `Cpm.*` 与 `HmiConnections.*`（工程无对象）以及 `HmiOpcUaAlarm` / `LoggingTags` 的 `Create`（组合到达但为空）。记录见 `docs/releases/v2.7.29.md#验证`。

## 2. 下一步（按顺序）

1. ~~发布 2.7.37~~ **2026-09-19 完成**（`validate-bundle` / `offline-checks` / `Publish complete release` 全绿；GitHub API 从本机限流时用 `https://github.com/<repo>/actions/workflows/<wf>.yml` 页面的 `aria-label` 抓状态）。
2. ~~部署 2.7.37 到虚拟机并真机验证~~ **2026-09-19 完成**（结果在 `docs/releases/v2.7.37.md#真机结果`）：四个新工具对 Unified / PLC 目标明确拒绝；`typeKind` 在 1,038 个工程库类型上核对；`ConstValue` / `NullableDateTime` 在 Unified 里不出现。**2.7.38 顺手补**：`Portal.LibraryDeep.cs` 的 `LibraryTypeKind` 加 `Hmi.Faceplate.FaceplateLibraryType` → `hmiFaceplate`、`Hmi.RuntimeScripting.VBScriptLibraryType` / `CScriptLibraryType` → `hmiVbScript` / `hmiCScript`、`HmiUnified.Library.ScriptModuleType` → `unifiedScriptModule`（V20 要核实这些类在 `Siemens.Engineering.Hmi.dll` 里是否存在，否则 `#if`），SiVArc 7 个规则表类型与 DCC `DcbBlockTypeLibraryType` 随阶段 6 对应选件包一起命名。未验证项仍留：所有经典 WinCC 实做路径（没有经典 HMI 工程）、R/H、`GoOnline userName`、`OpenProject` UMAC 凭据、`CompareProjects rootElement`、`exportCardReaderPsc` 实做、双实例改绑拒绝、安全单元本体、单元 / 文档 / 外部源母本、`.nvt` 文档、真实 XLSX 导出导入、ProDiag 实做、带地址驱动的硬件连接 / 主值耦合 / 解释器映射 / Ident；虚拟机桌面的 `mcp2733-opcua.xml`（20 MB）、`LDiag_typeFault.s7dcl`、`MCP_TMP_FC_2735.scl`、`mcp2735-alarmclasses.dat` 可删。
3. **阶段 6 选件包（只做形状检查，2.7.38 起）**：107 类型 / 515 成员——SiVArc 33 / 198、Startdrive 34 / 117（含 `*SDR*` 硬件连接接口）、DCC 14 / 69（另有 39 个异常类是壳子）、SafetyValidation 9 / 43、TestSuite 9 / 44、Teamcenter 7 / 37、CFC 1 / 7。虚拟机没有这些选件的许可，路线图约定：每个选件包一份 `*ShapeChecks.cs` 逐成员核对 + 一个只读 / 预览工具族（服务不可用时明确 NotSupported），不宣称真机验证。先跑审计拿清单：`bin-build/audits/<名>/V21-types.csv` 里 `assembly` 属于选件包且 `status=UNTOUCHED`，按壳子规则过滤。
4. ~~阶段 5 经典 WinCC（`Siemens.Engineering.WinCC.dll`）~~ **2.7.37 完成，功能类型缺口 0 / 0**（官方页面 id：弹出画面 `Bs8q~5oMGOhyPiULc5EjXg` / `WfkApIgS2OeHao78qoNfYQ`、滑入 `t9yZaXoGpzKIvND1XMsAKQ` / `g0G3yI1viTlSPt3pddPr_A`、模板 `~xq~afYOxwUtTqH3V1I_Tw` / `lOIf7qsktx_jISESVA846Q`、变量文件夹 `NjqFj5DVnfDkwuPfn6k8Xg` / `nda76ZAvfbuD9koVDoGIJQ`、图形 `txSvxCRTH2TMhIO~7G8jLw`、VB 脚本 `jA1DvFRkXxu0ll1Btu5hRw` / `iQJjjjTJbZJl4VkE_aN_Bw`）。原清单：`Hmi.Screen` 17 / 38（`ScreenPopup(Folder/SystemFolder/UserFolder)`、`ScreenSlidein(SystemFolder)`、`ScreenTemplate(Folder/SystemFolder/UserFolder)`、`ScreenSystemFolder` / `ScreenUserFolder`、`ScreenGlobalElements` / `ScreenOverview`、`ScreenLibraryType` / `StyleLibraryType` / `StyleSheetLibraryType`）、`Hmi.RuntimeScripting` 3 / 10（`VBScript`、`VBScriptSystemFolder` / `VBScriptUserFolder`）、`Hmi.Tag` 3 / 8（`TagSystemFolder` / `TagUserFolder`、`HmiUdtLibraryType`）、`Hmi.Globalization` 1 / 3（`MultiLingualGraphic`）。参考工程的 HMI 是 Unified（`HMI_RT_1`），经典 WinCC 只能形状检查 + 现有 `Portal.ClassicHmi*` 的类型化改造；先看 `docs/reference/openness-coverage.md` 的经典 WinCC 行与 `Portal.MotionProDiagClassicHmi.cs` 里已有的经典 HMI 工具（`ManageClassicHmiScripts` / `ManageClassicHmiCycles` / `ManageClassicHmiTextLists`…）。
5. ~~阶段 4 Step7（`Siemens.Engineering.Step7.dll`）~~ **2.7.34–2.7.36 完成，功能类型缺口 0 / 0**：
   - ~~④-① 软件单元 + `PlcDocument*` + `PlcChecksumProvider` / `PlcSimulationSettingsProvider` / `PlcBlockWriteProtectionProvider`~~ **2.7.34 完成**（官方页面 id：软件单元 `0h0oxLVauEUie7gZF4QPWw` / `cRardLf5JWn6RV4GwzVX2Q` / `1XBGooV5zT1c4pMm~pUcDQ` / `R3UTOiWAisuD36RmvULj9Q` / `JT6mLOEcCLGxh5bzCM6sxw`、安全单元 `nRkWKOSJ40n_TTB9pS3iEw` / `UXq9Lu_poeISbbKKJiZn7Q`、NVT 文档 `pi7xDzfb1aKRSwG4KIQP3g`、UDT 文档 `H_AHIQyhM0nIyyGLbQm7fQ` / `f7I~jdKwF~pGMltuKUvtAQ`、校验和 `4oWqygP07f_Pxz7N2LEwBw`、指纹 `CU2HzxWdQiVq8MOz7QkvNw`、写保护 `4qvX8TvoaH0t9f7LiQc_3g`、工程属性 `~zGrGn3wwJUHBCn6pO5Cjg`）。
   - ~~④-② Step7 收尾（外部源、系统组、常量、报警文本列表 XLSX、表条目、ProDiag、类型化改造）~~ **2.7.35 完成**（官方页面 id：外部源 `w7zZEXJa7dPksEDThvBSLw` / `PweFezJaXiy8nXiDWOWjhA` / `LLfdxabkgrjwzUfgkUOObw`、系统组 `uD8mauDSD~GqtowlNv9_7w` / `hyeOAzoyHSUcDfnBMNypGg`、文本列表 XLSX `Uj~HCXBceURnWIxp6Ea7oQ`、报警类 `uIFhxfIuwL~guvUnJIVf0Q`、ProDiag `EJJOqxC6VzWw3z5NX7RJjw`、OPC UA 接口 `y66Vhyx3jKO68WEVRckErQ`）。
   - ~~④-③ 工艺对象映射~~ **2.7.36 完成**（`Portal.TechnologyMapping.cs`；官方 Openness 地图的 Motion Control 章节不在文档站的 `clustered-search` 索引里，依据是 `Siemens.Engineering.Step7.xml` 摘要）。

剩余缺口一览（2.7.37 审计，未封装功能类型 / 成员；壳子类型不计）：

| 层次 | 剩余 | 内容 |
|---|---|---|
| 核心 | 0 / 0 | Base 0 / 0、Step7 0 / 0、经典 WinCC 0 / 0 |
| ├ ~~③-④ Base 收尾~~ | 0 / 0 | `Siemens.Engineering` 根命名空间 10 / 28（`TiaPortalSession` / `Transaction`、`TiaPortalProduct`、`AttributeConfiguration`、`UmacCredentials`…）、Download / Upload / Online / Compiler 结果消息与配置 17 / 41、Connection 3 / 7、Multiuser 3 / 7、Settings 2 / 5、VersionControl 2 / 2、Compare 1 / 5、CrossReference 2 / 15、HW 零头 13 / 40 |
| ├ ~~④-① 软件单元与 PlcSoftware 小服务~~ | 0 / 0 | 2.7.34 封装：`SW.Units` 3 / 18、`SW` 11 / 14、`PlcBlockWriteProtectionProvider` 1 / 6 |
| ├ ~~④-② Step7 收尾~~ | 0 / 0 | 2.7.35 封装：`SW.ExternalSources` 4 / 13、`SW.Blocks` 3 / 5、`SW.Types` 4 / 6、`SW.Tags` 2 / 6、报警 / 监控结果与 `PlcAlarmTextListProvider` 3 / 6、OPC UA 2 / 7、表规则 / 条目 5 / 15、`TechnologicalInstanceDBUserGroup` 1 / 2 |
| ├ ~~④-③ 工艺对象映射~~ | 0 / 0 | 2.7.36 封装：`TOMapping` 3、`AxisEncoderHardwareConnectionInterface` 19、`TorqueHardwareConnectionInterface` 14、`TechnologicalInstanceDBGroup` 4、`TechnologicalParameter` 2 |
| └ ~~阶段 5 经典 WinCC~~ | 0 / 0 | 2.7.37 封装：`Hmi.Screen` 17 / 38、`Hmi.RuntimeScripting` 3 / 10、`Hmi.Tag` 3 / 8、`Hmi.Globalization` 1 / 3 |
| ~~WinCC.Extension~~ | 0 / 0 | 2.7.37：`ConstValue`、`NullableDateTime` 经值渲染钩子 |
| 选件包（只做形状检查） | 107 / 515 | SiVArc 33 / 198、Startdrive 34 / 117、DCC 14 / 69、SafetyValidation 9 / 43、TestSuite 9 / 44、Teamcenter 7 / 37、CFC 1 / 7 |
| **合计** | **107 / 515** | 2.7.36 为 133 / 585，2.7.35 为 139 / 637，2.7.34 为 162 / 695，2.7.33 为 178 / 735，2.7.32 为 230 / 896，2.7.31 为 239 / 929，2.7.30 为 253 / 1,006，2.7.29 为 267 / 1,067 |

## 3. 每个阶段的固定动作

| 步骤 | 位置 / 命令 |
|---|---|
| 纯逻辑（无 Siemens 依赖）：参数门控、JSON 拆分、路径规则 | `tools/tiaportal-mcp/src/TiaMcpServer/Siemens/<Family>Logic.cs`（Unified 的放 `Siemens/Hmi/`）；把它加进 `tests/TiaMcpServer.Tests/TiaMcpServer.Tests.csproj` 的 `<Compile Include=…>` |
| 门户实现 | `Siemens/Portal/Portal.<Family>.cs`，用 `RunHmiStepTool(name, meta => {...}, requiresProject)`、`ExactPlcForEngineering`、`ResolvePlcService<T>`、`AcquireHmiEditAccess()`、`EngineeringScalarProperties`、`EngineeringGroupOperations`、`EngineeringObjectAddress`（属性路径 JSON `[{property,name}]`）、`NativeFileOutput.Plan/Verify`、`PortalException(PortalErrorCode.*)`；V20/V21 差异用 `#if TIA_V20` |
| 工具注册 | `ModelContextProtocol/Tools/McpServer.<Family>.cs`：`[McpServerTool(Name=...), Description("[L2][Domain][OP] ...")]`；描述里点名的其他工具必须真实存在（`scripts/checks/Check-DeadToolReferences.py`，例外名加进其 `ALLOWED`） |
| 离线测试 | `tests/TiaMcpServer.Tests/<Family>Tests.cs`，在 `Program.cs` 注册；`dotnet run -c Release` 于该目录。**不能用 Windows 盘符字面量**（CI 在 ubuntu 上跑），要 `Path.Combine(Path.GetTempPath(), …)` |
| 引擎 API 形状检查（逐成员核对 PublicAPI） | `tests/TiaMcpServer.HttpTests/<Family>ShapeChecks.cs`，在其 `Program.cs` 的 `engineering-api-only` 分支注册；跑 `tests/TiaMcpServer.HttpTests/bin/Release/net48/HttpTests.exe <引擎exe> engineering-api-only <PublicAPI目录>`，两版都跑，把 `COMPLETE: N` 写回 `scripts/build/Build-Release.ps1` 第 73 行 `if($major -eq 21){…}else{…}` |
| 覆盖审计回写 | `.\scripts\diagnostics\Audit-OpennessCoverage.ps1 -PublicApiDirectory <V21\net48> -Version V21 -OutputDirectory .\bin-build\audits\<名> -SummaryMarkdown .\bin-build\audits\<名>\V21-summary.md`；把统计表与"动态覆盖的类型"段整段替换进 `docs/reference/openness-coverage.md`，更新标题/缺口结构段的数字。"未封装功能类型"= `V21-types.csv` 里 `status=UNTOUCHED` 且类型名不以 `Composition/Association/EventArgs/Exception/Result/Info` 结尾者，按程序集求和 |
| 词法审计的坑 | 审计只看"类型名字面量 + `.成员(`"是否同时出现：链式调用 `umac.UmcUsers.Create(...)` 不算引用 `UmcUserComposition`，要写成类型化局部变量 `UmcUserComposition users = umac.UmcUsers;`；`Siemens.Engineering.Private.*` 与 `Compiler.CompileProvider` 是 internal，2.7.33 起审计脚本从分母排除；命名空间 `TiaMcpServer.Siemens` 会遮住根 `Siemens`，在 Portal 文件里写全名要用 `global::Siemens.Engineering...` |
| 动态覆盖登记 | 反射/泛型到达、类型名不出现在源码里的类型，登记进 `scripts/diagnostics/openness-dynamic-coverage.json`（`pattern` / `tool` / `mechanism` / `verified`）；`verified` 必须如实写真机证据或 "shape checks only" |
| 文档 | `CHANGELOG.md`、`docs/releases/vX.Y.Z.md`、`docs/development/roadmap.md`（§2.0 表 + §5 已完成段，编号顺延）、`docs/reference/capabilities.md`、`docs/reference/openness-coverage.md`、`docs/README.md` 当前发布链接；工具数手改 `README.md` / `README.zh-CN.md` / `tools/tiaportal-mcp/skill/SKILL.md`（`tool-matrix.md` 与 `manifest/*` 由构建生成） |
| 版本号 | 两个 `.csproj`（`AssemblyVersion` / `FileVersion` / `InformationalVersion`）、`tools/mcp-configurator/Configurator.cs`、`.claude-plugin/plugin.json` |

## 4. 构建与发布闸门（新机器要先满足）

- 需要：Windows、.NET SDK 8 或 10（desktop-ivrlcht 用 10.0.401，net48 目标包正常）、Python 3.10+、Git，以及**放在仓库根的 PublicAPI 副本**（Siemens 授权组件，不可分发，`.gitignore` 已忽略这两个目录名）：`TIA_V20_PublicAPI\V20`（2000.4.401.2）与 `TIA_V21_PublicAPI\V21\net48`（2100.0.121.1，含 `Siemens.Engineering.Safety.dll`）。不需要安装 TIA Portal。
- `dotnet build … -p:SiemensEngineeringDirectory=D:\…` 要在 PowerShell 里跑：Git Bash 会改写反斜杠路径，Openness NuGet 的 targets 找不到目录就回落到 "Package" 解析，结果是 456 个 CS0246。
- 统一构建：
  ```powershell
  powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build/Build-Release.ps1 -V20ReferenceRoot <repo>\TIA_V20_PublicAPI\V20 -V21ReferenceRoot <repo>\TIA_V21_PublicAPI\V21\net48 -Python <python.exe 完整路径>
  ```
  约 4–15 分钟；它重建两版引擎、跑离线套件与形状检查、重建 `TiaMcpConfigurator.exe`、重生成 `manifest/*.json` 与 `docs/reference/tool-matrix.md`。若后台运行并用 `*>` 记日志，日志是 UTF-16——用 `tr -d '\0'` 再 grep。**构建期间不要再改 `src` / `tests` 里的任何文件**：源码哈希在最后一步（`delivery.log` 的 Validate-Bundle）才算，中途改一行就 "Source changed after validation"，整轮白跑（2.7.30 第一轮就是这样）。
- 手工单版构建（调试用）：V21 `dotnet build tools/tiaportal-mcp/src/TiaMcpServer/TiaMcpServer.V21.csproj -c Release -p:SiemensEngineeringDirectory=<V21\net48>`；V20 需另给 `-p:SiemensEngineeringDirectory=<V20> -p:BaseIntermediateOutputPath=<src>\obj-v20\ -p:MSBuildProjectExtensionsPath=<src>\obj-v20\`（绝对路径、以 `\` 结尾；在 PowerShell 里跑，Git Bash 会把尾部反斜杠吃掉）。
- **哈希闸门**：`manifest/release-build.json` 记录 `tools/tiaportal-mcp/src` 与 `tests`（含离线测试 .cs）的源码哈希；任何改动后不重跑 Build-Release，`validate-bundle` CI 与 `Package-Release.py` 都会报 "Source changed after validation"。不要手改哈希。只改文档/登记表不触发。
- 提交模式：`Release X.Y.Z (1/3)` 源码 + 版本 + CHANGELOG + 发布说明 + 文档；`(2/3)` `runtime/v20/TiaMcpServer.exe`；`(3/3)` `runtime/v21/TiaMcpServer.exe` + `TiaMcpConfigurator.exe` + `manifest/*` + `tool-matrix.md`。然后 `python scripts/build/Package-Release.py --git "<git.exe>"` 本地干跑，`git push origin master`，再推注解 tag `vX.Y.Z` → "Publish complete release" 工作流上传 ZIP。
- 不要 `git add -A`：仓库根可能有本地大压缩包。提交、PR、Release 正文**不加任何 AI 署名行**（维护者明确要求）。
- 本地等价于 CI 的检查：`validate` 工作流 = `python scripts/checks/Check-Repository.py` + `./scripts/checks/Validate-Bundle.ps1 -Strict`（PowerShell）+ `Build-Configurator.ps1 -Test`；`offline-checks` = `dotnet run --project tools/tiaportal-mcp/tests/TiaMcpServer.Tests/TiaMcpServer.Tests.csproj -c Release` + `python scripts/checks/Check-DeadToolReferences.py`。推 master 前四个都过；推 tag 前确认两条工作流已绿。
- CI 状态无 `gh` 时：`curl -s https://api.github.com/repos/asckye/TIA_Portal_Openness_MCP/actions/runs?per_page=6`；发布产物：`.../releases/tags/vX.Y.Z`（ZIP + .sha256 两个资产）。
- 遇到过一次 `.git/refs/heads/master` 被写成 41 个空白字符（`git status` 报 "No commits yet"、push 报 "cannot be resolved to branch"）：提交对象与 reflog 都完好，`rm .git/refs/heads/master` 后 `git update-ref refs/heads/master <reflog 里最后的 sha>` 即恢复，`git fsck` 干净。再遇到照此处理，不要重新 clone。
- 给 AI 助手：含反斜杠的补丁脚本（Windows 路径、`\"` 转义）不要经 Bash heredoc 传，先用 Write 写到临时目录再执行；文档补丁一律先 `assert s.count(old)==1` 再替换。

## 5. 真机验证约定

- 虚拟机 HTTP 端口 `8765`（Bearer 鉴权，用户 `SIEMENS`，TIA V21），**地址随宿主机网络变**：一台机器上是 `192.168.0.172`，desktop-ivrlcht 上是 `10.10.10.56`——以本机 `~/.claude.json` 里 `tia-portal-vm` 的 `url` 为准。服务器端路径（`C:\Users\SIEMENS\...`）在虚拟机上，宿主机不可访问。
- 宿主机若设了 `HTTP_PROXY`（desktop-ivrlcht 是 `127.0.0.1:7897`），MCP 客户端会把局域网请求送进代理，表现为 "Version negotiation probe timed out after 5000ms"（curl 经代理同样 5 秒 502，`--noproxy '*'` 直连立即 401/200）。把虚拟机地址加进 `NO_PROXY`，或临时用直连脚本经 `CallTool` 调用。
- 默认 lite 只暴露 56 个工具，Unified 等全量工具经 `CallTool(name, argumentsJson)` 调用；大响应（>20,000 字符）会被服务器切成 `GetExport(exportId, offset)` 分页——验证时给小的 `limit`（2–5）而不是翻页。
- 直连探针（不经 MCP 客户端，绕过代理）：`python scripts/diagnostics/Probe-McpServer.py tools [关键字]` / `call <lite 工具> '{...}'` / `bridge <任意工具> @args.json`，连接信息默认取 `~/.claude.json` 的 `tia-portal-vm`（或 `--url` / `--token`、`TIA_MCP_URL` / `TIA_MCP_TOKEN`）。`bridge` 会把 `CallTool` 包装的内层 JSON 解开打印。每次调用是独立 MCP 会话，引擎的 Portal 状态跨会话保留。
- 换引擎 exe 后必须重启虚拟机上的服务进程，否则 `server:` 行仍报旧版本。工具返回 "HMI operation blocked after a connection failure"（`GetState` 里 `snapshotReadsBlocked=true`）时，再调一次 `AttachToOpenProject` 即解封；这是 HMI 快照的保守保护，已释放代理在 2.7.31 起由各工具就地处理。
- 工程 `AutomaticDipCoatingMachine`（V21），PLC `+S1-K1`（1510SP F V4.1），HMI `HMI_RT_1`（Unified）。**永不保存工程**；临时对象（画面、变量、组）用完即删；改过的设置（报警类颜色、记录大小、GlobalSettings）复原；先 `dryRun` 再实做。
- 工程事实（硬件）：设备 `ET 200SP station_1` 在组 `+S1` 下（`devicePathJson=["ET 200SP station_1"]` 单名即可），CPU 项路径 `["+S1-K1"]`（**不经**机架 `机架_0`），PROFINET 接口 `["+S1-K1","PROFINET 接口_1"]`，其端口 `["+S1-K1","PROFINET 接口_1","端口_3"]` 与 `["BA 2xRJ45","Port_1"]`；IO 模块 `["+S1-K3","+S1-K3"]`（K3 DI16、K4 DQ16、K5 AI2、K6 AI4 RTD）、`F-DI 8x24VDC HF_1`；OPC UA 子模块 `["+S1-K1","OPC UA_1"]`；4 台 SINAMICS G120C（`SINAMICS G_5` → `["+S1-7Q1","PROFINET 接口"]` 等）是 IO 设备；子网 `PN/IE_1`，IO 系统 "PROFINET IO-System" #100；无拓扑互连、无传输区、无 MRP 环。
- 工程事实：`HmiSoftware.Connections` 为空（集成连接 `HMI_Connection_1` 只在硬件网络里）；无工厂视图；报警审计类为空；语言 en-US / zh-CN / de-DE；变量表 `Default tag table` 可放临时变量（`EnsureUnifiedHmiTag`，参数名是 `hmiSoftwarePath`）。

## 6. 只在真机上学到的 Openness 事实

（从 API 上核实的：`WebserverUserPermissions` 值为独立单比特但没有 `[Flags]`，组合值 `ToString()` 是数字，引擎按位解码；`SimpleWebserverUserComposition` 无 `Create`、`SimpleWebserverUser` 无 `Delete`；`Subnet.Nodes` / `Subnet.IoSystems` 是关联；`ISyncDomainParticipant` 由 `NetworkInterface` 与 `IoSystem` 实现；`TransferAreaType.F_CD` 仅 V21；`Address.AssignProcessImageToOrganizationBlock` 仅 V20。官方文档站的正文可经 `https://docs.tia.siemens.cloud/api/khub/maps/<mapId>/topics/<contentId>/content` 直接取 HTML，`clustered-search` API 按标题搜页面——比逐页导航快得多。）

- `Create<T>` / `Create` 返回的代理与 `Find()` 不同，`ReferenceEquals` 恒假——创建后按名称查回 + 计数核对。
- `HmiSlider` / `HmiToggleSwitch` / `HmiCircleSegment` / `HmiEllipseSegment` 隐藏基类 `EventHandlers`，`Type.GetProperty` 抛歧义——取最派生声明（`UnifiedUiModelLogic.FindProperty`）；`HmiLabel` 没有 `EventHandlers`；`HmiButton` 事件是 `Tapped` 不是 `Click`。
- `Object` 类型属性写 JSON 数字 `0` 后读回是字符串 `"0"`——跨类型比较（`EngineeringScalarProperties.SameValue`）。
- `HmiThresholdComposition.Create()` 在 PublicAPI 里，但 TIA V21 对普通变量拒绝 "New thresholds are not supported"；替代值只对外部变量可设，内部变量报"已禁用的字段不支持设置"；`Font.Weight` 只接受 `Bold` / `Normal`。
- `HmiTagComposition.Export(dir, name)` 回报的 `FileInfo` 没有 `.hmi.yml` 扩展名（文件本身写出）；脚本模块导出正确回报 `Name.hmi.yml` + `Name.hmi.js`。`ShiftModel` 这张 15 变量的表导出只有 `ShiftModel.hmi.yml`（4,400 B）一个文件，没有 `NameData.yml` 等副文件。
- 硬件（2.7.30 真机）：变更前取到的 HW 组合代理是陈旧的——`MrpDomainComposition.Create` 后在旧代理上按名查不到，`Delete` 后旧代理枚举抛 `EngineeringObjectDisposedException`；从 owner 重新导航即正常。空的临时同步域会被 TIA 释放。SINAMICS 接口暴露两个 IoConnector（文档说一个），未连接的那个所有 PN 属性不支持。1510SP F V4.1 上 `WebserverUserManagement` / `SimpleWebserverUserManagement` 为 null（Web 用户由 UMAC 接管），`OpcUaUserManagement` 在 `OPC UA_1` 子模块。`MrpInstancesOwner` 只在组态了 MRP 环的接口项上出现。ET 200SP DI 模块通道的 `ChannelActivated` / `InputDelay` 只读；AI 通道 `Smoothing` 写 1 被拒、写 0 同值可通。S7-1500 地址不支持 `Context` / `IsochronousMode` / `InterruptObNumber`。
- `HmiTextList.Export` / `HmiSystemTextList.Export` 各写两个文件：`<名>.hmi.yml` + `<名>.TextLibrary.hmi.yml`，后者带整个文本库（`PowerState` 的两个文件 554 KB + 599 KB）。
- `HmiTag.Validate()` 在 `ShiftModel` 的变量上返回 3 条 `HmiValidationResult`（`Connection` / `DataType` / `PlcTag`），错误文本按 TIA 界面语言本地化（zh-CN），`Warnings` 为空；"HMI_Connection_1 不存在"与 `HmiSoftware.Connections` 为空一致。
- `HmiScreenGroupComposition.Create(name)` 后 `deleteEmpty` 对空组生效，按名查回 "Exact named object not found"。
- 变量对象路径：`[{TagTableGroups,组},{Groups,子组},{TagTables,表},{Tags,变量}]`；`Default tag table` 在该工程为空，临时变量要用它但真实变量在 `/_SICAR_HMI-tags/...`。
- V20 画面对象目录 37 个、V21 43 个；`HmiScreenItemBaseComposition` 在 `UI.Base` 命名空间。
- 下载提示 `SafetyProgram` 应答 `ConsistentDownload`；`SafetyAdministration.ProgramSignatures` 与 `SafetyBaseIdProvider` 仅 V21。
- Base 收尾（2.7.33 真机）：`ObjectIdentifierProvider.GetIdentifier` 返回 Base64 短串（设备 `mf8qUCbYAkituuc//yPo5g==`），`Find` 反查同名对象；`TiaPortalProcess.AttachedSessions` 里能看到本引擎自己（`accessLevel` "Modify, Published"、`trustAuthority` None）；`InstalledSoftware` 5 个产品（TIA Portal / Startdrive Advanced / STEP 7 Professional / WinCC Basic-Comfort-Advanced / WinCC Unified）；工程 18 个 `TextCategories`；`HwUtilities` 三个标识符 `OPCUAExportProvider` / `ModuleInformationProvider` / `CardReaderPscProvider`；`ModuleInformationProvider.FindContainerTypes(1510SP)` → `System:Device.ET200SP` / `System:Rack.ET200SP`；`OpcUaExportProvider.Export` 对 1510SP F 写出 20 MB XML；路由树：PG/PC 接口本身 `Addresses` 为空，地址挂在子网 `PN/IE_1` 上，目标接口 `1 X1` 的 `Addresses` 也为空；`DefaultWebPagesFeature` 2 个 Web 应用（系统页 `System_Inbuilt` + 硬件配置入口页 `None` 默认）；`CertificateManagementConfiguration` 默认 `TIAPortal` / 0 服务；`Transaction` 真机行为：全部成功 + `CommitOnDispose` 持久，任一失败不调用 `CommitOnDispose` 即整体回滚（第一条已执行的创建被撤销）；`CrossReferenceResult` 的 `SourceObject.TypeName` 是本地化描述（"LAD-Function block"），`Address` 是 `%FB12020` 形式。
- Step7 ④-②（2.7.35 真机）：`PlcExternalSourceComposition.CreateFromFile(name, path)` + `GenerateBlocksFromSource(PlcBlockUserGroup, None)` 从 208 B 的 SCL 文件生成 FC 到用户组，回报 `IList<IEngineeringObject>`（`FC`）；根外部源组名本地化“外部源文件”；`PlcTableCommentEntryComposition.Create()` 后同一组合代理的 `Count` 陈旧（重读为准），而 TIA 拒绝 `PlcTableCommentEntry.Delete`（“not valid because of the current status”）；旧工具 `SetWatchTableModifyValue` 在 V21 建不了带地址的条目（`Create()` 无地址重载）；`PlcAlarmTextListProvider.ExportToXlsx` 在无用户文本列表时抛 `TextListNotFoundException`，过滤重载对不存在的列表抛 `EngineeringTargetInvocationException`（文案和官方页面一致）；`WatchTableAccessRuleComposition.Create` 在 CPU Web 服务器未启用时被拒（“Operation cannot be performed when the web server is disabled.”）；`AlarmClassDataProvider` 在 PLC 上直接可得、消息本地化；默认变量表 `Standard` 有 79 个 `Pip` 类型系统常量；`GetPlcTagTables` 回的表路径带组名（`IO/F_Input`），`ReadPlcTagTableConstants` 要用同样的路径。
- 经典 WinCC（2.7.37 真机）：Unified 工程 `AutomaticDipCoatingMachine` 的工程库有 1,038 个类型——`CodeBlockLibraryType` 425、`PlcTypeLibraryType` 454、`HmiUdtLibraryType` 4（经典 HMI 命名空间的子类在 Unified 工程里也会出现）、Unified `ScriptModuleType` 8、其余 147 个 Unified 面板 / 图形的运行时类就是基类 `LibraryType`（Unified 没有自己的面板 / 画面库类型子类）；V21 PublicAPI 共 19 个 `LibraryType` 子类（Step7 3、经典 HMI 8、Unified 1、SiVArc 7、DCC 1）。四个经典 HMI 工具对 `HmiUnified.HmiSoftware` 与 `SW.PlcSoftware` 都在参数门之后、任何写操作之前以 NotSupported 拒绝。
- 经典 WinCC（2.7.37，PublicAPI 核实）：`HmiTarget` 的六个文件夹根都是系统文件夹子类，用户文件夹 `Name` 只读（没有原生改名）；`ScreenSlidein` 只有 `SlideinType` + `Export`（无 Name / Delete，`ScreenSlideinComposition.Find(SlideinType)`）；`ScreenOverview` / `ScreenGlobalElements` 只有 `Export`，导入是 `HmiTarget.ImportScreenOverview / ImportScreenGlobalElements`；`GraphicsProvider`、`ConstValue`、`NullableDateTime` 仅 V21，后两者在 `Siemens.Engineering.WinCC.Extension.dll`（引擎经 NuGet 包的 `*.dll` 通配引用到），`NullableDateTime` 的年月日时分秒是可写 `Nullable<Int32>`、`DateTimeValues` 是粒度枚举；这些值类型不出现在任何 XML 文档的成员签名里，只作为 `Object` 属性的运行时值出现，所以走 `EngineeringScalarProperties.ValueRenderer` 钩子（离线套件里为空）。
- Step7 ④-③（2.7.36 真机）：工艺对象的 `typeIdentifier` 是 `TO_SpeedAxis` / `TO_PositioningAxis`… 这种形式（`System:TO.…` 被 TIA 拒绝 "does not exist or is not a valid technology object"），1510SP F V4.1 + TIA V21 的 TO 版本是 9.0（7.0 / 8.0 都不存在）；`TechnologicalInstanceDBComposition.Create` 后 `Find` 立即可见（没有陈旧代理问题）；新建速度轴 42 个参数、`isConsistent=false`、`number 1`；速度轴 `AxisHardwareConnectionProvider.SensorInterface` 为空、其余 Motion / Ident 服务 `notProvidedByObject`；`Connect(DeviceItem)` 对没有 I/O 地址的 G120C 报 "Target not available for input=-1 and output=-1"；根工艺对象组名本地化“工艺对象”，运行时类是 `TechnologicalInstanceDBGroup`。
- Step7 ④-③（2.7.36，PublicAPI 核实）：`PlcSoftware.TechnologicalObjectGroup` 的声明类型是 `TechnologicalInstanceDBGroup`（不是 `TechnologicalInstanceDBSystemGroup`）；`TechnologicalParameter.Value` 是可写 `Object`（`ManageTechnologyObject setParameter` 按现值类型转换）；`TOMapping` / `DBMemberMapping` / `InterpreterMappings` / `SuperimposingAxes` / `IdentTechnologicalObjectProvider` 仅 V21，V20 反而多 `Connect(Telegram[, ConnectOption])`；`TorqueHardwareConnectionInterface` 没有 `Connect(Channel)` / `Connect(PlcTag)`；`AxisEncoderHardwareConnectionInterface` 七个 `Connect` 重载（Channel / DeviceItem / PlcTag / string DB 成员 / 两模块 [+ ConnectOption] / 两个位地址 + ConnectOption）。
- Step7 ④-②（2.7.35，PublicAPI 核实）：`WatchTableAccessRule` / `ForceTableAccessRule`（命名空间 `Siemens.Engineering.HW`）在 V21 位于 `Siemens.Engineering.Step7.dll`，形状检查要按类型所在程序集解析；`PlcExternalSourceUserGroup.Name` V20 只读、V21 可写；`NamespaceAccessRestriction` 仅 V21；`PlcWatchTable.Entries` / `PlcForceTable.Entries` 都是 `PlcTableCommentEntryComposition`（`Create()` 只建注释行，`PlcWatchTableEntry` / `PlcForceTableEntry` 是其子类）；`PlcAlarmTextListProvider` 同时是 PLC 与单元的服务，`ExportToXlsx` 过滤重载要 `IEnumerable<string>` + `IEnumerable<Language>`；`AlarmClassDataProvider` 官方在 `ProjectBase` 上（引擎先试 PLC 再回退工程）；`PlcExternalSource.GenerateBlocksFromSource` 有四个重载（无参 void、`GenerateBlockOption`、`PlcBlockUserGroup` + 选项、`PlcTypeUserGroup` + 选项，后三个回 `IList<IEngineeringObject>`）。
- Step7 ④-①（2.7.34 真机）：`PlcUnitRelationComposition.Create` 后在同一组合代理上 `Find(relatedObject)` 回 null，`PlcUnitRelation.Delete` 后旧代理 `Find` 抛 `EngineeringObjectDisposedException`（和 2.7.30 的硬件组合一样：变更后要从 owner 重新导航）；`PlcUnitSystemGroup.Name` 按界面语言本地化（"软件单元"），单元里的系统组也是（"程序块" / "PLC 数据类型"）；`PlcType.ExportAsDocuments` 只写一个 `.s7dcl`（168 B 的 UDT）；UDT 文档导入时名字在整个 PLC 内唯一，单元不隔离（"already exists in the plc"）；`ImportFromDocuments Override` 成功后该 UDT 变为不一致、`GetFingerprints` 随即被拒绝，直到编译；参考工程 `PlcChecksumProvider.Software` 为 null（未编译）而 `TextLists` 有值；通道对象在嵌套的同名设备项上（`["+S1-K3","+S1-K3"]`），硬件组件层 0 通道；`PlcTagProvider.GetLinkedTags` 对 DI / F-DI / DQ / AI 都回 1 个变量并带 `Parent` 变量表；V21 `Project.GetService<PlcSimulationSettingsProvider>()` / `<VirtualPlcSettingsProvider>()` 在参考工程都非 null。
- Step7 ④-①（2.7.34，PublicAPI 核实）：V21 的 `Project` **没有** `IsSimulationDuringBlockCompilationEnabled` / `IsVirtualPlcDuringBlockCompilationEnabled`（官方 "Updating project properties" 仍按属性举例），只剩 `Project.GetService<PlcSimulationSettingsProvider>()` / `<VirtualPlcSettingsProvider>()`；`Channel` / `Address` 在 V20 不实现 `IEngineeringServiceProvider`（`PlcTagProvider` / `ProcessImageProvider` 是 V21 才有的通道 / 地址服务，V21 同时删掉了 `Address.AssignProcessImageToOrganizationBlock`）；`PlcUnitSystemGroup.Name` 与 `PlcDocumentComposition.CreateFrom(MasterCopy / PlcDocumentLibraryTypeVersion)` 仅 V21，而 `PlcTypeComposition.CreateFrom` 与 `PlcUnitComposition.CreateFrom` 两版都有；`PlcDocument` 在 PublicAPI 只有 `Name` + `ExportAsDocuments`；`PlcSafetyUnit` 没有 `Delete`、`PlcSafetyUnitComposition` 没有 `Create`；`PlcBlockWriteProtectionProvider : ProtectionProviderBase`（`GetInvalidPasswordCharacters` 可用）；`DocumentExportResult.ExportedDocuments` 是 `IEnumerable<FileInfo>`；`FingerprintProvider.GetFingerprints()` 返回 `IList<Fingerprint>`（`Id : FingerprintId` 12 值）。
- Base 收尾（2.7.33，PublicAPI 核实）：`ConfigurationAddress` 没有公开构造函数，只能从路由树（`TargetInterfaces[].Addresses`）里取；`RHOnlineProvider.GoOnlineToPrimary/Backup(ConfigurationAddress)` 与 `Device : IShowable`、`ProjectBase.TextCategories`、`DefaultWebPagesFeature` / `WebApplicationConfiguration`、`TelecontrolDataPoint*`、`OnlinePasswordConfiguration.IsSecureCommunication`、`CertificateSupportedService.ApplicationUri / Guid` 都是 V21 才有（V20 的 `CertificateSupportedService.Id` 是只读 `UInt16`，`CertificateSupportedServiceName` 只有 `OpcUaServer` / `Webserver`）；`CertificateSupportedServiceComposition.Create(id, serviceGroupName, serviceType)` 三参；`IShowable` 在 Step7 里由 `PlcBlock` 全家、`PlcType` / `PlcStruct`、`PlcTagTable`、`PlcWatchTable` / `PlcForceTable`、`TechnologicalInstanceDB` 实现；`AttributeConfiguration` 只在 `SetAttributes(pairs, AttributeDelegate)` 的回调里出现（子类 `AttributeNameUnsupported` / `AttributeReadOnly` / `AttributeTypeUnsupported` / `AttributeValueUnsupported`，`CurrentSelection` Abort / Ignore）；`TableData` / `StructuredData` 没有公开的拥有者属性，只作为 `GetAttribute` 的值出现；`ExceptionMessageData` 挂在每个 `EngineeringException.MessageData / DetailMessageData` 上；`UmacDelegate` 是 `Projects.Open/OpenWithUpgrade/Retrieve(file, UmacDelegate[, ProjectOpenMode])` 的凭据回调，`TiaPortal.Authentication` 事件是另一条路。
- 安全 / UMC（2.7.32 真机）：`SyslogServerProvider` 在工程上可用（0 台服务器），但 `Servers.Create(name)` 让 TIA Portal V21 崩溃；CPU `+S1-K1`（1510SP F V4.1）的 `SysLogConfigurationManager` 可读（`EnableSystemLogging=false`、`TransportProtocol=None`、三个动态属性可读）；`UmcServerConfigurator.CheckConsistency()` 不带凭据时 TIA 抛 `EngineeringTargetInvocationException` "凭证无效"，`UmcServer` 没有属性信息；`CreateOfflineUmcUserGroup()` 的原生名是 `Group_1`；offline 用户 / 组的 `DomainId` 为空串、创建即 `IsActive=true`；系统角色 `Engineering administrator` 与自定义角色都能挂到 UMC 用户 / 组；`UmacDevice` 服务在设备 `ET 200SP station_1` 上（30 个系统设备功能权，`comment` 全空），不在 CPU 项；证书模板默认值：Tls CN `S1-K1/Tls-2` + SAN `IP 192.168.0.1`，WebServer CN `S1-K1/Webserver-2` + SAN IP/Dns，有效期到 2037；三套密码策略在本工程都可用（PLC `PasswordPolicyEnabled=true`）。
- 安全 / UMC（2.7.32，PublicAPI 核实）：`Security` 与 `Umac` 的功能成员 V20/V21 完全一致（V21 只多 `.Any()`）；`SyslogServerProvider` 是工程服务（`project.GetService`），`SyslogServer.Port` 是 `Int64`，`AssignedModules` 是 `DeviceItemAssociation`（有 `Add` / `Remove` / `Contains`）；`SysLogTransportProtocolType` 在 `Siemens.Engineering.HW` 而非 `.HW.Features`，`SysLogServerConfigurationComposition.Create(address, UInt16 port)`；`UmcUserGroupComposition.CreateOfflineUmcUserGroup()` 不带名字（之后 `SetName`），`UmcUser` 派生自 `User`（`Name` / `Roles`），`UmcUserGroup` 不是（自带 `Name` / `Roles` / `Description`）；`UmcServer` 没有公开标量，`Authentication` 是 `EventHandler<UmcAuthenticationEventArgs>`，凭据写进 `e.UmcCredentials.Name` / `SetPassword`；`CustomDeviceFunctionRight.Comment` 只读（只能在 `Create(name, group, comment)` 时给）；`CertificateTemplate` 五个标量全可写，`SubjectAlternativeName.Type / Value` 只读（删了重建）；`Certificate.Id` 是 `UInt32`；`PasswordPolicyConfigurator` 的数值属性都是 `Int16`。
- 库（2.7.31 真机）：`ProjectLibrary` 没有 `Name`（只有 `MasterCopyFolder` / `Parent` / `TypeFolder`）；`SystemGlobalLibrary.TypeFolder` 为 null（系统库只有母版，`Buttons-and-Switches` 的母版树在 `FunctionButtons/Rectangular` 等文件夹）；对系统库调 `UpdateCheck` 抛 `NonRecoverableException`（会被连接保护当作断连，需要 `AttachToOpenProject`，且打开的全局库句柄随之丢失）；`Close` / `Save` / `SaveAs` / `Archive` / `CleanUpLibrary` 只在 `UserGlobalLibrary` 上，`Open(GlobalLibraryInfo)` 打开的系统库无法经 API 关闭；`GetGlobalLibraryInfos` 在虚拟机上列出 4 个系统库（Buttons-and-Switches / Long Functions / Monitoring-and-control-objects / Documentation templates），没有用户全局库。工程库顶层 26 个类型文件夹（PLC / LGF / LBC / _SICAR_Types_PLC / Open Library V19 / LUC / LSicar / LAF / LAxisCtrl / LSafe / _LibrarySafety / LPD / … / LSyslog），`LGF/00 Bit logic operations` 下 7 个 `CodeBlockLibraryType`（`LGF_BitSet` 3.0.1 等，`originalLibrary=LGF_V5_4_0_TiaLib_V21`）可作比较/读取样本。`DetailedCompareResult` 的 `Original Library` 行状态是 `NotCompared`。
