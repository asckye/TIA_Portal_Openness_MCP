# 接续交接历史（逐版本记录与阶段收口清单）

[交接总页](handoff.md) · [换机器交接单](handoff-checklist.md) · [文档目录](../README.md) · [覆盖清单](../reference/openness-coverage.md)

这一页只存历史：从 `handoff.md` §1 移出来的逐版本条目（每版做了什么、部署后真机结果）、2.7.42 覆盖审计与阶段 1–6 的收口清单（含当时用过的官方页面 id）、阶段 6 的旧类型清单。当前状态与下一步在 [handoff.md](handoff.md)；各版详情在 `../releases/`，变更摘要在 `../../CHANGELOG.md`。新发布后在本页 §1 顶部加一条，`handoff.md` §1 只改现状表。

## 1. 逐版本记录（2.7.41–2.7.58，倒序；每条为当时写下的原文）

- **2.7.58**（2026-09-21，`Release.ps1` 全程自动发布，三条工作流全绿，ZIP `TIA_MCP_Delivery_v2.7.58_20260921.zip` + `.sha256` 已上传；见 `docs/releases/v2.7.58.md`）：维护者要求"规范 AI 对所有工具的调用、不再试错"——`SchemaHintedTool` 把参数备选值 / 默认值 / 示例值 / 词汇表描述写进每个工具的 `inputSchema`（备选值解析在真机 schema 上核对后收紧：只认参数自己那句话里独立成段的列表，去掉了 `password` / `promptAnswersJson` / `folderPath` / `pgPcInterface` / `blockGroupPath` / `blockScope` 等误判，67 enum 落在 51 个工具）；`ArgDiagnosticTool` 对每个失败响应附 `meta.preflight`（`PreflightSummary`，成功响应同一对象引用不动，`CallTool` 看内层）；`ParameterVocabulary` 79 条通用参数描述 + 手写批次 1（14 个工具 96 个参数，取值来自 Logic 数组），无自身描述 1488 → 1392（1002 由词汇表覆盖），`tools-list.json` 的 `callDiscipline` 统计 + 回升门；`ToolExamples.Derive` 让 453 个工具都有示例；`ToolRecipes` 12 条真机序列 + `GetRecipe`（lite）；`CallDisciplineTests` 97 项，离线 2453。
- **2.7.57 部署后真机**（2026-09-21，维护者用 `Update-Engine.ps1` 部署，引擎手动重启）：`Bootstrap` 2.7.57.0、lite 58 / 452、45 个描述带 `Example:`；`Connect {projectName:"项目1"}` 在只开着维护者工程（pid 11104）时绑到它 + `warning`、不自启、单句号（2.7.56 待真机 2 的场景）；`PreflightToolCall downloadtoplc {SoftwarePath, targetIp}` → NOT READY，列出 `targetIp (did you mean 'targetIpAddress'?)` 与 `Case: SoftwarePath -> softwarePath`、前提已绑、示例；`CheckForUpdate` 经 API `latestVersion 2.7.57`、`updateAvailable false`、`installRoot` / `updaterScript` 找到。`项目1` 此时未打开。
- **2.7.57**（2026-09-21，`Release.ps1` 第一次真跑发布，三条工作流最终全绿，ZIP `TIA_MCP_Delivery_v2.7.57_20260921.zip` + `.sha256` 已上传；见 `docs/releases/v2.7.57.md`）：更新器 `scripts/operations/Update-Engine.ps1`（引擎在运行就拒绝并列出 pid、不杀；GitHub API / 发布页回退 / `-ZipPath` 离线；ZIP + `.sha256` 校验；`.previous` 备份；`-Check` / `-Rollback`；本机在 2.7.55 解压包上验证四条路径）、只读 `CheckForUpdate`（API 限流时读发布页）、`PreflightToolCall`（纯逻辑 `PreflightLogic`：缺参 / 未知与大小写 / 类型 / 备选值 / dryRun 与 confirm / 前提 / 示例）、`ToolExamples` 80 条附进描述 + `FindTools` + `CallTool` 拒绝，`Generate-ToolsListFromAssembly.ps1` 构建时校验、`tools-list.json` 加 `example`；服务器指令 / Bootstrap 加 "PLAN, DO NOT PROBE"；`Connect` 双句号修。工具 452、lite 58、离线 2356。发布脚本两处缺陷：PS 5.1 把 git 的 stderr 进度当终止错误（推完 1/3 就停）→ 原生命令只看退出码、加 `-Resume`；`-Resume` 把 tag 打在 3/3 上而 HEAD 已是修复提交 → publish run 58 "Master changed" 失败 → 删 tag 重打在 HEAD、run 59 通过；脚本改成 tag 打 HEAD、按 HEAD 标题等 CI。
- **2.7.56 部署后真机**（2026-09-21）：`Bootstrap` `serverVersion 2.7.56.0`；`ListPortalProcessProjects` 2 个进程（4840 `项目1`、15100 维护者工程；15748 已关）；`Connect {projectName:"项目1"}` → attached to PID 4840，`processCount 2`、`candidates` 只列 4840（命中即停）、`attachElapsedMs 70`、`startedNew false`；再 `ListPortalProcessProjects` 仍 2 个；`Connect {projectName:"NoSuchProject_2756"}` 探测两个（385 ms）后绑 4840 + `warning`，不自启；`GetState` `project 项目1` / `boundProcessId 4840`。**Connect 多进程缺陷关闭**。未跑：只留维护者工程时按名 Connect 的 warning 路径（要关 `项目1`）。瑕疵：名字不存在时消息末尾双句号。
- **2.7.56**（2026-09-21，本机构建并发布，三条工作流全绿，ZIP `TIA_MCP_Delivery_v2.7.56_20260921.zip`（15.9 MB）+ `.sha256` 已上传；见 `docs/releases/v2.7.56.md`）：`ConnectPortal(projectName, allowStart, info)`——有 TIA 进程时按 `projectName` 找持有该工程的进程（命中即停），否则第一个有工程 / 会话的，再否则第一个可附加的；全部附不上 → `PortalException` 逐进程说明并提示（访问对话框 / 忙 / 重试 / `ListPortalProcessProjects` / `allowStart` / `ConnectIsolated`），**不自启**；只有没有 TIA 进程（或 `allowStart=true`）才 `new TiaPortal`；每进程 20 s、总 45 s。`Connect` 工具 `projectName` / `allowStart`，`Meta` 回报 `processCount` / `candidates` / `boundProcessId` / `startedNew` / `warning`。纯逻辑 `ConnectLogic`（离线 2218）。**部署 2.7.56 后的下一步**：`docs/releases/v2.7.56.md` "待真机" 1–2（`ListPortalProcessProjects` 计数 → `Connect {projectName:"项目1"}` → `boundProcessId` 是持有 `项目1` 的进程、进程数不增）。
- **2.7.55 部署后真机**（2026-09-21）：`RunPlcSimAdvancedTestScenario singleStep` **PASSED** 8/8、4/4——`operatingModeApplied SingleStep_C`，`cyclesStepped 5`（71 ms），`Cycles` 304→309 恰好 +5，`Start=false` 后 2 周期不增。`SaveProject` 通过（`IsModified false`，`项目1` 现在含 `MCP_STD` + 程序、两台 CPU 的保护设置、监控表两行）。**新缺陷**：引擎重启后 `Connect` 遇到两个 TIA 进程（维护者的 `AutomaticDipCoatingMachine` 15100 + `项目1` 4840）没有附加而是**又启动了一个空 TIA**（15748；MCP 客户端 60 s 超时，`camp.py` 能看到引擎已绑到 15748 / 无工程），`AttachToOpenProject {projectName:"项目1"}` 按名重新绑到 4840 后一切正常；空实例没有工具能关，要维护者在虚拟机上关掉。**2.7.56 要做**：`Connect` 在已有 TIA 进程时只附加（一个进程直接附加，多个时按 `projectName` / 拒绝并列出进程），绝不自启；可选 `ClosePortalProcess` 只允许关引擎自己启动的、无工程的实例。
- **2.7.55**（2026-09-21，本机构建并发布，三条工作流全绿，ZIP `TIA_MCP_Delivery_v2.7.55_20260921.zip`（15.9 MB）+ `.sha256` 已上传；见 `docs/releases/v2.7.55.md`）：2.7.54 部署后 `RunPlcSimAdvancedTestScenario singleStep` 的模式切换生效（`operatingModeApplied SingleStep_CP`，步间 `Freeze`，Running 断言过）但 `{cycles:5}` 只让 `Cycles` 303→304、全程 44 ms——手册 "SingleStep operating modes"：`RunToNextSyncPoint()` 只解除冻结、立即返回，实例跑到下一个同步点才再次 `Freeze`；修：`StepCycles` 每周期等 `Freeze` → 触发 → 等 `Freeze`（5 s / 周期），`singleStep` 优先 `SingleStep_C`（每周期一个同步点）。`UploadStationFromPlc`：接口去重生效，工程级扫描把 Softbus 实例列为 192.168.0.1、预览通过，真跑被 TIA 拒 "The selected object cannot be uploaded from the device."（PLCSIM 实例不支持上载为新站，⛔，设备数仍 5）。`GoOnline MCP_PLC` 回报 TIA 原文 + `onlineStateAfter Offline`。离线 2211。**部署 2.7.55 后的下一步**：`docs/releases/v2.7.55.md` "待真机" 1（singleStep 场景 Cycles 恰好 +5）——在线族到此收口。
- **2.7.54**（2026-09-21，本机构建并发布，三条工作流全绿，ZIP `TIA_MCP_Delivery_v2.7.54_20260921.zip`（15.9 MB）+ `.sha256` 已上传；见 `docs/releases/v2.7.54.md`）：2.7.53 部署后 `ManagePlcProtection` 把 `MCP_PLC` 改成 `FullAccessIncludingFailsafe` + `WithPassword`、`CompileDevice` 0 错，但 `DownloadToPlc` 被 TIA 拒 "Loading or overloading fail-safe data in Openness is not permitted."（F-CPU 不能经 Openness 下载——官方规则）。按 §6 规则新建标准 CPU **`MCP_STD`**（1515-2 PN V2.9，X1 192.168.0.3 在 `MCP_PN`；新建默认同样 `NoAccess` + `WithoutPassword`）→ 保护改好 → `CompileDevice` 0 错 → `DownloadToPlc {pgPcInterface:"PLCSIM", targetIpAddress:"192.168.0.1", masterSecretPassword}` **Success**（首次 created 地址；`PlcMasterSecretPassword` 提示由 `masterSecretPassword` 应答；实例变成 CPU1515 / 192.168.0.3 / RUN）→ `GoOnline` **Online** → `CompareSoftwareToOnline` 0 差异 → SCL 外部源生成 `MCP_SimDB` / `MCP_SimLogic` / `Main` 后再次下载 Success → `ReadPlcSimAdvancedTags` 5 标签 / 读 4 → `WritePlcSimAdvancedTags` 写回读 → `RunPlcSimAdvancedTestScenario` default 模式 **PASSED** → `GoOffline`。三处修：singleStep 在 8.0 API 是 `SingleStep_CP`（`OperatingModeCandidates`）；`UploadStationFromPlc` 把按模式重复的 "PLCSIM (#1)" 误判歧义（同名同号视为一块网卡，PN/IE 优先）；`GoOnline` 失败回报内层异常链与 TIA 当前状态。离线 2210，工具 450。**部署 2.7.54 后的下一步**：`docs/releases/v2.7.54.md` "待真机" 1–3（singleStep 场景、站上载预览 / 真跑后删设备、F-CPU GoOnline 文案）。
- **2.7.53**（2026-09-21，本机构建并发布，三条工作流全绿，ZIP `TIA_MCP_Delivery_v2.7.53_20260921.zip`（15.9 MB）+ `.sha256` 已上传；见 `docs/releases/v2.7.53.md`）：2.7.52 部署后 `GoOnline {ipAddress:"192.168.0.1", pgPcInterface:"PLCSIM"}` 的 `meta.tlsVerification` 证明 TLS 提示经 `OnlineLegitimation` 来并被答成 Trusted（`verificationInfo "certificate not matching"`，TIA 记住、第二次不问）；`GoOnline` 仍抛无正文异常，但 `GetOnlineState` = `Incompatible`（实例未下载过——连接已建立）；`GoOffline` 后 `DownloadToPlc` 报 "硬件配置编译完成，但出现错误"，用 `InvokeService … ICompilable.Compile` 看到 3 个错：访问级别 `NoAccess` 而无完全访问密码、机密 PLC 组态数据无密码、通信证书因此建不了；这两项没有任何工具能改，反射桥传不了枚举 / `SecureString`。新增 **`ManagePlcProtection`**（`PlcAccessLevelProvider` + `PlcMasterSecretConfigurator`，CPU 设备项，`itemPathJson []` 自动找 CPU；读回验证；官方页 "Access level setting" / "Managing PLC Master Secret in PLCs"）与 **`CompileDevice`**（`ICompilable` 硬件编译 + 结构化诊断），反射桥支持枚举名与 `SecureString` 参数；`HardwareServicesShapeChecks` 第 7 节；形状 V20 2805 / V21 3097，离线 2208，**工具 450**。**部署 2.7.53 后的下一步**：`docs/releases/v2.7.53.md` "待真机" 1–4（读保护状态 → `setAccessLevel FullAccessIncludingFailsafe` + `protectMasterSecret` → `CompileDevice` 0 错 → `DownloadToPlc` → 在线族其余）。
- **2.7.52**（2026-09-21，本机构建并发布，`validate-bundle` / `offline-checks` / `Publish complete release` 全绿，ZIP `TIA_MCP_Delivery_v2.7.52_20260921.zip`（15.9 MB）+ `.sha256` 已上传；见 `docs/releases/v2.7.52.md`）：2.7.51 部署后跑了交接单 §3 的 1–3：监控表行 `%M0.0` appended / `MCP_Start` updated 都 `readbackVerified`（`ModifyIntention` 仍 false，TIA 不推导、Openness 只读）；`register … Softbus` 经 `SimulationRuntimeManager.NetworkMode` 通过（`TCPIPSingleAdapter → Softbus`，`powerOn` 后 `controllerIP 192.168.0.1`），`ReadTransferRoutes` 只剩 "PLCSIM" 接口、`CheckDownloadReadiness` Ready、`ScanAccessibleDevices` 在 PLCSIM 上看到 `S7-1500 CPU:192.168.0.1`；`CompileSoftware` Success；**`GoOnline` 报 "Connection to device cannot be established. The device is not trusted. Please check the certificate."，`DownloadToPlc` 同路由报 "连接到模块 MCP_PLC 失败"**——FW ≥ 2.9 的 TLS 信任提示（`TlsVerificationConfiguration` 经 `OnlineLegitimation`，官方页 "Supporting secure S7 communication TLS"）从没被应答，因为处理器只在给了密码时订阅。修：`AttachOnlineLegitimationHandler` 总是订阅，`trustDeviceCertificate`（`GoOnline` / `DownloadToPlc`，默认 true）答 `Trusted`，`meta.tlsVerification` 记录；上载 / 指纹工具同样应答；`GoOnline` 响应带 Meta，`SetWatchTableModifyValue` / `CheckDownloadReadiness` / `DownloadToPlc` 的 Meta 带 `success`，PLCSIM `data.api.networkMode` 动作后重取。离线 2199，工具 448。**部署 2.7.52 后的下一步**：`docs/releases/v2.7.52.md` "待真机" 1–3（`GoOnline` 看 `meta.tlsVerification`；`DownloadToPlc` → 在线族其余）。
- **2.7.51**（2026-09-21，本机构建并发布，`validate-bundle` / `offline-checks` / `Publish complete release` 全绿，ZIP `TIA_MCP_Delivery_v2.7.51_20260921.zip`（15.9 MB）+ `.sha256` 已上传；见 `docs/releases/v2.7.51.md`）：2.7.50 部署后在 `项目1` 跑了交接单 §3 的 1–4：`ManagePlcTableEntries deleteTable` 删掉 `MCP_WT_1` … `MCP_WT_5`（工程已保存）；`SetWatchTableModifyValue` 的 SimaticML 往返被 TIA 拒 "'set_ModifyIntention' is not supported … read-only"→ 行里不再写 `ModifyIntention`、导入前从每行剥掉；PLCSIM Advanced 8.0 的 `IInstance.CommunicationInterface` 只读且无任何 `Set…` 方法，手册 "To set the network mode, refer to the NetworkMode section"→ `communicationInterface` 改走全局 `SimulationRuntimeManager.NetworkMode`（`Softbus` / `TCPIPMultipleAdapter` / `TCPIPSingleAdapter`，有实例在跑时 API 拒绝；`data.communicationInterfaceRoute` 回报前后模式，`ReadPlcSimAdvancedInstances` 回报 `api.networkMode` / `managerMembers`）；`UploadStationFromPlc` 对扫描到的 MAC 被 TIA 拒 "does not specify a valid address"（`ConfigurationAddressComposition.Create` 只收 IP，官方页亦然）→ 拒绝信息明说。在线族（`DownloadToPlc` / `GoOnline` / `CompareSoftwareToOnline`）仍等 PG 侧：虚拟网卡配 IP 或 2.7.51 的 Softbus。离线 2197，形状检查 V20 2789 / V21 3077，工具 448。**部署后真机（2026-09-21）**：`SetWatchTableModifyValue` `%M0.0` appended / `MCP_Start` updated，`readbackVerified` 都 true（`ModifyIntention` 仍 false——TIA 不推导，Openness 写不了）；`register … Softbus` 经 `SimulationRuntimeManager.NetworkMode`（`TCPIPSingleAdapter → Softbus`）通过，`powerOn` 后 `controllerIP 192.168.0.1`；`ReadTransferRoutes` 只剩 PC 接口 **"PLCSIM"**（子网 `MCP_PN` 192.168.0.1），`CheckDownloadReadiness` Ready。**下一步**：在线族 `DownloadToPlc {softwarePath:"MCP_PLC", pgPcInterface:"PLCSIM", targetIpAddress:"192.168.0.1"}` → `GoOnline {ipAddress:"192.168.0.1", pgPcInterface:"PLCSIM"}` → `GetOnlineState` → `CompareSoftwareToOnline` → PLCSIM 读写 / 场景 → `UploadStationFromPlc {targetIpAddress:"192.168.0.1", dryRun:true}` → `GoOffline`。小修留 2.7.52：`SetWatchTableModifyValue` / `CheckDownloadReadiness` 的 Meta 没有 `success`（桥接报 `operationStatus unknown`）；`ManagePlcSimAdvancedInstance` 的 `data.api.networkMode` 是调用开始时的快照。
- **2.7.50**（2026-09-21，本机构建并发布，`validate-bundle` / `offline-checks` / `Publish complete release` 全绿，ZIP `TIA_MCP_Delivery_v2.7.50_20260921.zip`（15.7 MB）+ `.sha256` 已上传；见 `docs/releases/v2.7.50.md`）：2.7.49 部署后 `DownloadToPlc` / `GoOnline` 路由选择通过（`-> address 192.168.0.1 (subnet MCP_PN)`），TIA 真正发起下载 / 连接，失败在 PG 侧（虚拟网卡无 IP）；`GetTechnologyObjects` 递归生效；`SetWatchTableModifyValue` 的 `Entries.Create()` 只建注释行且旧代码每次新建一张表（真机留下 `MCP_WT_1` … `MCP_WT_5`）→ 改 SimaticML 往返 + 递归找表，`ManagePlcTableEntries` 新增 `deleteTable`；PLCSIM 8.0 实例对象无可写 `CommunicationInterface` 属性 → 试 setter 方法 + `memberFilter` 诊断；站上载再加 PC 接口地址。**部署 2.7.50 后的下一步**（也在发布说明"待真机"里）：① `ManagePlcTableEntries deleteTable` 删 `MCP_WT_1` … `MCP_WT_5`；② `SetWatchTableModifyValue {tableName:"MCP_W/MCP_WT", address:"%M0.0", modifyValue:"TRUE", trigger:"OnceOnlyAtStart"}` 与 `address:"MCP_Start"`，看 `readbackVerified`，`ManagePlcTableEntries read` 核对；③ `ReadPlcSimAdvancedInstances {memberFilter:"CommunicationInterface"}` 定 setter，再 `register … communicationInterface Softbus`；④ PG 侧网卡配 192.168.0.x/24 后跑在线族（下载 → 上线 → 比较 → PLCSIM 读写 / 场景 → 站上载预览 → 下线）。
- **2.7.49**（2026-09-21，本机构建并发布，`validate-bundle` / `offline-checks` / `Publish complete release` 全绿，ZIP `TIA_MCP_Delivery_v2.7.49_20260921.zip`（15.7 MB）+ `.sha256` 已上传；见 `docs/releases/v2.7.49.md`）：2.7.48 部署后 `ManageOpcUaInterface delete` + 编译 Success、PID_Compact 2.3 导出 / 批量导出 / 删除 / 导入（进 `MCP_TO`）/ 批量导入全过；在线族对 PLCSIM Advanced `MCP_SIM`：扫描（按 MAC）/ 就绪 / 快照 / 下线 通过，`DownloadToPlc` 与 `GoOnline` 因路由缺陷失败（本版修：子网 / 网关地址、官方 `Addresses.Create`、5 参 `Download`、`GoOnline(ConfigurationAddress)` + 新参数 `pgPcInterface`），`ManagePlcSimAdvancedInstance … communicationInterface` 的 setter 缺陷（本版修，经 `IInstance` 接口）、`SetWatchTableModifyValue` 的 `Entries.Create(address)` 缺陷（本版改官方 `Create()` + `SetAttribute`）、`GetTechnologyObjects` 只列根级（本版递归）；`FingerprintDataProvider` / `ParameterUploadProvider` 在 1515F-2 PN V2.9 上为 null（TIA 侧）。**部署 2.7.49 后的下一步**：① 先解决 PG 侧：虚拟网卡 "Siemens PLCSIM Virtual Ethernet Adapter" 没有 IP（路由 `[no IP]`）——在虚拟机给它配 192.168.0.x/24，或 `ManagePlcSimAdvancedInstance register MCP_SIM cpuType CPU1500_Unspecified communicationInterface Softbus`（先 powerOff + unregister）后用 `ReadTransferRoutes` 看有没有 "PLCSIM" 接口；② `DownloadToPlc {softwarePath:"MCP_PLC", pgPcInterface:"<网卡或 PLCSIM>", targetIpAddress:"192.168.0.1", startAfterDownload:true}` → `GoOnline {softwarePath:"MCP_PLC", ipAddress:"192.168.0.1", pgPcInterface:…}` → `GetOnlineState` / `CompareSoftwareToOnline` / `SetWatchTableModifyValue`（看 `readbackVerified`）/ `ReadPlcSimAdvancedTags` / `WritePlcSimAdvancedTags` / `RunPlcSimAdvancedTestScenario` / `UploadStationFromPlc {targetIpAddress:"<扫描到的 MAC 或 IP>", dryRun:true}` / `GoOffline`；③ `GetTechnologyObjects` 应列出 `MCP_TO` 里的 TO（先 `ImportTechnologyObject` 桌面 `mcp48_pid.xml` 进 `MCP_TO`）。
- **2.7.48**（2026-09-21，本机构建并发布，`validate-bundle` / `offline-checks` / `Publish complete release` 全绿，ZIP `TIA_MCP_Delivery_v2.7.48_20260921.zip`（15.7 MB）+ `.sha256` 已上传；见 `docs/releases/v2.7.48.md`）：2.7.47 部署后重跑台账（43/45 🔁 通过）、工程级工具与工艺对象（5.0 版本可建）、经典画面（800×480 成功）；crash ⑩ 守卫；新增 `ManageOpcUaInterface`（**448 个工具**）；PLCSIM `communicationInterface=TCPIP`。**部署 2.7.48 后的下一步**：① `ManageOpcUaInterface {softwarePath:"MCP_PLC", interfaceName:"mcp46_opcua", action:"delete", dryRun:false}` 然后 `CompileAndDiagnosePlc` 应回 Success；② 在线族对 PLCSIM Advanced：`ManagePlcSimAdvancedInstance register MCP_SIM cpuType CPU1500_Unspecified communicationInterface TCPIP` → `powerOn` → `DownloadToPlc {softwarePath:"MCP_PLC", pgPcInterface:"Siemens PLCSIM Virtual Ethernet Adapter", targetIpAddress:"192.168.0.1", startAfterDownload:true}` → `GetOnlineState` / `GoOnline` / `CompareSoftwareToOnline` / `ReadPlcBlockFingerprints` / `UploadDeviceParameters` / `UploadStationFromPlc`（dryRun）/ `ReadPlcSimAdvancedTags` / `WritePlcSimAdvancedTags` / `RunPlcSimAdvancedTestScenario` / `SetWatchTableModifyValue` / `ManagePlcDataBlockSnapshot createSnapshot` / `GoOffline`；③ TO 导出导入（先建 `TO_SpeedAxis 5.0`，编译，`ExportTechnologyObject` → 删除 → `ImportTechnologyObject`）。
- **2.7.47**（2026-09-20，本机构建并发布，`validate-bundle` / `offline-checks` / `Publish complete release` 全绿，ZIP `TIA_MCP_Delivery_v2.7.47_20260920.zip`（15.6 MB）+ `.sha256` 已上传；见 `docs/releases/v2.7.47.md`）：`CallTool` 宽松绑定——维护者反映 AI 调用工具反复格式错误；对象形式 `argumentsJson`、`*Json` 参数给对象/数组、字符串数字/布尔、枚举大小写重试，`Meta.bridgeNormalizedArguments` 回报。部署后顺手验证：`CallTool {name:"GetDeviceInfo", argumentsJson:{devicePath:"MCP_PLC"}}`（对象）、`ReadIoSystems {devicePathJson:["MCP_PLC"], itemPathJson:["MCP_PLC","PROFINET 接口_1"]}`（数组）、`ManageProjectLanguage {action:"Read"}`（大小写）。
- **2.7.46**（2026-09-20，本机构建并发布，`validate-bundle` / `offline-checks` / `Publish complete release` 全绿，ZIP `TIA_MCP_Delivery_v2.7.46_20260920.zip`（15.6 MB）+ `.sha256` 已上传；见 `docs/releases/v2.7.46.md`）：维护者要求"没测过的接口全部重测、不留问题"——447 个工具在 `项目1` 上各跑一遍（批跑器逐步检查 TIA 进程），23 处修复、crash ⑧⑨ 守卫；**`项目1` 已按维护者同意保存**，里面现在有 `MCP_PLC`（1515F-2 PN V2.9，含 MCP_G/MCP_FC/MCP_FB/MCP_FB_DB/MCP_DB、MCP_T/MCP_UDT、MCP_Tags/MCP_Table、MCP_W/MCP_WT、MCP_X、MCP_TO、单元 MCP_Unit、子网 MCP_PN、多余的空 OPC UA 服务器接口 `mcp46_opcua`）、`MCP_TP700`（Comfort V17，MCP_TagFolder/MCP_HmiTags、MCP_ScreenFolder、MCP_Scripts）、`MCP_UCP`（MTP700 Unified V21，MCP_Conn/MCP_UTags/MCP_UScreen/MCP_Log）、`MCP_S120`（V5.2 + 驱动轴_1：MCP_MM/MCP_Motor/MCP_Encoder）、项目库主副本 MCP_MC/MCP_UDT、MCP_TP700，全局库 `MCP_GL`（桌面 mcp46_gl），VCI 工作区 MCP_WS（桌面 mcp46_vci）——崩溃重启后只需重开工程。
- **2.7.45**（2026-09-20；见 `docs/releases/v2.7.45.md`：`GetState` 重绑守卫、`plugOnDevice`、DCC 引脚值类型、`sequenceIndex` 单独 update；真机结果见同页"真机结果"）。**测试用工程**：维护者建了空工程 `项目1`（与 `AutomaticDipCoatingMachine` 分开），引擎在里面自建设备；里面现在留有 `MCP_S120`（V5.1）与 `MCP_S120B`（V5.2 + 驱动轴_1：MCP_MM / MCP_Motor / MCP_Encoder / 编码器模块_1）两台临时设备与桌面上的 `mcp2744-chart.dcc` / `mcp2744-all.dcc`，工程未保存——可直接复用，也可 `deleteDevice`。**只开一个 TIA 实例做测试**：2.7.44 上第二个实例打开维护者工程后引擎切了过去（2.7.45 已守卫，但别再给它机会）。
- `v2.7.44`（2026-09-20，本机构建并发布，`validate-bundle` / `offline-checks` / `Publish complete release` 全绿，ZIP `TIA_MCP_Delivery_v2.7.44_20260920.zip`（15.6 MB）+ `.sha256` 已上传；**2026-09-20 已部署并真机重跑**，结果见 `docs/releases/v2.7.44.md#验证`：CFC 预检对没有图表的 PLC 回 InvalidState，`export` 附图表清单，TIA 未退出——2.7.42 暴露的三处退出全部有守卫并经真机验证）：CFC 预检解析器按真实空导出结构修正。
- `v2.7.43`（2026-09-20，发布并于同日部署真机重跑，结果见 `docs/releases/v2.7.43.md#验证`：三处守卫全部拦住，TIA 未退出；暴露了解析器把 `FunctionChartsFolder Name="Charts"` 当图表的问题）：2.7.42 真机重跑的三处 TIA 退出守卫：CFC `CompleteExport` 预检、Test Suite 空组 `runAll` 拒绝、Safety Validation 条件 NotRelevant 预校验 + `changeEvaluationDevice` 只认 `EvaluationDevices()`）。
- `v2.7.42`（2026-09-20，发布并于同日部署真机重跑，结果见 `docs/releases/v2.7.42.md#验证`：**Safety Validation Assistant 虚拟机有选件，全链路通过**；Test Suite 三组空、样式指南空组执行 Success；CFC 空导出通过；Teamcenter 提供者存在无服务器；TIA 退出三次——CFC `GetChartProtection` / `ExportInstructionData`、条件级 `CheckValidity`——引擎三次都活着）：阶段 6 ⑥-③——Safety Validation Assistant 五个工具（V21 only）、Test Suite 三个反射工具的类型化改造 + `ManageTestSuiteCase`、Teamcenter Gateway 三个工具、CFC `ExchangeCfcCharts` 类型化 + `ManageCfcChartProtection`；工具 437 → 447。文件：`Siemens/SafetyValidationLogic.cs`、`TestSuiteLogic.cs`、`TeamcenterLogic.cs`、`CfcLogic.cs`、`Siemens/Portal/Portal.SafetyValidation.cs`（整族 `#if TIA_V20` 存根 / `#else` 实做）、`Portal.TestSuite.cs`（重写）、`Portal.Teamcenter.cs`（会话内 `_teamcenterConnection`）、`Portal.Cfc.cs`、`ModelContextProtocol/Tools/McpServer.SafetyValidation.cs` / `McpServer.TestSuite.cs` / `McpServer.Teamcenter.cs` / `McpServer.Cfc.cs`（旧注册从 `McpServer.SpecializedEngineering.cs` 移出，反射版 `ExchangeCfcCharts` 从 `Portal.SpecializedExchange.cs` 删除）、`tests/.../SafetyValidationTests.cs` / `TestSuiteTests.cs` / `TeamcenterTests.cs` / `CfcTests.cs`、`tests/.../SafetyValidationShapeChecks.cs` / `TestSuiteShapeChecks.cs` / `TeamcenterShapeChecks.cs` / `CfcShapeChecks.cs`（V20 经 `Api(split, legacy)` 回落到整体 `Siemens.Engineering.dll`；SafetyValidation 在 V20 只钉命名空间缺席）。**真机能验的很少**：SafetyValidation / Teamcenter 无许可 / 无环境；Test Suite 三组为空、CFC 无图表——部署后只跑 NotSupported / NotFound / 空表路径（§2 第 1 步）。
- 上一 tag `v2.7.41`（2026-09-20，本机构建并发布，`validate-bundle` / `offline-checks` / `Publish complete release` 全绿，ZIP `TIA_MCP_Delivery_v2.7.41_20260920.zip`（15.4 MB）+ `.sha256` 已上传；**2026-09-20 已部署并真机重跑**，结果见 `docs/releases/v2.7.41.md#验证`：全程 TIA 未退出，Startdrive 读端与 G120C 电机投影 / S120 工艺扩展实做通过，DCC 全族与在线工具仍未到达真实对象）。`v2.7.40` 已部署并真机重跑一部分（`docs/releases/v2.7.40.md#验证`）：`processAlive` 诊断、`includeValue=false`、`Bits` 位名通过；新建未联网 G120C 上的报文 `Can*` 让 TIA 退出，**随后引擎进程也崩溃**（0xE0434352，"Exception.ToString() 失败"）——2.7.41 让引擎活下来（`App.config` legacy 策略）并再加守卫；同版把 PLCSIM Advanced 桥改成缓存接口 + 变量表装载一次 + 串行调用（维护者报告 2.7.38 在 PLCSIM 连续读写 15–30 次后引擎崩溃 5 次，与并发无关；本机无 PLCSIM，未验——请维护者连续读写 50 次以上核对，结果里 `interface.uses` 应递增）。`v2.7.39` 的真机结果见 `docs/releases/v2.7.39.md#验证`。上一个 `v2.7.38` 同日，已真机验证（`docs/releases/v2.7.38.md#验证`）。

## 2. 2.7.42 覆盖审计与阶段 1–6 收口

- 审计（V21，2.7.42 源码）：类型 1,215（分母去掉 internal 的 `Compiler.CompileProvider` 与 `Private.ProcessHelper`，审计脚本 2.7.33 起排除）= 专用引用 668 / 仅类型名 106（含 39 个只在 `Portal.KnownDccExceptions` 里点名的 DCC 异常类）/ 动态覆盖 325 / 完全未触及 116；**未封装功能类型 0 / 0——所有程序集（Base / Step7 / 经典 WinCC / `WinCC.Extension` / WinCC Unified / Safety / SiVArc / Startdrive / DCC / SafetyValidation / TestSuite / Teamcenter / CFC）**。116 个未触及类型全是壳子（`*Composition` / `*Exception` / `*Result` / `*Info`）或经通用标量读写到达的属性袋（`HW.CommunicationConnections` 等），"仅类型名" 106 个里除 DCC 异常类外多是 Test Suite / Teamcenter 的异常与集合壳子。
- 阶段 6 ⑥-② Startdrive + DCC（2.7.39；2026-09-20 真机验证了驱动对象 / 参数 / BICO，其余待 2.7.40 部署后；**维护者要求：所有测试只在新建的临时设备上做，不碰工程原有设备**）：Startdrive `ReadDriveObjects`、`ReadDriveParameters`、`ManageDriveTelegrams`、`ManageDriveFunctions`、`ManageDriveSecurity`、`ManageTechnologyExtensions`、`ManageDriveHardwareModule`（V21）、`ManageDriveSafetyAcceptanceTest`（V21）、`ReadOnlineDriveParameters`、`ManageOnlineDriveFunctions` + 类型化 `ManageStartdriveParameter`（`driveObjectIndex`、BICO 写入）与 Startdrive 下载 / 上载提示；DCC `ReadDccCharts`、`ManageDccBlock`、`ManageDccPin`、`ManageDccChartInterface`、`ManageDccChartPartition`、`ManageDcbLibraries` + 类型化 `ManageDccChart` / `ReadDccObject`。文件：`Siemens/StartdriveLogic.cs`、`Siemens/DccLogic.cs`、`Siemens/Portal/Portal.Startdrive.cs`、`Siemens/Portal/Portal.Dcc.cs`（`KnownDccExceptions` / `DccStep`）、`ModelContextProtocol/Tools/McpServer.Startdrive.cs`、`McpServer.Dcc.cs`、`tests/.../StartdriveTests.cs`、`DccTests.cs`、`tests/.../StartdriveShapeChecks.cs`、`DccShapeChecks.cs`；旧的 `Portal.DccEngineering.cs` 已删，`Portal.OptionalEngineering.cs` 只剩 SiVArc 通用读写。另修 2.7.38 真机的三处 SiVArc 缺陷（`ManageSivarcRule` → `ManageSivarcTableRule`、`GenerateSiVArc` 传 PLC 设备名、通用 `ManageSiVArcRule delete` 重导航）并给 `Check-DeadToolReferences.py` 加了工具名不分大小写唯一的闸门。
- 阶段 6 ⑥-① SiVArc（2.7.38，**真机验证通过** 2026-09-19——虚拟机其实装有 SIMATIC Visualization Architect V21 且有许可；三处缺陷已在 2.7.39 修，待重跑）：`ReadSivarcRuleTree`、`ManageSivarcRuleContainer`、`ManageSivarcRule`、`ReadSivarcBlockDefinitions`、`ManageSivarcBlockDefinition`、`ResolveSivarcExpression`、`ManageSivarcScreenLayout`（V21）、`UpgradeSivarcDefinitions` + `GenerateSiVArc` / `ReadSiVArcRules` / `ManageSiVArcRule` / `ReadLibraryType typeKind`（加 `hmiFaceplate` / `hmiVbScript` / `hmiCScript` / `unifiedScriptModule` / `sivarc*` / `dccBlockType`）类型化。文件：`Siemens/SivarcLogic.cs`、`Siemens/Portal/Portal.Sivarc.cs`（族表 `SivarcFamilies`：六族的类型化开关；`OptionPackageLibraryTypeKind`）、`ModelContextProtocol/Tools/McpServer.Sivarc.cs`、`tests/.../SivarcTests.cs`、`tests/.../SivarcShapeChecks.cs`。
- 阶段 5 经典 WinCC（2.7.37，真机已验其 Unified 拒绝路径与 `typeKind`；经典实做路径无工程可验）：`ReadClassicHmiScreenTree`、`ManageClassicHmiScreenObject`、`ManageClassicHmiFolder`、`ManageClassicHmiGraphic` + `ReadClassicHmiScripts` / `ManageClassicHmiScript` / `ReadLibraryType typeKind` / `EngineeringScalarProperties.ValueRenderer`（`ConstValue` / `NullableDateTime`）类型化。文件：`Siemens/ClassicHmiFoldersLogic.cs`、`Siemens/Portal/Portal.ClassicHmiFolders.cs`、`ModelContextProtocol/Tools/McpServer.ClassicHmiFolders.cs`、`tests/.../ClassicHmiFoldersTests.cs`、`tests/.../ClassicHmiFoldersShapeChecks.cs`。
- 阶段 4 ④-③ 工艺对象映射（2.7.36，**真机验证通过** 2026-09-19，临时 `TO_SpeedAxis` V9.0）：`ReadTechnologyObjectTree` + `ReadMotionAxisConfiguration` / `ManageMotionAxis` / `ManageTechnologyObject` / `ConfigureMotionHardwareConnection` 类型化改造（`Portal.TechnologyMapping.cs` 的类型化行、`TypedMotionView`、`ConnectTyped` / `DisconnectTyped`；`targetJson` 新增 `channelType` / `channelIoType` / `channelNumber`）；另修 2.7.35 真机的注释行计数缺陷。文件：`Siemens/Portal/Portal.TechnologyMapping.cs`、`ModelContextProtocol/Tools/McpServer.TechnologyMapping.cs`、`tests/.../TechnologyMappingTests.cs`、`tests/.../TechnologyMappingShapeChecks.cs`。
- 阶段 4 ④-② Step7 收尾（2.7.35，**真机验证通过** 2026-09-19；注释行计数缺陷已在 2.7.36 修）：`ManagePlcExternalSources`、`ReadPlcSystemGroups`、`ReadPlcTagTableConstants`、`ExchangePlcAlarmTextListsXlsx`、`ManagePlcTableEntries`、`ExportPlcProDiagInfo` + 类型化改造（`ManageWatchForceTableWebAccess` 访问规则、OPC UA `OpcUaCommunicationGroup` / `NamespaceAccessRestriction`、`ExportAlarmClasses` / `ImportAlarmClasses` 消息、`ManagePlcSupervision` 设置消息、`ReadLibraryType typeKind`、`ManagePlcUserGroup` 新增 `watchTables` / `externalSources`）；另修 2.7.34 真机的关系回读缺陷。文件：`Siemens/Step7LeftoversLogic.cs`、`Siemens/Portal/Portal.Step7Leftovers.cs`、`ModelContextProtocol/Tools/McpServer.Step7Leftovers.cs`、`tests/.../Step7LeftoversTests.cs`、`tests/.../Step7LeftoversShapeChecks.cs`。
- 阶段 4 ④-① Step7 软件单元与 `PlcSoftware` 小服务（2.7.34，**真机验证通过** 2026-09-19，结果见 `docs/releases/v2.7.34.md#真机结果`；关系回读缺陷已在 2.7.35 修）：`ReadPlcSoftwareUnits`、`ManagePlcDocuments`（命名值类型文档 + UDT 文档导入导出 / 母本 / 库类型实例化）、`ReadPlcChecksums`、`ReadPlcObjectFingerprints`、`ManagePlcBlockWriteProtection`（V21）、`ManageProjectCompilationSettings` + `ManagePlcSoftwareUnit`（`unitKind=safety` / `createFromMasterCopy` / `commentsJson`）、`ReadDeviceItemChannels includeLinkedTags`（V21 `PlcTagProvider`）、`UpdateDeviceAddress`（V21 `ProcessImageProvider`）、类型化 `DocumentImportResultForBlocks` 扩展；另修 2.7.33 真机的事务缺陷（`ForceRealExecution` 无键也写 `dryRun=false`）。文件：`Siemens/SoftwareUnitDeepLogic.cs`、`Siemens/Portal/Portal.SoftwareUnitDeep.cs`、`ModelContextProtocol/Tools/McpServer.SoftwareUnitDeep.cs`、`tests/.../SoftwareUnitDeepTests.cs`、`tests/.../SoftwareUnitDeepShapeChecks.cs`。
- 阶段 3 ③-④ Base 收尾（2.7.33）：`ReadPortalInfo`、`ReadTransferRoutes`、`ManageHardwareUtilities`、`ManageDeviceServiceObjects`、`ReadObjectIdentifier`、`ShowObjectInEditor`、`RunToolsInTransaction` + `OpenProject` UMAC 凭据、`GoOnline` 用户认证 / R/H、`DownloadToPlc` R/H、类型化的传输提示 / 结果 / 属性批量写 / 交叉引用 / 比较 / 目录 / 多用户 / 版本控制 / 设置；2.7.32 真机的五处问题已修（工程级 syslog `create` 只预览、证书 `template` 门、`UmacDevice` 文案、`CheckLibraryUpdates parts`、多实例改绑守卫）。**2.7.33 真机重跑已于 2026-09-19 完成**（`docs/releases/v2.7.33.md#真机结果`）：五处修复、门户诊断、传输路由、硬件工具、设备服务对象、对象标识往返、事务提交与回滚、类型化交叉引用 / 目录行全部通过；**一处缺陷**：`RunToolsInTransaction` 内层调用不带 `dryRun` 时按预览跑却回报 `committed=true`（源码已在本机修好、未发布，见 §2 第 1 步）。
- 阶段 3 ③-③ 用户管理与安全（2.7.32，真机验证通过）：`ManageSyslogServers`（工程级 `SyslogServerProvider` + CPU `SysLogConfigurationManager`）、`ManagePasswordPolicy`（UMAC / PLC / legacy PLC 三套）、`ManageUmcUsers`（offline 与服务器导入的 UMC 用户 / 组、`UmcServerConfigurator`）、`ManagePlcCertificate` 的 `template` / SAN / 密码导入、`ManageProjectUserManagement` 的匿名用户激活；2.7.31 真机的四处库缺陷已修（`LibraryRef` 标签、null `TypeFolder` 守卫、`UpdateCheck` 预拒绝、用户库限定文案）。**2.7.32 真机重跑已于 2026-09-18 完成**：库缺陷修复、`CompareLibraries`（首次真机到达，登记表已回写）、密码策略、CPU 级 syslog 读取、offline UMC 用户 / 组、证书模板 + SAN、匿名用户预览、设备功能权行全部通过；**两处问题**：工程级 `SyslogServerComposition.Create` 让 TIA Portal 崩溃（两次复现）、`ManagePlcCertificate template` 误要求 `certificateId`。记录见 `docs/releases/v2.7.32.md#真机结果`。
- 阶段 3 ③-② 库深层（2.7.31）：`ReadLibraryOverview` / `ReadLibraryType` / `ManageLibraryType` / `CheckLibraryUpdates` / `SynchronizeLibrary` / `CompareLibraryObjects` + `ManageGlobalLibrary`（infos / openInfo / archive）与 `ImportLibraryTypeDocuments`（版本导入）扩展。**2.7.31 真机重跑已于 2026-09-18 完成**：2.7.30 的三处修复（组合刷新、就地捕获已释放代理、通道 accessMode）全部验证通过；全局库头/母版树、`ReadLibraryType` 三种入口、`ManageLibraryType` 预览、`CompareLibraryObjects` 三种对象、`ManageGlobalLibrary infos/openInfo` 通过。**暴露 4 处要在 2.7.32 修的问题**：`ProjectLibrary` 没有 `Name`（工程库上的 `ReadLibraryOverview` / `CheckLibraryUpdates` / `SynchronizeLibrary` 与原有 `CompareLibraries` 全部在读头时失败）；`SystemGlobalLibrary.TypeFolder` 为 null 未守卫（NRE）；`UpdateCheck` 对系统库触发 TIA `NonRecoverableException` 并被连接保护当作断连；`close` 对系统库报 "not found" 而非说明 API 没有 `Close`。记录见 `docs/releases/v2.7.31.md#真机结果`。
- 阶段 3 ③-① 硬件网络深层（2.7.30）：13 个强类型工具（`ReadIoSystems` / `ManageIoSystem`、`ReadNetworkDomains` / `ManageNetworkDomain`、`ReadTransferAreas` / `ManageTransferArea`、`ReadDeviceItemChannels` / `UpdateDeviceItemChannel`、`ReadDeviceAddressing` / `UpdateDeviceAddress`、`ManageDeviceUserGroup`、`ManageDeviceUsers`、`ManagePortInterconnection`）。**2.7.30 真机重跑已于 2026-09-18 完成**：全部到达真实对象，读取与可复原写入通过；暴露两处引擎缺陷（域组合代理在 Create/Delete 后陈旧；`EngineeringObjectDisposedException` 误触发连接失败保护）与一处改进（通道属性名缺 AccessMode），记录见 `docs/releases/v2.7.30.md#真机结果`。
- 阶段 1 Safety（2.7.25）、阶段 2 WinCC Unified（2.7.26–2.7.29）已收口。**2.7.29 真机重跑已于 2026-09-18 完成**（变量导出核对通过；文本/系统文本列表读取与原生导出、`Validate`、画面组建删通过），登记表 `scripts/diagnostics/openness-dynamic-coverage.json` 已回写；仍未真机验证的只剩 `Cpm.*` 与 `HmiConnections.*`（工程无对象）以及 `HmiOpcUaAlarm` / `LoggingTags` 的 `Create`（组合到达但为空）。记录见 `docs/releases/v2.7.29.md#验证`。

已收口的阶段（备查，含当时用过的官方页面 id）：

- ~~阶段 6 ⑥-③ SafetyValidation + Test Suite + Teamcenter + CFC~~ **2.7.42 完成（8 / 34 + 9 / 44 + 7 / 37 + 1 / 7 → 0 / 0，阶段 6 收口）**：Openness 主手册（文档地图 `gpR5ZkKnLSuzVoGX1ovoKg`）"Functions for Safety Validation Assistant" 28 页（`GY8HXDlHez8R~V0qscr9vA` 入口、`sPEojwVVNbOui1FybEHA4A` 建测试 / 组、`tHhcmj9KsHUAPmGi5IaWcQ` 可用设备、`QsZORP4TqaQQFhdFqmKzcg` 有效性、`VwciIWFnOD4uXdmCVmmg2Q` 报告、`YOV3CnmFpwhOAxBGorlGhA` / `_CAAieXK~iPIozbjYw6qOA` 导出导入、`J1Xg6_8Uvnp0QCjwC9CRiw` 安全功能、`Y2_i9Estztq6wGC_FWd~ww` 条件、`WYHODZV6V2WWV7h4QQUpTw` 跟踪配置）与 "Functions for accessing Teamcenter Gateway" 17 页（`kA1Nuiq~N58UEFxiqlleiw` Connect、`BUgCZfOA~5a6VmyRo75_Zg` ConnectSSO、`x65dEYSb7ce5~cWDlCBVXA` Save、`cEAAO4zVo6y_6wWFuVnRNg` 自定义属性、`HKiZByxLu9uWGJy7Ymk1oA` / `8mOMZfX3uNNPuBaJ7~z_kw` 新条目 / 新修订、`u2A03oGZ3eD8d~XWJneZmA` / `iKQ9y3Im~vwaPAHSU9a0mQ` 下载 / 搜索、`mQ3RrTsSHTAFkKRZ~2Aw3w` / `vRoC9x9ZLxec5Yh5kXLrSA` / `QdMpS5O_iE~oW~b7suKHTg` 锁）；Test Suite 手册（文档地图 `i3KAb2aA6rlZXMaHdjXYzQ`）"Openness Support" 章 37 页（`NSHcekhoOh4HUnEXZYj2NQ` 规则集作用域、`WGPTp9Llk1NU_797VfJYTA` / `aV1ptsofaYA~AdBsUi9czQ` / `2GGYL53NgiwD4wCOYtjpyg` 三类执行、`Qy6Pai40nhREp7fclrFBCw` / `tnJ9CWCEtdmApQHtkERrBA` / `c~July4K7daytKKinmqoVg` 导入导出、`dddHh6zi9HVAxFWhFwi_eg` / `0_9sawP5Vww46heiFiCJeg` 用例作用域、`rDkCu53SRge7hJRIrEMjxA` / `2aFktG6StxhbfN4n5kml9A` 测试集、`hEf9KgKTv1y2Cxg40ND23g` 异常）；CFC 手册（文档地图 `5LsFzdksZFmsqKBcHDZNFQ`）9 页（`xIQOOuWNYHxhkk7kqZco2w` / `j20wRRNIg2BxGV_0p7e6Qw` / `6E5VuxfhnsELedG9sCZFAA` 导出导入、`ND_Jyj6ZjL3QmxR3sEu1Ng` / `_MSenNo~iM8AQh04iVRnaA` / `SHFk0_PsBqKuLhQ1YJHi7w` / `wi8SiUSCeHDvqKEtGjkVng` 密码）。手册与 PublicAPI 差异：手册的 `Connect` 参数顺序在两页里互相矛盾，以 PublicAPI 的 `(userName, password, group, role, hostURL, instance)` 为准；`TestCase.SetScope(PlcSoftware)` 回 bool，三参数重载回 void；`RuleSet.SaveToFile` 回 bool（手册写 void）；`SearchResult.RevisionId` 是 `IEnumerable<string>`。
- ~~阶段 6 ⑥-② Startdrive + DCC~~ **2.7.39 完成（34 / 117 + 13 / 68 → 0 / 0）**：Startdrive 手册 "Functions for Startdrive"（文档地图 `WtRgStszJ5UCQ8X~aGwgnA`）：`HhjJAJi6U6yZimx_z84Blw`（DriveObjectContainer）、`_TD2umad8hu_owqAlOBIrQ`（DriveObject）、`FE1T8V7YQsMURQQT1dSAUg` / `MivZPt2mcK_5OsqkWM6GkA` / `Obb73kGdPxUMcDJWCWE5QQ`（确定驱动对象 / 号 / 类型）、`zZPPnm4NYgnCTcC7kOrmCw` / `YGV1jVGdI1o~IDg9dUNpuQ`（参数与组合）、`gsh3DvaCMe6XhZxa3~bhkA` / `8qzSI46WtOOkyl1FCa~ZWw` / `IuN8oMgvtQY8_W8LCugRuQ` / `QaYbrzsvR8FZE6Pj4CJeHw`（参数读写、BICO、在线）、`1BdKwwY3Ro6_h38bnSGzaw` / `9MCspscINoS769brxwlTWw` / `JkKSwarEADJm6fVF2r_Tig` / `2hcjC1B9hxaXkK494B55yw`（报文）、`fJVm990bWGMn1T1hQfUcJw` / `luFFZ4KOOew4out5dTE0QA` / `9i1Fm9PBoMQgcztPQQ31ow` / `GYIG1xfucCDd6MKMux~3sw` / `QytO3bAMC70UKTD60SyMfw`（报文用法、安全 / 转矩报文、工艺对象连接、SDR 接口）、`1t1JcXrIfj3QZK2DBR5RBQ` / `8neihYbn4qPmkjfLr7eZCQ` / `OQLdt31J7MLxs5rQ7yRO~g` / `DuwGG7o7nHijZqGIMljvtg`（驱动功能、激活、类型处理器）、`NyWBNrnpP0J4wWuNJ9o7Xg` / `4pjKMBBlF0mA6qW203YhyA` / `E84A1tFT5m1tiJd4wsaHJQ` / `ricJazzVOSw3EgdUGVeSdw` / `YMUPb8bp2IVSuU09SKD9cg` / `XoobNBUGyIBDPfNUrMOWHQ`（硬件投影、电机 / 编码器配置）、`rn~vwv4m_T6ByVZkRj4FgA` / `QpMH0w76WHOqdXIAXDVCsg`（Function in Use）、`G5Qw_gwCX1T7ARA5r~81Pg` / `eOqPRo5wuxrzr53Vod533g`（调试、SIMOGEAR）、`N2H48mjX31D8flpRYg10jg` / `klFcUYa0nyM1ZUgH5xZK5g`（安全校验和）、`mqk~eBMocBb0NUGxJjWftg` / `fsONddjBBOVrhx2CHVc6eQ` / `fctL7fDAt3xTg_TfvFL3xA`（在线域功能）、`mEuFMvHjKIHZXW03t8npdQ` / `5EkeDKn5_MUz08k7HLsGnA` / `QLB~zWRjohOGkWM7iup6rQ` / `d0fLPiNFeGK8xdhoWP8ZbQ`（UMAC / DDE）、`19Uyman1Wb68bLTwA9BcGQ` / `~OkhDKKH4EKtPFHdr4Qw_A` / `aDo6a1WbtYCzXdhTqQUq4g`（工艺扩展）、`_jpYzA7sfaV~r0tx1G_moA` / `mu6jwoT0akY16NHzwHw3Fw` / `WzdsHjId9iZmKnIXk37ahQ` / `EMk5xjhBmkgky6FISUjQrQ`（硬件模块、模块访问点、硬件标识）、`1owqlE4jXz6wMqb82OUvHg`（下载检查）；DCC 手册 "Functions for DCC"（`3BtKZ42eEtW7HPmLDWNxyw`，2026-04 版）：`wqVCOS1y3~D5hgH9ssp1MQ` / `OTFWlYtRZDTw1WQzWvJllw`（容器）、`vkm73BEZkeySIj_ZPuqfQg` / `ruhc4H3ZP2~h9~Xt3rClsA` / `zCJY46SwmEABX2iQWkEy2g`（图表、组合、Statement）、`O1fb8u2TXCNukNWCp4~aWw` / `kDCia2JY11rHCBhfwgo4yg`（块）、`3laToW8l9i4350HsD9XSnw` / `K0nEmO8MV8wc1tbRSf_3Ig` / `Lo6ME_A35Lk1E1XGAVisxA`（引脚、参数、连接）、`X9jB8igC4F~hHaXkFcNKqQ`（分区）、`DnZuvDotXPBAh823i_26aQ` / `0z_Ke5PaIxhXcvMZfKsvag`（DCB 库、导入）、`kkLSxUkJSCsxIgIgzSVcuw` / `xov9SoLa60vHJQyege5XxA` / `L9dIK5RNIyEr4NVLCfSFDQ` / `tP5BD1RK6sOvo8E0ygef4Q` / `yw4NyjW5EgwmUVYs5VyejA` / `pSoZCAEwWilPHgFiCUoqYQ` / `FQijZxwgw32sABPvLDsaEA` / `HEZ4~Qc_j5waqwmZjVLedw` / `oKIvFBryNxIB043dwtSl8Q` / `350JxH5IYrPUmvAhEUBo_A`（示例）、`6TjmDK5Jw1VIwkALksNf2A`（异常）。SafetyAcceptanceTest* 手册无页，按 PublicAPI XML 摘要封装。
- ~~阶段 6 ⑥-① SiVArc~~ **2.7.38 完成（33 / 198 → 0 / 0）**：SiVArc 手册 "SiVArc Openness" 章（文档地图 `TOFaq6RHmuKvo0~qDLnlOg`）：`7jsotg3vRMHP5fMp4~mpbg`（目录）、`JNDUmjTZelImVpuyAWcg9w` / `A7fhbqEnyPl_noKRDZehsw` / `u6oPjTTEJ4abhw9zRPS5aA`（表 / 文件夹建删）、`G5ktNwrTqpegOskwzELRHw` / `ag0o7vJ1Lcxr9rOEs0ABJA`（规则与组）、`iZ3bqYOjRUgaZnAET1rsLg` / `Ed5HkSoKjQpP4HKvEzFnnA` / `rochzr136ReXk_O5IFHyBQ`（建 / 属性 / 删）、`83UuJtvWQoOFiJAFi77dtw`（母本复制）、`Yq_tCdg_kwuYJaX9FVHEFQ` / `HM_JoVhRy6KSohy6Xn6lRA`（库对象与文件夹引用）、`XWQeSFvM4tXbT6uScH4xtQ`（PLC / HMI 设备列）、`7xFfjWDCQlMwXI9KQ~FNKw`（变量规则）、`wR0s~JbToYoHyJMbyBgkjQ` / `drxfjMfZPmbSl95ax9IAvw` / `xFmNLyFSN69vYrbPVAkLwg`（库类型实例化 / 更新）、`leIwOikkueZKInsP78Ve_Q` / `yQQQnmGbdBeKYfeXx7~3Sg`（变量 / 文本定义、成员设置）、`E6M_JL8eOiEZkUtnwJ3JHg`（表达式解析器）、`~NQEART_ZAeGGt0M1jOvBQ`（布局字段）、`_4_Q~ngjdmqlENnMReRStg`（定义升级）、`XnoZ1AsuHzhsclVJsJgb0Q`（生成）、`0aucChIKavsGvP9kTa_~kw`（许可）。
- ~~阶段 5 经典 WinCC（`Siemens.Engineering.WinCC.dll`）~~ **2.7.37 完成，功能类型缺口 0 / 0**（官方页面 id：弹出画面 `Bs8q~5oMGOhyPiULc5EjXg` / `WfkApIgS2OeHao78qoNfYQ`、滑入 `t9yZaXoGpzKIvND1XMsAKQ` / `g0G3yI1viTlSPt3pddPr_A`、模板 `~xq~afYOxwUtTqH3V1I_Tw` / `lOIf7qsktx_jISESVA846Q`、变量文件夹 `NjqFj5DVnfDkwuPfn6k8Xg` / `nda76ZAvfbuD9koVDoGIJQ`、图形 `txSvxCRTH2TMhIO~7G8jLw`、VB 脚本 `jA1DvFRkXxu0ll1Btu5hRw` / `iQJjjjTJbZJl4VkE_aN_Bw`）。原清单：`Hmi.Screen` 17 / 38（`ScreenPopup(Folder/SystemFolder/UserFolder)`、`ScreenSlidein(SystemFolder)`、`ScreenTemplate(Folder/SystemFolder/UserFolder)`、`ScreenSystemFolder` / `ScreenUserFolder`、`ScreenGlobalElements` / `ScreenOverview`、`ScreenLibraryType` / `StyleLibraryType` / `StyleSheetLibraryType`）、`Hmi.RuntimeScripting` 3 / 10（`VBScript`、`VBScriptSystemFolder` / `VBScriptUserFolder`）、`Hmi.Tag` 3 / 8（`TagSystemFolder` / `TagUserFolder`、`HmiUdtLibraryType`）、`Hmi.Globalization` 1 / 3（`MultiLingualGraphic`）。参考工程的 HMI 是 Unified（`HMI_RT_1`），经典 WinCC 只能形状检查 + 现有 `Portal.ClassicHmi*` 的类型化改造；先看 `docs/reference/openness-coverage.md` 的经典 WinCC 行与 `Portal.MotionProDiagClassicHmi.cs` 里已有的经典 HMI 工具（`ManageClassicHmiScripts` / `ManageClassicHmiCycles` / `ManageClassicHmiTextLists`…）。
- ~~阶段 4 Step7（`Siemens.Engineering.Step7.dll`）~~ **2.7.34–2.7.36 完成，功能类型缺口 0 / 0**：
   - ~~④-① 软件单元 + `PlcDocument*` + `PlcChecksumProvider` / `PlcSimulationSettingsProvider` / `PlcBlockWriteProtectionProvider`~~ **2.7.34 完成**（官方页面 id：软件单元 `0h0oxLVauEUie7gZF4QPWw` / `cRardLf5JWn6RV4GwzVX2Q` / `1XBGooV5zT1c4pMm~pUcDQ` / `R3UTOiWAisuD36RmvULj9Q` / `JT6mLOEcCLGxh5bzCM6sxw`、安全单元 `nRkWKOSJ40n_TTB9pS3iEw` / `UXq9Lu_poeISbbKKJiZn7Q`、NVT 文档 `pi7xDzfb1aKRSwG4KIQP3g`、UDT 文档 `H_AHIQyhM0nIyyGLbQm7fQ` / `f7I~jdKwF~pGMltuKUvtAQ`、校验和 `4oWqygP07f_Pxz7N2LEwBw`、指纹 `CU2HzxWdQiVq8MOz7QkvNw`、写保护 `4qvX8TvoaH0t9f7LiQc_3g`、工程属性 `~zGrGn3wwJUHBCn6pO5Cjg`）。
   - ~~④-② Step7 收尾（外部源、系统组、常量、报警文本列表 XLSX、表条目、ProDiag、类型化改造）~~ **2.7.35 完成**（官方页面 id：外部源 `w7zZEXJa7dPksEDThvBSLw` / `PweFezJaXiy8nXiDWOWjhA` / `LLfdxabkgrjwzUfgkUOObw`、系统组 `uD8mauDSD~GqtowlNv9_7w` / `hyeOAzoyHSUcDfnBMNypGg`、文本列表 XLSX `Uj~HCXBceURnWIxp6Ea7oQ`、报警类 `uIFhxfIuwL~guvUnJIVf0Q`、ProDiag `EJJOqxC6VzWw3z5NX7RJjw`、OPC UA 接口 `y66Vhyx3jKO68WEVRckErQ`）。
   - ~~④-③ 工艺对象映射~~ **2.7.36 完成**（`Portal.TechnologyMapping.cs`；官方 Openness 地图的 Motion Control 章节不在文档站的 `clustered-search` 索引里，依据是 `Siemens.Engineering.Step7.xml` 摘要）。

剩余缺口一览（2.7.42 审计，未封装功能类型 / 成员；壳子类型不计）：

| 层次 | 剩余 | 内容 |
|---|---|---|
| 核心 | 0 / 0 | Base 0 / 0、Step7 0 / 0、经典 WinCC 0 / 0 |
| ├ ~~③-④ Base 收尾~~ | 0 / 0 | `Siemens.Engineering` 根命名空间 10 / 28（`TiaPortalSession` / `Transaction`、`TiaPortalProduct`、`AttributeConfiguration`、`UmacCredentials`…）、Download / Upload / Online / Compiler 结果消息与配置 17 / 41、Connection 3 / 7、Multiuser 3 / 7、Settings 2 / 5、VersionControl 2 / 2、Compare 1 / 5、CrossReference 2 / 15、HW 零头 13 / 40 |
| ├ ~~④-① 软件单元与 PlcSoftware 小服务~~ | 0 / 0 | 2.7.34 封装：`SW.Units` 3 / 18、`SW` 11 / 14、`PlcBlockWriteProtectionProvider` 1 / 6 |
| ├ ~~④-② Step7 收尾~~ | 0 / 0 | 2.7.35 封装：`SW.ExternalSources` 4 / 13、`SW.Blocks` 3 / 5、`SW.Types` 4 / 6、`SW.Tags` 2 / 6、报警 / 监控结果与 `PlcAlarmTextListProvider` 3 / 6、OPC UA 2 / 7、表规则 / 条目 5 / 15、`TechnologicalInstanceDBUserGroup` 1 / 2 |
| ├ ~~④-③ 工艺对象映射~~ | 0 / 0 | 2.7.36 封装：`TOMapping` 3、`AxisEncoderHardwareConnectionInterface` 19、`TorqueHardwareConnectionInterface` 14、`TechnologicalInstanceDBGroup` 4、`TechnologicalParameter` 2 |
| └ ~~阶段 5 经典 WinCC~~ | 0 / 0 | 2.7.37 封装：`Hmi.Screen` 17 / 38、`Hmi.RuntimeScripting` 3 / 10、`Hmi.Tag` 3 / 8、`Hmi.Globalization` 1 / 3 |
| ~~WinCC.Extension~~ | 0 / 0 | 2.7.37：`ConstValue`、`NullableDateTime` 经值渲染钩子 |
| ├ ~~⑥-① SiVArc~~ | 0 / 0 | 2.7.38 封装：`SiVArc` 33 / 198（六个规则族 26、服务与解析器 4、定义 / 成员设置 3、布局数据 1）+ 32 个壳子类型也有专用引用 |
| ├ ~~⑥-② Startdrive + DCC~~ | 0 / 0 | 2.7.39 封装：Startdrive 34 / 117（`MC.Drives` 驱动对象 / 参数 / 报文 / 安全 / 工艺扩展、`DFI` 16 个功能类型、V21 硬件模块 / 验收测试 / SDR 接口、上载提示）、DCC 13 / 68（图表 / 块 / 引脚 / 参数 / 连接 / 接口 / 分区 / DCB 库 / 导入器 / 两个接口）+ 39 个异常类点名 |
| └ ~~⑥-③ SafetyValidation + Test Suite + Teamcenter + CFC~~ | 0 / 0 | 2.7.42 封装：SafetyValidation 8 / 34（助手 / 设备查询 / 激活测试 / 组 / 打印 / 安全功能 / 条件 / 跟踪 / 有效性）、TestSuite 9 / 44（三个系统组、规则集 / 用例 / 系统用例 / 测试集、结果与消息）、Teamcenter 7 / 37（连接 / 锁 / 搜索下载 / 工作流提供者、条目 / 修订详情、属性）、CFC 1 / 7（`ChartProvider` + `ChartProviderS7`）——只有形状检查（SafetyValidation / Teamcenter 无许可 / 环境） |
| **合计** | **0 / 0** | 2.7.39 为 25 / 122，2.7.38 为 72 / 307，2.7.37 为 107 / 515，2.7.36 为 133 / 585，2.7.35 为 139 / 637，2.7.34 为 162 / 695，2.7.33 为 178 / 735，2.7.32 为 230 / 896，2.7.31 为 239 / 929，2.7.30 为 253 / 1,006，2.7.29 为 267 / 1,067 |

## 3. 阶段 6 剩余类型清单（历史：2.7.39 审计，`status=UNTOUCHED` 且非壳子；括号里是成员数；**2.7.42 已全部归零，本表只作 2.7.42 实做范围的对照**）

| 程序集 | 功能类型 / 成员 | 壳子类型 | 类型（`Siemens.Engineering.` 前缀省略） |
|---|---|---:|---|
| SafetyValidation | 8 / 34 | 5 | `SafetyValidation.ActivationTest`(11)、`SafetyValidation.ActivationTestPrintout`(1)、`SafetyValidation.ActivationTestUserGroup`(4)、`SafetyValidation.DeviceQuery`(1)、`SafetyValidation.SafetyFunction`(11)、`SafetyValidation.SafetyValidationAssistant`(3)、`SafetyValidation.TestValidity`(1)、`SafetyValidation.TraceConfiguration`(2) |
| TestSuite | 9 / 44 | 7 | `TestSuite.ApplicationTest.ApplicationTestSet`(3)、`TestSuite.ApplicationTest.ApplicationTestSystemGroup`(4)、`TestSuite.ApplicationTest.TestCase`(7)、`TestSuite.StyleGuide.RuleSet`(7)、`TestSuite.StyleGuide.StyleGuideSystemGroup`(2)、`TestSuite.SystemTest.SystemTestCase`(8)、`TestSuite.SystemTest.SystemTestSystemGroup`(2)、`TestSuite.TestResults`(4)、`TestSuite.TestResultsMessage`(7) |
| TeamcenterGateway | 7 / 37 | 5 | `TeamcenterGateway.ItemDetails`(7)、`TeamcenterGateway.RevisionDetails`(2)、`TeamcenterGateway.TcGatewayLockProvider`(3)、`TeamcenterGateway.TcGatewaySearchAndDownloadProvider`(2)、`TeamcenterGateway.TcGatewayWorkflowProvider`(9)、`TeamcenterGateway.TeamcenterConnectionProvider`(3)、`TeamcenterGateway.TeamcenterProperty`(11) |
| CFC | 1 / 7 | 0 | `SW.FunctionCharts.ChartProvider`(7) |

说明（2.7.42 起本表为历史）：壳子类型（`*Composition` / `*Association` / `*EventArgs` / `*Exception` / `*Result` / `*Info`）不计缺口，但类型化封装时顺手引用它们（`Items(...)` 的类型化局部变量）会让审计的"有专用引用"一起涨；DCC 的 39 个异常类已在 `Portal.KnownDccExceptions` 里点名（审计记为"仅类型名"）。`SafetyValidation.Condition` 已被 2.7.38 的规则代码词法误记为已引用，这里仍按 2.7.37 的 9 / 43 计。2.7.42 之前的反射入口 TestSuite → `ReadTestSuiteCases` / `ExchangeTestSuiteCase` / `RunTestSuiteCase`、CFC → `ExchangeCfcCharts` 已改为类型化实现（签名兼容），Teamcenter / SafetyValidation 的入口在 2.7.42 新建。重新生成本清单：跑 §3 的审计命令后 `python` 读 `V21-types.csv` 过滤 `assembly` 与 `status`。
