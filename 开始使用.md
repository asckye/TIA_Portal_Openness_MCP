# 开始使用

本完整包包含 TIA Portal V20 / V21 对应的 MCP 程序、依赖、原版配置入口、手册、模板、源码和测试。两个 EXE 的文件版本均为 **2.7.9.0**，设备分组内 PLC 查找修复、图形选择和 HMI 启动设置接口已编入程序，无需旧版包补文件。

## 运行前提

- 在安装对应版本 TIA Portal、Openness 和 .NET Framework 4.8 的 Windows 系统中运行。TIA 在虚拟机时，MCP 也在该虚拟机运行。
- 运行用户须已加入 `Siemens TIA Openness` 组；新加入后注销并重新登录 Windows。
- Siemens PublicAPI 使用本机 TIA 安装提供的版本，不随本包分发。
- 整包解压到新目录，保留现有连接配置和密钥。本公开包不含个人连接密钥。

## 配置与启动

同机 AI 客户端使用原有配置方式：V21 双击 `配置MCP.bat`，V20 双击 `配置MCP-v20.bat`；完成后重启对应客户端。

宿主机连接虚拟机的 HTTP 场景，请沿用已有 HTTP 启动参数、端口和密钥，将程序路径指向新版 `runtime/v21/TiaMcpServer.exe`（V20 使用 `runtime/v20/TiaMcpServer.exe`）。配置 BAT 用于同机客户端注册，不会自动完成虚拟机网络和远程 HTTP 配置。完整参数见 [中文说明](README.zh-CN.md)。

命令行入口为 `tia.cmd`（V21）或 `tia-v20.cmd`（V20）。完整运行依赖保存在相应 `runtime` 目录，请不要只复制 EXE。包内另外保留旧手册引用的 `bin/Release/net48` 和 `bin-v20/Release/net48` 兼容目录，其内容与相应 runtime 一致。

## 修复与验证

见 [本版说明](docs/releases/v2.7.9.md)。本版修复设备分组内 PLC 的唯一精确名称查找，补充图形对象选择/坐标对比，以及默认预览、带令牌和回读验证的 HMI Runtime 启动画面/运行设置修改。保留 HTTP 鉴权、HMI 递归、全局脚本编辑及已有稳定性修复。离线回归和两版实际 EXE 检查结果记录在 `manifest/release-build.json`。**新修复及写入接口尚未完成真实博途工程验收；原生图形组合、面板内部绑定和 V20 Unified 原生能力仍有已知缺口。**

`RELEASE_STATUS.txt` 记录源码提交；`manifest/release-file-hashes.json` 记录包内文件校验值。ZIP 校验值见 Release 的 `.sha256` 附件。可运行 `scripts/Validate-Bundle.ps1 -Strict` 检查解压后的包。
