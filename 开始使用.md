# 开始使用

本完整包包含 TIA Portal V20 / V21 对应的 MCP 程序、依赖、WPF 图形配置入口、手册、模板、源码和测试。交付包版本 **2.7.16**；两个引擎 EXE 沿用已验证的 **2.7.15.0**，设备分组内 PLC 查找修复、图形选择和 HMI 启动设置接口已编入程序，无需旧版包补文件。

## 运行前提

- 在安装对应版本 TIA Portal、Openness 和 .NET Framework 4.8 的 Windows 系统中运行。TIA 在虚拟机时，MCP 也在该虚拟机运行。
- 运行用户须已加入 `Siemens TIA Openness` 组；新加入后注销并重新登录 Windows。
- Siemens PublicAPI 使用本机 TIA 安装提供的版本，不随本包分发。
- 整包解压到新目录，保留现有连接配置和密钥。本公开包不含个人连接密钥。

## 配置与启动

推荐双击根目录 **`TiaMcpConfigurator.exe`** 打开配置窗口，无需输入命令：

- **TIA 在虚拟机里**：在虚拟机选择“虚拟机服务端”，填写 V20/V21、TIA 安装目录、IPv4、端口和密钥，保存后点击“配置网络权限”，再点“启动服务”。在宿主机打开同一个 EXE，选择“AI 客户端”，多选要配置的 AI，填写相同地址、端口、密钥，测试并保存。
- **TIA 和 AI 在同一台电脑**：填写 TIA 版本与目录后，选择“本机连接”，勾选客户端并保存。
- 支持 Claude Code、Claude Desktop、Codex、Cursor、VS Code、Gemini CLI、Windsurf、Cline。Claude 官方桌面 Code 页选择“Claude Code”，保存后使用 **Code → Local**；Chat 页需 Node.js/mcp-remote 桥接。
- 以后在虚拟机打开窗口直接点“启动服务”，保持窗口打开。详细操作及保存位置见 [图形配置指南](docs/gui-configuration.md)。

旧配置 BAT 与 tia CMD 已由 GUI 替代并删除。命令行操作直接运行 `runtime\v21\TiaMcpServer.exe`（V20 改用 `runtime\v20`），例如 `runtime\v21\TiaMcpServer.exe doctor`。完整依赖必须保留，不要只复制 EXE。工程生成、预热和取证脚本仍保留独立用途。

## 修复与验证

见 [本版说明](docs/releases/v2.7.16.md)。引擎已有 38 个扩展专用入口，覆盖 Unified 属性/事件/归档/工厂视图、工程文本/语言/库和 PLC 原生交换及部分选件，详见 [实现与缺口清单](docs/openness-expansion-progress.md)。新增写入默认仅预览。保留 HTTP 鉴权、HMI 递归、全局脚本编辑及已有稳定性修复。原引擎离线回归与两版 EXE 检查保留在 `manifest/release-build.json`；新配置器测试见 `manifest/configurator-build.json`，交付组成见 `manifest/delivery.json`。**新修复及写入接口尚未完成真实博途工程验收；原生图形组合、面板内部绑定和 V20 Unified 原生能力仍有已知缺口。**

`RELEASE_STATUS.txt` 记录源码提交；`manifest/release-file-hashes.json` 记录包内文件校验值。ZIP 校验值见 Release 的 `.sha256` 附件。可运行 `scripts/Validate-Bundle.ps1 -Strict` 检查解压后的包。
